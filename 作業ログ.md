# 作業ログ

## いまの作業
ロボ組み立て（系統 2：両側上半身＝Variant.BothUpperMeshFilter）

コードで確認した分岐：UsesMirror=true / UsesBridge=false / UsesSkin=false。
したがって 仕切り・面削除・穴点数合わせ・穴つなぎ・スキンド変換・ウェイト塗り は行わない。
部位は 8 つ（センター／上半身／上半身2／首／頭／左腕／左ひじ／左手首）。

段の並び：
S1 図形生成（モデル作成 → 8 部位 → 左手首_姿勢 → マテリアル色）
S2 階層 → S3 ミラー分岐ルート → S6 Humanoid割当 → VRM 書き出し

応答の分割（4 回）：
1. resetProject＋胴 3 つ ← 完了
2. 首・頭・腕 3 つ＋左手首_姿勢＋マテリアル色 ← 完了
3. 階層＋ミラー分岐ルート ← 完了
4. Humanoid 割当＋VRM 書き出し＋キャプチャ ← 完了

## 完了した段

- S0 モデル作成（resetProject "StickRobot"）
  前回の系統 1（片側上半身・スキンド済）は利用者の承認を得て破棄
  検算：queryModelStructure が meshContexts=0 / drawables=0

- S1 前半：胴 3 つ（すべて createStadiumBox、capSegments 3 / lengthSegments 3 /
  capBottom false / pivot 0,-0.5,0 / placeRotation 0,0,0 / bakeRotation true /
  addMode NewObject / materialIndex 0）
  masterIndex 0 センター  0.22/0.10/0.12 高さ分割1 上蓋なし → (0, 0.90, 0) 頂点24 面12 穴2
  masterIndex 1 上半身    0.20/0.18/0.11 高さ分割1 上蓋なし → (0, 1.09, 0) 頂点24 面12 穴2
  masterIndex 2 上半身2   0.22/0.18/0.12 高さ分割3 上蓋あり → (0, 1.36, 0) 頂点64 面60 穴1
  検算：センターの getRawData 座標が X±0.10196 / Y 0〜0.1 / Z±0.06（補足 4-3 と一致）
        queryModelStructure が drawables=3

- S1 後半：首・頭・腕 3 つ（すべて createCylinder、radialSegments 6 / heightSegments 1 /
  上下蓋なし / edgeRadius 0 / pivot 0,-0.5,0 / bakeRotation true / addMode NewObject /
  materialIndex 0）。いずれも 頂点12 面6 穴2
  masterIndex 3 首     r0.035 h0.080 回転なし   → (0, 1.61, 0)
  masterIndex 4 頭     r0.100 h0.200 回転なし   → (0, 1.76, 0)
  masterIndex 5 左腕   r0.045 h0.150 RotZ=90    → (-0.1853, 1.52, 0)
  masterIndex 6 左ひじ r0.040 h0.135 RotZ=90    → (-0.4253, 1.52, 0)
  masterIndex 7 左手首 r0.045 h0.095 RotZ=90    → わざと (-0.6503, 1.30, 0)
  検算：首の getRawData が Y 0〜0.08・X/Z ±0.035（長さが Y＝回転なし）
        左腕の getRawData が X 0〜-0.15・Y/Z ±0.045（長さが X＝補足 4-3 と一致）

- 左手首_姿勢（シナリオの MisplacedPart。生成時 Y=1.30 → 正しい 1.52 へ直す）
  beginBoneTransformSliderDrag(7) → setBoneTransformValue PositionX/Y/Z → end("左手首 姿勢")
  検算：uiGetValue で 直す前 positionY=1.3 → 直した後 positionX=-0.6503 / positionY=1.52
        rotationZ=0（回転は頂点へ焼き込み済みで姿勢側は 0）

- マテリアル色：setMaterialColor slot0 = 0.62,0.68,0.75,1
  検算：queryModelStructure が meshContexts=8 / drawables=8 / bones=0
  ※ 色そのものは数値で読み返す手段を見つけていない。キャプチャで灰青を確認した

- S2 階層：reorderMeshes（category=Drawable / preserveWorldTransform=true）
  entryValues = 1,1,0, 2,2,1, 3,3,2, 4,4,3, 5,3,2, 6,4,5, 7,5,6（親を持つ 7 件のみ）
  検算：左手首 positionX=-0.225 / 頭 positionY=0.15 / 左腕 positionX=-0.1853
        いずれもレシピの Origin（親からの相対値）と一致。drawables=8 のまま
  キャプチャ Robot2_S2_hierarchy_0001.png：見た目の位置は変わっていない
        （preserveWorldTransform が効いている）

- S3 ミラー分岐ルート：setMirrorBranchRoot masterIndices=5（左腕） value=true
  MirrorBranchRoots は {左腕, 左足} だが左足はこの系統に無いので左腕のみ
  検算：uiGetValue meshList.detail.mirrorBranchRoot
        左腕（索引5）= true / 上半身2（索引2）= false
  キャプチャ Robot2_S3_mirror_0001.png：右腕は出ていない（この系統では実体が作られないので正常）

- S6 Humanoid割当（UI 経由。HowTo のとおり自動割当 → 適用）
  uiShowPanel humanoidMapping → uiSetValue humanoidMapping.includeNonBone=true
  → uiClick humanoidMapping.autoMap → uiClick humanoidMapping.apply
  ボーン 0 本のモデルなので「ボーン以外も候補に含める」が要る
  検算：humanoidMapping.mappedCount =「マッピング済: 8 ボーン」
        humanoidMapping.modelMapping =「このモデル: マッピング設定済み 8 ボーン（必須未割当 9 件）」
        必須未割当 9 件は脚など。だから書き出しで supplementHumanoid が要る
  ※ queryBoneSkin は全項目 0。MeshType.Bone が 1 本も無い系統なので当然で、失敗ではない

- VRM 書き出し：exportVrmFile
  filePath=StickRobot_両側上半身.vrm / supplementHumanoid=true / exportSkinning=false
  （exportSkinning はシナリオが UsesSkin(v) を渡す。この系統は false）
  結果：D:\UNITY保存の品\TEO3.0開発中\PolyLing\StickRobot_両側上半身.vrm
        exists=true / 22,316 バイト
  キャプチャ Robot2_final_0001.png

## 系統 2（両側上半身）完了

## 次にやること
（未定）

## 確認できなかったこと
- マテリアル色の数値での読み返し（キャプチャで灰青を確認済み）
- 3D プレビューの更新が遅れていたかどうか。撮影前の画面を見ていないので切り分け不能
- 書き出した VRM をビューアで読み込んでの確認（ファイルの存在と大きさまでしか見ていない）

## 懸念事項（次回も引き継ぐ）
- **確認用キャプチャを撮り忘れた。** S1 を終えるまで 1 枚も撮らず、利用者に指摘されてから撮った。
  数値検算（getRawData / uiGetValue）だけで済ませない。段ごとに unity_capture を撮る
- **3D プレビューの更新の遅れ。** 左手首_姿勢のあと、画面が遅れて更新されていた疑いが残った。
  数値は 3 回とも撮影前に正しい値（positionY=1.52、頂点は原点中心に対称）を返しており、
  データ側は正しい。分からないのは画面の側だけ。
  切り分け方：姿勢を変える段では変更前と変更直後に 1 枚ずつ撮って比べる。
  ProjectSettings.asset:86 は runInBackground: 0（背面ではフレームが進まない）

## 気づいた点
- 系統 2 では右腕の実体は作られない。SetMirrorBranchRootCommand は印を付けるだけで、
  実体を作るのは ConvertMeshFilterToSkinnedCommand（この系統は変換しない）。
  → 補足手順書へ「系統ごとにミラーの実体がいつ出来るか」を書き足す候補
- 生成を polyling_call で直接送る場合、params / placement を毎回すべて明示するので
  「前の部位の RotZ が残る」事故は起きない（UI 経由のときだけの注意点）
