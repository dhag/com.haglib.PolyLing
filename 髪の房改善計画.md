# PolyLing 髪の房（Hair Strand）改善計画

対象リポジトリ: `dhag/com.haglib.PolyLing`

主な対象ファイル:

- `Runtime/Poly_Ling_Main/Tools/PrimitiveMesh/HairStrandParams.cs`
- `Runtime/Poly_Ling_Main/Tools/PrimitiveMesh/HairStrandMeshGenerator.cs`
- `Runtime/Poly_Ling_Player/View/PrimitiveMesh/PlayerPrimitiveMeshSubPanel.HairStrand.cs`
- `Runtime/Poly_Ling_Player/View/PrimitiveMesh/PrimitiveMeshTexts.cs`

---

## 1. 目的

PolyLing の「髪の房」生成機能について、現在の形状制御を整理し、調整時に発生する不自然な「切れ目」「つなぎ目」「折れ」を抑える。

特に、髪の房の幅・厚みプロファイルについて、

- 開始部分
- 中間部分
- 終端部分

という造形上の考え方は残しつつ、各区間の接続を滑らかにする。

最終的な目標は、ユーザーが数個の直感的なパラメータを調整するだけで、連続かつ自然な髪の房形状を得られるようにすることである。

---

## 2. 現在の実装

現在の `HairStrandMeshGenerator` では、房の長手方向の幅と厚みを `Profile()` 関数で求めている。

幅の場合、主に次のパラメータを使用している。

```text
WidthRoot
WidthMid
WidthTip
WidthMidT
WidthPowRoot
WidthPowTip
```

厚みについても同様に、

```text
ThickRoot
ThickMid
ThickTip
ThickMidT
ThickPowRoot
ThickPowTip
```

を持っている。

現在の `Profile()` は、中間位置 `tm` を境界にして2本の冪関数を接続している。

開始側:

\[
w(t)
=
w_r
+
(w_m-w_r)
\left(\frac{t}{t_m}\right)^{p_r}
\]

終端側:

\[
w(t)
=
w_t
+
(w_m-w_t)
\left(
1-\frac{t-t_m}{1-t_m}
\right)^{p_t}
\]

この方式では、

- `t = 0` で `Root`
- `t = tm` で `Mid`
- `t = 1` で `Tip`

を正確に通る。

したがって、値そのものは連続している。

---

## 3. 現在の問題

最大の問題は、接続点で「値」は連続していても「傾き」が連続していないことである。

現在の接続点 `t = tm` における左右の傾きは、

\[
w'_-(t_m)
=
(w_m-w_r)\frac{p_r}{t_m}
\]

\[
w'_+(t_m)
=
-(w_m-w_t)\frac{p_t}{1-t_m}
\]

となる。

一般にはこの2つは一致しない。

そのため、

```text
        /
       /
      /|
     / |
       |
       \
        \
```

のように、幅の値そのものはつながっているが、中間位置で輪郭の向きが急に変化することがある。

これは数学的には、

- C0連続: 値は連続
- C1連続: 傾きは不連続

という状態である。

髪の房では、この折れが視覚的に「切れ目」「つなぎ目」として現れやすい。

---

## 4. 基本方針

開始・中間・終端という考え方は維持する。

ただし、それぞれを別々の式で独立に作ってから接続するのではなく、全体を滑らかな補間曲線として構成する。

基本方針は次のとおりである。

1. ユーザーは代表点の位置と幅を指定する。
2. 各代表点を滑らかな曲線で補間する。
3. 接続点では値だけでなく傾きも連続させる。
4. 不要なオーバーシュートを抑える。
5. `sin^p` は標準形状の生成やプリセットに使用する。
6. 最終的な長手方向プロファイルは Hermite 系補間で生成する。

---

## 5. 開始・中間・終端の意味

3区間は「3本の別関数」として考えるのではなく、造形上の役割として扱う。

### 開始部分

役割:

- 頭皮や上流の髪束との接続
- 根元から房本体への移行
- 急激な幅変化を避ける

### 中間部分

役割:

- 房の主ボリューム
- 房らしい膨らみ
- ほぼ一定幅、または軽い膨らみ

### 終端部分

役割:

- 毛先への絞り込み
- 房の「尖り」を作る
- アニメ・フィギュア的な印象を決める

概念図:

```text
幅
↑

        ┌─────────┐
      ／             ＼
    ／                 ＼
___/                     \____

└────┬──────────┬────────┘ → t
   開始部        中間部        終端部
```

---

## 6. `sin^p` の位置付け

髪の房の基本形として、

\[
w(t)=W[\sin(\pi t)]^p
\]

は非常に扱いやすい。

特徴:

\[
w(0)=0
\]

\[
w(0.5)=W
\]

\[
w(1)=0
\]

となる。

`p` によって房の膨らみ方を変えられる。

おおよその傾向:

```text
p < 1   広く丸い
p = 1   標準的
p > 1   中央に膨らみが集中
```

ただし、この関数1本だけでは、

- 根元を0以外にしたい
- 最大幅の位置を中央以外にしたい
- 中間部を長く保ちたい
- 毛先だけ急に絞りたい

といった形状には対応しにくい。

したがって、`sin^p` は最終的な唯一のプロファイル式とするのではなく、

- 標準プリセット
- 初期形状生成
- 中間部の膨らみ生成

などに使用する。

---

## 7. 採用候補: Cubic Hermite 補間

幅の代表点を、

```text
t0        t1        t2        t3
●---------●---------●---------●
w0        w1        w2        w3
```

として持つ。

例えば、

```text
t0 = 0
t1 = StartPosition
t2 = TipStartPosition
t3 = 1
```

とする。

幅は、

```text
w0 = RootWidth
w1 = BodyStartWidth
w2 = BodyEndWidth
w3 = TipWidth
```

とする。

各区間を Cubic Hermite 曲線で補間する。

Hermite 基底関数:

\[
h_{00}(u)=2u^3-3u^2+1
\]

\[
h_{10}(u)=u^3-2u^2+u
\]

\[
h_{01}(u)=-2u^3+3u^2
\]

\[
h_{11}(u)=u^3-u^2
\]

補間式:

\[
w(u)
=
h_{00}(u)w_a
+
h_{10}(u)m_a
+
h_{01}(u)w_b
+
h_{11}(u)m_b
\]

これにより、接続点で、

- 幅
- 接線方向

を一致させることができる。

結果として C1 連続となり、現在問題となっている折れを大きく減らせる。

---

## 8. 傾きはユーザーに直接指定させない

Hermite 補間では接線 `m` が必要となるが、UIで直接「傾き」を指定させるのは避ける。

ユーザーは、

- 幅
- 位置

だけを指定する。

接線は周囲の点から自動計算する。

基本候補:

- Catmull-Rom 型
- Monotone Cubic Hermite 型

単純な Catmull-Rom では、代表点の値を超えて過剰に膨らむオーバーシュートが発生することがある。

髪の房では、

```text
0.10 → 0.40 → 0.30
```

という指定に対して、

```text
0.10 → 0.46 → 0.40 → 0.30
```

のような不要な膨らみが出ると扱いにくい。

そのため、第一候補は **Monotone Cubic Hermite interpolation** とする。

---

## 9. Monotone Cubic Hermite を優先する理由

髪の幅・厚み制御では次の性質が望ましい。

- 指定点を正確に通る
- 接続が滑らか
- 傾きが急変しない
- 不要なオーバーシュートが少ない
- 幅が負になる危険を抑えられる
- UIで直接傾きを指定する必要がない

したがって、

```text
Root
Start
End
Tip
```

の4点程度を使った Monotone Cubic Hermite 補間が髪の房には適していると考える。

---

## 10. パラメータ案

### 幅

初期案:

```csharp
float WidthRoot;
float WidthBodyStart;
float WidthBodyEnd;
float WidthTip;

float WidthStartT;
float WidthEndT;
```

あるいは、UIを簡潔にするなら、

```csharp
float WidthRoot;
float WidthBody;
float WidthTip;

float WidthStartT;
float WidthEndT;
```

として、中間部の両端を同じ `WidthBody` にしてもよい。

この場合、

```text
Root ─→ Body ─────────→ Body ─→ Tip
```

となり、中間部をほぼ一定幅として扱いやすい。

### 厚み

幅と同じ構造を採用する。

```csharp
float ThickRoot;
float ThickBody;
float ThickTip;

float ThickStartT;
float ThickEndT;
```

幅と厚みで区間位置を共通化する案もある。

その場合、

```csharp
float ProfileStartT;
float ProfileEndT;
```

を共通で持つ。

---

## 11. 推奨する最初の実装

最初から自由度を増やしすぎない。

まずは、

```csharp
WidthRoot
WidthBody
WidthTip

ProfileStartT
ProfileEndT
```

の5パラメータを基本とする。

プロファイル:

```text
0          StartT               EndT          1
●-------------●===================●-------------●
Root          Body                Body          Tip
```

この4点を滑らかに Monotone Cubic Hermite で補間する。

中間部の開始と終了の幅を同じにすることで、

- 開始部
- 中間部
- 終端部

という意味が非常に明確になる。

必要になった場合のみ、

```csharp
WidthBodyStart
WidthBodyEnd
```

に分離する。

---

## 12. `sin^p` を使ったプリセット

Hermite 補間を最終プロファイルにしても、`sin^p` はプリセット作成に使用できる。

例えば、

```text
Standard
Round
Sharp
Long Tip
Blunt Tip
```

などのプリセットを作る。

### Standard

標準的な房。

```text
RootWidth  = 小
BodyWidth  = 中
TipWidth   = 0
StartT     = 0.20
EndT       = 0.70
```

### Round

中央のボリュームを大きくする。

`sin^p` では `p < 1` 相当。

### Sharp

毛先区間を長くして尖らせる。

```text
EndT = 0.55 ～ 0.65
TipWidth = 0
```

### Blunt

ぱっつん形状。

```text
TipWidth > 0
```

---

## 13. 幅と断面形状は別概念として扱う

現在 `SectionPower` にも `Power` が使用されている。

断面では、

```csharp
Mathf.Pow(Mathf.Abs(Mathf.Sin(th)), q)
```

のような形で使用されている。

これは長手方向の幅プロファイルとは別の意味である。

混乱を避けるため、

```text
長手方向
    WidthProfile
    ThickProfile

断面方向
    SectionShape
```

として概念を明確に分離する。

可能であれば、UI表示も、

```text
Width Power
Section Power
```

のような曖昧な名称ではなく、

```text
Width Shape
Section Shape
```

などに変更する。

---

## 14. 実装ステップ

### Step 1: 現在の `Profile()` を分離

現在の `Profile()` を残したまま、新しいプロファイル関数を追加する。

例:

```csharp
private static float SmoothHairProfile(
    float t,
    float root,
    float body,
    float tip,
    float startT,
    float endT)
```

最初は幅のみ新方式にする。

---

### Step 2: Hermite 補間関数を実装

独立した補間関数を用意する。

例:

```csharp
private static float Hermite(
    float y0,
    float y1,
    float m0,
    float m1,
    float u,
    float dt)
```

その後、4点全体の接線を求める関数を作る。

---

### Step 3: Monotone 制約を導入

単純な中央差分による接線を実装した後、

- 符号反転
- オーバーシュート
- 負値

を抑える Monotone Hermite の接線計算に置き換える。

候補アルゴリズム:

- Fritsch-Carlson
- Fritsch-Butland

どちらかの方式を採用する。

---

### Step 4: 幅だけで動作確認

まず `widthAll[i]` の生成だけを新方式に変更する。

現在:

```csharp
widthAll[i] = Profile(...);
```

新方式:

```csharp
widthAll[i] = SmoothHairProfile(...);
```

厚みは現在のまま残す。

これにより問題の切り分けを容易にする。

---

### Step 5: 厚みにも適用

幅で問題がなければ、

```csharp
thickAll[i]
```

にも同じ方式を適用する。

幅と厚みの位置パラメータを共通化するかどうかをこの段階で判断する。

---

### Step 6: UI整理

現在の、

```text
WidthRoot
WidthMid
WidthTip
WidthMidT
WidthPowRoot
WidthPowTip
```

を整理する。

第一案:

```text
Root Width
Body Width
Tip Width
Start Position
Tip Start Position
```

必要であれば Advanced 項目として、

```text
Body Start Width
Body End Width
```

を追加する。

---

### Step 7: プリセット追加

以下を候補とする。

```text
Standard
Round
Sharp
Long Tip
Blunt
```

プリセットはパラメータ値を書き換えるだけにし、生成ロジックそのものは共通化する。

---

## 15. 検証項目

### 形状の連続性

代表点付近で輪郭に折れが見えないこと。

特に、

```text
t = StartT
t = EndT
```

を重点的に確認する。

### パラメータ調整時

スライダーを連続的に動かしたとき、突然形状が折れたり跳ねたりしないこと。

### オーバーシュート

指定した最大幅より不自然に太くならないこと。

### 負値

幅・厚みが負にならないこと。

### TipWidth = 0

毛先が自然に一点へ収束すること。

### TipWidth > 0

ぱっつん形状が自然に生成できること。

### RootWidth > 0

頭皮側との接続が自然になること。

---

## 16. 可視化デバッグ

プロファイルの調整では、3Dメッシュだけを見ていると原因が分かりにくい。

可能であればデバッグ表示として、

```text
横軸: t
縦軸: Width
```

の2Dグラフを表示する。

最低限、

```text
t
widthAll[i]
thickAll[i]
```

を CSV またはログへ出力できるようにする。

これにより、

- 折れ
- オーバーシュート
- 不自然な凹凸

を数学的に確認できる。

---

## 17. 互換性

既存パラメータをすぐ削除すると、既存保存データとの互換性に問題が生じる可能性がある。

移行方法は次のいずれかとする。

### 方法A

旧パラメータを残し、新方式へ内部変換する。

### 方法B

`ProfileMode` を追加する。

```csharp
enum HairProfileMode
{
    LegacyPower,
    SmoothHermite
}
```

既存データは `LegacyPower`、新規作成は `SmoothHermite` とする。

安定後に Legacy を廃止する。

現時点では **方法Bが安全** である。

---

## 18. 最終的な設計イメージ

髪の房の長手方向は、

```text
中心線
    C(t)

幅
    W(t)

厚み
    H(t)

断面
    Section(theta)

捻り
    Twist(t)
```

を独立した概念として扱う。

このうち今回改善するのは、

```text
W(t)
H(t)
```

である。

中心線や断面生成、球・円筒への配置、Lobe 分割などは基本的に現在の仕組みを維持する。

---

## 19. 今回の重要な判断

今回の改善では、

**「開始・中間・終端のパラメータを減らすこと」そのものを目的としない。**

重要なのは、

**「調整しても接続点に不自然な折れが出ないこと」**

である。

したがって、

- 開始
- 中間
- 終端

という造形上の考え方は残す。

一方で、それぞれを独立した式として接続する方式は避ける。

代表点を与え、全体を滑らかな補間曲線として生成する方式へ変更する。

第一候補は、

**Monotone Cubic Hermite interpolation**

とする。

---

## 20. 次の作業

次の実装作業は以下の順で進める。

1. `HairStrandMeshGenerator.cs` に新しい `SmoothHairProfile()` を追加する。
2. 4点補間用の Monotone Cubic Hermite を実装する。
3. 幅 `widthAll` のみ新方式へ切り替える。
4. UIパラメータを仮追加する。
5. 既存方式と並べて比較する。
6. つなぎ目・オーバーシュート・毛先形状を確認する。
7. 問題なければ厚みにも適用する。
8. 最後にパラメータ名とUIを整理する。

