# PolyLing マルチディスプレー対応 計画書（検討段階）

| 項目 | 内容 |
|---|---|
| 状態 | 検討段階（未実装） |
| 基準コード | GitHub `dhag/com.haglib.PolyLing` HEAD `268c505`（2026-09-11 11:57 +0900） |
| 備考 | ローカル作業ツリーとの差分は未確認。行番号は上記コミット時点のもの |

---

## 1. 目的と画面割り当て

- 第1ディスプレイ：中央ビューポート群（4画面）
- 第2ディスプレイ：左ペイン｜オブジェクトリスト（常駐列）｜右ペイン
- 動機：オブジェクトリストが常時見えないのはつらいという意見が多い

---

## 2. 現状の構造（コードで確定した事実）

### 2.1 画面出力はすべて UI Toolkit パネル

- ビューポートのカメラは RenderTexture 出力に固定（`PlayerViewport.cs:50-51, :169`）。ディスプレイへ直接描くカメラは無い。
- リポジトリ内に Display API・`targetDisplay` の使用箇所は無い。
- したがってマルチディスプレー対応は「UI を複数ルート（複数パネル）に分けて載せる」問題であり、カメラ側の変更は不要。

### 2.2 既にディスプレイに依存していない部分

- ビューポート入力は UI Toolkit のポインタイベント（パネルローカル座標）（`PlayerViewportPanel.cs:566-572`）。
- 投影は `cam.pixelWidth` / `cam.pixelHeight` 基準で `Screen` を使わない（`PlayerViewportManager.GpuSelect.cs:254, :262-274`）。
- RT 解像度は要素の `resolvedStyle` から決定（`PlayerViewportPanel.cs:743-748`）。
- 描画提出はカメラ単位（`PolyLingPlayerViewer.cs:136-142`）。
- `Input.mousePosition` を読む `PlayerMouseDispatcher` はどこからも生成されていない。

### 2.3 単一ルート前提の箇所

| 箇所 | 内容 | 位置 |
|---|---|---|
| UIDocument | 1個の rootVisualElement を Core に渡すだけ | `PolyLingPlayerViewer.cs:28, :45-46, :60-61` |
| レイアウト構築 | 左｜中央4画面｜右 を1つの root に一括構築 | `PolyLingPlayerViewerCore.Layout.cs:71` → `PlayerLayoutRoot.Build` |
| 中央と外側の接続 | 木構造上の接続は `_splitCR.Add(_splitCenter)` の1か所 | `PlayerLayoutRoot.cs:115` |
| 十字ドラッグ領域 | root に追加し、`_perspPane.worldBound` を root 座標へ変換して配置（同一パネル内が条件） | `PlayerLayoutRoot.cs:255-263, :301-311` |
| レイアウト保存 | PlayerPrefs キー4つ。DetachFromPanel で保存 | `PlayerLayoutRoot.cs:74-77, :173, :199` |
| キャレット USS / ボタンフィードバック / ボタン配色 | root 1つにだけ適用 | `PolyLingPlayerViewerCore.Layout.cs:49-64, :68, :93` |
| ショートカット | KeyDownEvent(TrickleDown) を root 1つに登録。入力中判定も登録 root のパネルで行う | `PlayerShortcutController.cs:54-58, :146-148`、`PolyLingPlayerViewerCore.Shortcuts.cs:148` |
| 画面キャプチャ | `ScreenCapture.CaptureScreenshotAsTexture` と `Screen.height` で切り出し | `PlayerScreenCapture.cs:73, :169` |

### 2.4 ホスト

- Core は Editor（`PolyLingEditorWindow.cs:50-54`、EditorWindow.rootVisualElement）と Player（UIDocument）の2ホストから、同じ `Initialize(VisualElement, …)` で使われている（`PolyLingPlayerViewerCore.Lifecycle.cs:38-41`、BuildLayout 呼出 `:345`）。

### 2.5 オブジェクトリストの現状

- `MeshListSubPanel` は右ペインのセクションの1つ（`PlayerLayoutRoot.RightPane.cs:297`、生成 `PolyLingPlayerViewerCore.Layout.Panels.cs:39-41`）。
- 右ペインの切替で隠れる（`PolyLingPlayerViewerCore.ViewAids.cs:310`）。
  - 表示する経路は `ShowMeshListPanel` の1か所（`ViewAids.cs:273`）。
  - 起動時は `ShowCategory1Panel` → `ShowRightPanel` で隠れる（`PolyLingPlayerViewerCore.Tools.cs:69, :210`）。
  - よって単一画面では「リストが見えている＝右ペインの担当」。
- 更新は `PanelContext.OnViewChanged` の購読で、表示状態を見ない（`MeshListSubPanel.cs:290-299`、`MeshListSubPanel.Refresh.cs:31-`）。常駐させても更新の配線追加は不要。
- 「ビューポート操作（操作なし／要素選択／姿勢調整）」の InteractionMode への反映は、リストが担当（`_activeRightSection == MeshListSection`）のときだけ（`Layout.Panels.cs:46-51`、`ViewAids.cs:266-296`）。担当の設定は `ShowRightPanel` の1か所（`PolyLingPlayerViewerCore.ButtonHighlight.cs:154-162`、代入 `:158`）。
- リストとボーン編集パネルは同じ ObjectMove 設定を参照している（`Layout.Panels.cs:44, :246`）。
  - リストのタブ切替で `ApplyPickFilter` が無条件に呼ばれる（`MeshListSubPanel.TreeView.cs:138`）。
  - 姿勢調整の4トグル（`_toggleOriginOnly` / `_toggleMoveWithChildren` / `_toggleShowMoveGizmo` / `_toggleShowRotationGizmo`）を両パネルが書く（`MeshListSubPanel.BuildUI.cs:334-363`、`PlayerBoneEditorSubPanel.cs:203-234`）。リスト側の表示同期は担当になったときだけ（`ViewAids.cs:274`）。
  - ボーン編集パネル側の規約コメント：ピック対象は、今ビューポートを担当しているパネルが入場時に決める（`PlayerBoneEditorSubPanel.Actions.cs:180-184`）。

---

## 3. Unity 側の制約（確認済み）

| 項目 | 内容 | 出典 |
|---|---|---|
| ディスプレイの有効化 | 追加ディスプレイは `Display.Activate()` で明示的に有効化が必要。一度有効化すると無効化できない | [R1] |
| Editor での `Display.displays` | 非対応。接続台数に関係なく Length は常に 1 | [R2] |
| Editor でのプレビュー | Game ビュー左上の Display メニューで表示先を選ぶ | [R1] |
| `Display.SetParams` | Windows 専用。描画サイズとウィンドウ位置を設定 | [R3] |
| `PanelSettings.targetDisplay` | パネルの表示先ディスプレイ。レンダーテクスチャ未指定時のみ有効 | [R4] |
| PanelSettings のクリア設定 | `clearColor` / `colorClearValue` がある | [R5] |
| 複数パネルのイベント | ポインタイベントは Sort Order に従って各パネルへ送られる。イベントでフォーカスを変えた最初のパネルがフォーカスパネルになる（2022.3 マニュアル） | [R6] |
| ScreenCapture | `CaptureScreenshotAsTexture` にディスプレイ指定の引数は無い。複数ディスプレイ時、Editor では複数 Game ビューが混ざる、Windows ビルドではメインの解像度に切り詰められる、という報告がある | [R7] [R8] |
| Editor のスケール不具合 | 解像度の異なる Game ビュー2枚で UI スケールがずれる不具合は、報告後に修正済みとされる | [R9] |

---

## 4. 設計方針

- Core は「paneRoot が渡されたか」で分割するかどうかを決める。paneRoot が null のときは現行動作をそのまま通す。
- ルートの用意はホストの責務とする（Player：追加 UIDocument、Editor：追加 EditorWindow）。
- Runtime に `#if UNITY_EDITOR` を入れない。Editor／ビルドの分岐が要る箇所は `Application.isEditor` を使う。
- 中央4画面のサブツリー（`_splitCenter` 以下）は作り直さず、親だけを替える。

---

## 5. 変更計画

### 5.1 構成構造体（新規）

- `RemoteConfig`（`PolyLingPlayerViewerCore.cs:71-`）と同じ形の構成構造体を追加する（名称は実装時に決定）。
- 項目：
  - マルチディスプレー ON/OFF（Player ホストが第2 UIDocument を作るかの判定に使う）
  - 常駐リストの担当条件：(a) オブジェクトリストボタン ／ (b) 操作モード押下。既定は (a)（現行の意味を維持する側）
- PolyLingPlayerViewer の SerializeField から渡す（既存の受け渡し `PolyLingPlayerViewer.cs:19-26, :63-71` と同じ形）。
- UI には出さない。起動時に1回読むので、切替は Inspector で変更して Play し直す。
- Editor ホストは Default を渡す。

### 5.2 モード決定（起動時のみ）

- `Display.Activate()` は取り消せないため、実行中の切替は持たない。
- Editor：設定だけで判定する（`Display.displays.Length` が常に 1 のため Length では判定しない）。
- ビルド：設定 ON かつ `Display.displays.Length >= 2` のときだけ `Display.displays[1].Activate()` を呼ぶ。それ以外は現行の単一ルートで起動する。

### 5.3 PolyLingPlayerViewer（Player ホスト）

- 子 GameObject を非アクティブで生成 → UIDocument を追加 → 既存の PanelSettings を Instantiate し、targetDisplay を Display 2 に変えたものを割り当て → 有効化。
- その rootVisualElement を paneRoot として Core に渡す。既存の UIDocument は viewportRoot（第1ディスプレイ）になる。
- 破棄時に、子 GameObject と複製した PanelSettings を破棄する。
- PanelSettings アセットの追加、`CreatePlayerViewer.cs` の変更は不要。

### 5.4 Core（Initialize / BuildLayout）

- `Initialize`（`Lifecycle.cs:41`）に paneRoot と構成構造体を追加し、BuildLayout（呼出 `:345`）へ渡す。
- キャレット USS、ボタンフィードバック、`PostBuildButtonColors` を両ルートに適用する（`Layout.cs:49-64, :68, :93`）。

### 5.5 PlayerLayoutRoot.Build

- `Build(viewportRoot, paneRoot)` にする。paneRoot が null のときは現行コードをそのまま通す。
- 分割時：
  - `_splitCenter` を viewportRoot 直下に置く。`ViewportArea` は従来どおり `_splitCenter`。
  - paneRoot に TwoPaneSplitView で「左ペイン｜常駐列｜右ペイン」を組む。
  - `AddSection`（`RightPane.cs:471-485`）に親を指定する引数を追加し、`MeshListSection` を常駐列に作る。他のセクションは右ペインのまま。
  - `_rootRef` と十字ドラッグ領域は viewportRoot 側に置く。
  - 分割モードの 3×3 ボタン（`LeftPane.cs:294`）は左ペインのまま。第2ディスプレイから第1ディスプレイの分割を操作する形になる。

### 5.6 レイアウト保存

- 分割時は別キーで保存する：左幅、右幅、常駐列幅、中央の縦横位置。
- 単一時のキー（`PlayerLayoutRoot.cs:74-77`）と保存値は変えない。
- DetachFromPanel での保存（`PlayerLayoutRoot.cs:199`）は両ルートに登録する。

### 5.7 右ペイン切替と常駐リストの担当

**共通（分割時）**

- `HideAllRightPanels` で `MeshListSection` を隠さない（`ViewAids.cs:310`）。
- リストを担当にする処理は既存の `ShowMeshListPanel`（`ViewAids.cs:266-275`）をそのまま使う。分割時は結果として右ペインが空になる。
- 担当判定は既存の `_activeRightSection == MeshListSection` をそのまま使う。
- 単一時は「リストが見えている＝担当」（2.5）なので、以下の変更で単一時の挙動は変わらない。

**(a) オブジェクトリストボタンで担当**

- `OnViewportOpModeChanged` の条件（`Layout.Panels.cs:48-50`）は現行のまま。
- 担当外で押した操作モードは保存されるだけ（`MeshListSubPanel.BuildUI.cs:275-280`）。次に「オブジェクトリスト」ボタンで担当になったとき反映される（`ViewAids.cs:270-272`）。

**(b) 操作モード押下で担当**

- 同じハンドラで、担当外なら `ShowMeshListPanel()` を呼ぶ。
- `SetViewportOpMode` は通知の前に値を更新している（`MeshListSubPanel.BuildUI.cs:277-280`）。そのため `ShowMeshListPanel` が読む `CurrentViewportOpMode` は押したモードになる。
- 担当中は現行どおり `ApplyMeshListViewportOpMode(mode)`。
- (a) との差分はこのハンドラ1か所のみ。

### 5.8 共有 ObjectMove 設定の保護（(a)(b) 共通）

常駐化によりリストが担当外のまま操作できるようになるため、以下を入れる。担当判定は Core から関数で `MeshListSubPanel` へ渡す。

- タブ切替時の `ApplyPickFilter`（`MeshListSubPanel.TreeView.cs:138`）は、担当外なら呼ばない。
- 姿勢調整の4トグルは、担当外の間は無効にする。
  - 担当の変化は `ShowRightPanel`（`ButtonHighlight.cs:158`）の1か所で起きるので、そこで有効／無効を切り替える。
  - 担当になったときは、既存の `SyncObjectPoseToggles`（`MeshListSubPanel.BuildUI.cs:473`、呼出 `ViewAids.cs:274`）で値を同期する。

### 5.9 ショートカット

- `PlayerShortcutController` を複数ルートに対応させる（現行は登録フラグと root が1つずつ：`:54-58`）。
- 入力中判定は、キーイベントが発生したパネルの focusController で行う（現行は登録 root 固定：`:146-148`）。
- Core は両ルートに Attach する（`Shortcuts.cs:148`）。

### 5.10 Editor ホスト

- `PolyLingEditorWindow`（`:50-54`）とは別に左右ペイン用の EditorWindow（Editor アセンブリ）を開き、その rootVisualElement を paneRoot として渡す。Core は Player と同じ経路を通る。
- Editor ホストで2ウィンドウ構成を有効にする方法（メニュー等）は未決定。

---

## 6. 変更しないもの

- カメラ、RenderTexture、描画提出、GPU ホバー経路
- `CreatePlayerViewer.cs`（PanelSettings は実行時に複製する）
- `ListClientBase` 系の別プロセスクライアント（描画メッシュを取得しない設計のため使わない：`ListClientBase.cs:7`）
- 画面キャプチャ（MainView / TriView の切り出し対象は第1ディスプレイに残る。変更の要否はビルドでの確認結果で決める）

---

## 7. 確認手順

### 7.1 Editor（MCP）

- 準備：Game ビューを2枚（Display 1 / Display 2）にする。2枚目を開く操作だけ手作業が1回必要。
- Unity ウインドウをアクティブ化してから MCP で Play する。
- 確認項目：
  - 第1に4画面、第2に 左｜リスト｜右 が表示される
  - 両ディスプレイ側でショートカットが効く。テキスト入力中は効かない
  - 十字ドラッグ、分割モード 3×3、レイアウトの保存と復元
  - (a) / (b) それぞれで、担当の切替と右ペインの空表示
  - ボーン編集パネル使用中に、リストのタブ切替・4トグルでピック対象と ObjectMove 設定が変わらない
  - 第2ディスプレイの未描画領域の見え方（必要なら PanelSettings の `clearColor` を設定）
- 注意：Editor で設定 ON のまま Display 2 の Game ビューが無いと、左右ペインが見えない。

### 7.2 単一時の回帰

- 設定 OFF で、表示・操作・レイアウト保存値が現行と同じであること。

### 7.3 ビルド（実モニタ2台）

- 第2ディスプレイ上のパネルにポインタ／キー入力が届くか
- 画面キャプチャ（MainView / TriView / Window）の結果
- 解像度・DPI の異なるモニタでの表示

---

## 8. 未確認・未決定事項

| 種別 | 内容 |
|---|---|
| 未確認 | Unity 6 の Windows ビルドで、第2ディスプレイ上のパネルに入力が正しく届くか（実装前に最小構成のビルドで確認する） |
| 未確認 | 複数ディスプレイ時の ScreenCapture の挙動 |
| 未確認 | `PanelSettings.targetDisplay` で Display 2 を指す値のインデックス基準（実装時に確認） |
| 未決定 | (a) / (b) の最終選択（実使用で判断） |
| 未決定 | Editor ホストで2ウィンドウ構成を有効にする方法 |

---

## 9. 変更対象ファイル

パスは `Runtime/Poly_Ling_Player/View/` 起点（Editor 分を除く）。

| ファイル | 変更内容 |
|---|---|
| `Core/PolyLingPlayerViewer.cs` | SerializeField 追加、第2 UIDocument 生成、Activate、破棄 |
| `Core/PolyLingPlayerViewerCore.cs` | 構成構造体の追加 |
| `Core/PolyLingPlayerViewerCore.Lifecycle.cs` | Initialize に paneRoot・構成を追加 |
| `Core/PolyLingPlayerViewerCore.Layout.cs` | 両ルートへの USS・フィードバック・配色の適用 |
| `Core/PolyLingPlayerViewerCore.Layout.Panels.cs` | `OnViewportOpModeChanged` の (a)/(b) 分岐、担当判定の受け渡し |
| `Core/PolyLingPlayerViewerCore.ViewAids.cs` | `HideAllRightPanels` の分割時除外 |
| `Core/PolyLingPlayerViewerCore.ButtonHighlight.cs` | 担当変化時の4トグル有効／無効 |
| `Core/PolyLingPlayerViewerCore.Shortcuts.cs` | 両ルートへの Attach |
| `Core/Shortcut/PlayerShortcutController.cs` | 複数ルート対応、入力中判定 |
| `Core/PlayerLayoutRoot.cs` | `Build(viewportRoot, paneRoot)`、十字ドラッグ、保存キー |
| `Core/PlayerLayoutRoot.RightPane.cs` | `AddSection` の親指定、常駐列 |
| `SubPanels/Model/MeshListSubPanel.*.cs` | 担当判定の受け取り、タブ切替時の `ApplyPickFilter` 条件、4トグル有効／無効 |
| `Editor/Poly_Ling_Player/PolyLingEditorWindow.cs` ほか新規 EditorWindow | 左右ペイン用ウィンドウ（5.10） |

---

## 参考資料

- [R1] Unity Manual: Display camera views on multiple monitors — https://docs.unity3d.com/Manual/MultiDisplay.html
- [R2] Unity Scripting API: Display.displays — https://docs.unity3d.com/ScriptReference/Display-displays.html
- [R3] Unity Scripting API: Display.SetParams — https://docs.unity3d.com/ScriptReference/Display.SetParams.html
- [R4] Unity Scripting API: PanelSettings.targetDisplay — https://docs.unity3d.com/ScriptReference/UIElements.PanelSettings-targetDisplay.html
- [R5] Unity Scripting API: PanelSettings — https://docs.unity3d.com/ScriptReference/UIElements.PanelSettings.html
- [R6] Unity Manual 2022.3: Runtime UI event system — https://docs.unity.cn/cn/current/Manual/UIE-Runtime-Event-System.html
- [R7] Unity Scripting API: ScreenCapture.CaptureScreenshotAsTexture（2022.3）— https://docs.unity.cn/kr/current/ScriptReference/ScreenCapture.CaptureScreenshotAsTexture.html
- [R8] Unity Discussions: How can I take a screenshot of a given display? — https://discussions.unity.com/t/how-can-i-take-a-screenshot-of-a-given-display-not-main-display/285588
- [R9] Unity Discussions: UI Toolkit on multiple screens — https://discussions.unity.com/t/ui-toolkit-on-multiple-screens/936046
