/* PolyLingCaret.uss
 * 全 TextField のキャレット(カーソル)色を白にする。
 * root(UIDocument.rootVisualElement) に一度だけ付与し、全子孫の入力要素へ一元適用する。
 * キャレット色は --unity-cursor-color でのみ設定可能（C# style.* 不可）。 */
.unity-base-text-field__input {
    --unity-cursor-color: rgb(255, 255, 255);
}
