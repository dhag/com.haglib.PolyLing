# MCP で PolyLing を操作するときの手順書

外部（MCP）から PolyLing を動かす担当が、**作業を始める前に読む**もの。
どのシナリオにも通用する内容だけを置く。個別シナリオの事情は補足手順書へ。

| 補足手順書 | 対象 |
|---|---|
| `ロボ組み立て補足.md` | 「ロボットを組み立てる」シナリオ |

ここに書いてあるのは知識ではなく手順。過去に実際にやらかした失敗を、
同じ形で繰り返さないためにある。
「知っていればできる」ではなく「手順として踏む」こと。

---

## 1. 使える道具

### 1-1. Unity Editor の制御

| ツール | いつ使うか |
|---|---|
| `unity_ping` | 何かおかしいとき最初に打つ。応答が返るか |
| `unity_state` | 再生中か、コンパイル中か、取り込み中かを確かめる |
| `unity_play` / `unity_stop` | 再生の開始・終了。**応答は遷移前に返る**ので、必ず `unity_state` で確認する |
| `unity_refresh` | **スクリプトを直したら必ず呼ぶ。** Editor に取り込ませてコンパイルさせる。再生中は失敗するので先に `unity_stop` |
| `unity_recompile` | 全スクリプトを強制的に再コンパイル。`unity_refresh` より遅い。普段は要らない |
| `unity_focus` | Unity のウインドウを前面にする。**MCP サーバ側で動く**ので、Editor がコンパイル中でも効く |
| `unity_capture` | **画面を撮って見る。** 前面化・撮影・待ち・読み込みまで 1 回で済む（2-3） |

### 1-2. ファイル

| ツール | いつ使うか |
|---|---|
| `list_files` | 知らないディレクトリを見るとき |
| `search_text` | 宣言や使用箇所を探すとき。`read_file` で全部読むより先にこちら |
| `read_file` | テキストを読む。MQO は `encoding=shift_jis` |
| `read_binary` | バイナリを読む。**画像には使わない**（2-3） |
| `read_image` | **撮ってある画像を見直す。** PNG / JPEG / GIF / WebP。プロジェクトの中ならどこでも読める。撮るのは `unity_capture`（2-3） |
| `image_selftest` | 画像が届くかどうかだけを試す。撮れていないのか送れていないのかの切り分け（2-3） |
| `apply_patch` | 既存ファイルの一部を直す。`old_text` は 1 か所だけ一致すること |
| `write_file` | 新規作成、または全面書き換え |
| `write_binary` | バイナリを書く。大きいものは分割して `append=true` |

### 1-3. コンパイル

| ツール | いつ使うか |
|---|---|
| `compile` | `dotnet build` による外部チェック。**Editor にコンパイルさせるものではない** |
| `get_compile_errors` | 直前の `compile` のエラー |
| `read_unity_log` | **Unity 側のコンパイルエラーや例外はここにしかない。** Console に出ているものが全部読める |

`compile` が通っても Unity 側で通るとは限らない。逆も同じ。両方見ること。

### 1-4. PolyLing のコマンド

| ツール | いつ使うか |
|---|---|
| `polyling_tools` | コマンド一覧（約 262 個）。**作業の前に取り直す。** 増えていることがある |
| `polyling_call` | コマンドを 1 つ実行 |

`polyling_call` の引数は **文字列だけの平らな組**。

- 配列 → カンマ区切りの 1 つの文字列（`"0,1,2"`）
- 入れ子 → ドット付きのキー（`"params.widthTop"`）

JSON の配列をそのまま渡すと断られる。

---

## 2. 守る手順

### 2-1. スクリプトを直したとき

```
unity_stop → 編集 → compile → unity_refresh → unity_play → unity_state
```

**`unity_refresh` を飛ばすと、Unity は古いコードのまま動く。**
`compile` が通ったからといって Editor に反映されてはいない。
実際に「再生しても直したはずの挙動にならない」で何度も時間を失っている。

### 2-2. 実行のあとに `unity_focus`

**心がけでは守れない。手順の 1 行にする。**

作業の初めに 1 回呼ぶだけでは足りない。
途中で前面から外れると、**画面が更新されないまま作業が進む。**
利用者からは何も起きていないように見える。

「そのつど呼ぶ」という心がけで済ませようとして、実際に 1 回も呼ばずに
6 部位を作り終えたことがある。だから **実行コマンドの直後に置く。**
セットで 1 操作と考える。

### 2-3. 結果は必ず確かめる

- コマンドの成功応答は「受け付けた」であって「意図どおりになった」ではない
- `uiCaptureStatus` の `completed` は「撮り終えた」であって「写っている」ではない

**画像は `unity_capture` で自分で見る。** 呼び出し 1 回で撮って見られる。

```
unity_capture target=MainView
```

前面化・撮影・完了待ち・ファイル読み込みまで、すべてこの中で終わる。
`unity_focus` も `uiCapture` も `uiCaptureStatus` も `read_image` も要らない。

| 引数 | 既定 | 意味 |
|---|---|---|
| `target` | `MainView` | `MainView` / `TriView` / `Window`。パネルまで写るのは `Window` だけ |
| `folder` | `.` | 作業フォルダからの相対。**空にしない**（キャプチャパネルの設定先はプロジェクトの外かもしれず、その場合は読めない） |
| `base_name` | `PolyLingCapture` | 連番と `.png` が付く |
| `focus` | `true` | 落とさない。背面では撮影が終わらない（下記） |
| `timeout_ms` | `15000` | 前面化から読み込みまでの全体 |

PNG はそのまま残り、応答にプロジェクト相対パスが出る。
**あとで見直すときは `read_image` にそのパスを渡す。** 撮り直す必要はない。

撮影はフレーム終端で 1 回。**Unity が背面で Run In Background がオフだとフレームが進まず、
いつまでも終わらない。** `focus=true` はそのための引数であって、心がけではない。
期限切れの文言はこの事情を名指しする。

制限は 2 つ。

| 制限 | 値 | 超えるとどうなるか |
|---|---|---|
| 1 応答の画像バイト数 | 600,000 | **読まずに断られる。** ファイル自体は残る |
| 長辺 | 1,568px | 送れるが縮小される。注意書きが付く |

実測: `target=MainView` は 717×507 / 42KB、`target=Window` は 1960×830 / 150KB。
どちらも状態確認 1 回で完了し、142〜823ms（前面化が要るときが長い側）。
`Window` は長辺の注意書きが出るが、縮小後もパネルの文字は読めた。
撮影時の縮小は無い（今後の課題。7 を見ること）。

`unity_capture` が妙なときは `image_selftest` を打つ。
赤緑青白の 4 分割が見えれば伝送路は正常で、原因は撮影側にある。

**`read_binary` を画像に使ってはいけない。**
base64 の文字列が返るだけで、見えないまま文脈を食い潰す。
過去に「`read_binary` → container へ書き写し → Python で展開」を試して失敗している。

- 717×507 の PNG（38KB / base64 52,000 文字）
- 受け取りは正確（`remaining=0`、末尾に `IEND` あり）。展開処理も正しく、IHDR から 717×507 / 8bit / RGBA を読めた
- **中ほど（16441 バイト目）でチャンクが壊れていた。**
  後ろが切れたのではなく内容がずれた。デコード後が 12 バイト多かった
- 分割して読み直しても、結合するのは結局自分なので同じこと

判断の誤りではなく、**単純な書き写しができなかった。**
`unity_capture` と `read_image` はこの書き写しが要らないので通る。

### 2-4. 確認手段が使えないときは飛ばす

キャプチャや Unity 本体が不調なときは、**その確認を飛ばして作業を進める。**

- キャプチャが返らない / 壊れる
- Unity が応答しない、再生に入らない
- カメラが整えられず、撮っても見切れる

こういうときは、`query*` で数値を確かめられる範囲だけ確かめ、
**確認できなかったことを作業ログに書いて先へ進む。**

確認手段そのものを直そうとして本題を止めない。
実際に、キャプチャの解析に固執して作業が何度も止まった。

数値で確かめられることは多い。

| 確かめたいこと | 手段 |
|---|---|
| オブジェクトの数と名前 | `queryModelStructure` |
| 頂点数・面数・境界ループ | `queryDrawableStats` |
| 穴の数と点数 | `queryHoles` |
| 向き・寸法・位置 | `getRawData` の座標 |
| ボーン階層とウェイトの分布 | `queryBoneSkin` |
| 1 メッシュのウェイトの行き先 | `querySkinWeightSummary` |
| UI の値が入ったか | `uiGetValue` で読み返す |

見た目は `unity_capture` で自分で確かめる。それも使えないときだけ利用者に任せる。

### 2-5. 確認用のキャプチャはカメラを整えてから撮る

既定のカメラのままだと、作ったものが画面からはみ出して見切れる。
実際に、組み上げたあとのキャプチャで全体が入っておらず、
利用者から「カメラの姿勢が悪く見切れている」と指摘された。

撮る前に `camera` パネルで注視点と距離を合わせる。

- `camera.main.targetX/Y/Z` — 注視点を対象の中心へ
- `camera.main.distance` — 全体が入る距離へ
- 対象の座標は `getRawData` で分かるので、中心と大きさは計算できる

ジオメトリの数値が合っていても、**写っていなければ確認にならない。**
撮ったら `unity_capture` の結果を見て、見切れていないことを確かめてから次へ進む。

### 2-6. 消す操作は必ず断ってから

`resetProject` や `deleteMeshes` は、利用者の作業結果を消す。

**実行前に必ず確認を取る。** 自分の都合（動作確認をしたい等）で黙って消さない。
過去に、色の確認のために `resetProject` を実行して、組み立て途中のモデルを丸ごと消している。

### 2-7. 指示された手順を、自分の都合で変えない

呼び出し回数が足りない、時間がかかる——それは**こちらの都合**であって、
手順を省いてよい理由にはならない。

回数がかかるなら、かかったぶんだけ応答を分ける。
「本当の目的はこうだから省いてよい」と**目的を勝手に言い換えない。**

---

## 3. 引っかかりやすい点

| 事象 | 実際 |
|---|---|
| 再生し直すとモデルが無い | 再生のたびにプロジェクトは空になる。`resetProject` から始める |
| 背面だと画面が更新されない | Player Settings の Run In Background がオフだと、再生中でも背面ではフレームが進まない。オンでも遅れることはある |
| `control is not displayed` | 折りたたみやタブの中にある。`uiReveal` してから押す |
| `control is not built` | そのパネル・その図形のときだけ作られる部品。`uiReveal` か `uiShowPanel` を先に |
| `no project` | プロジェクトが無い。`resetProject` |
| スライダーに入らない値がある | 下限・上限で丸められる。**数値欄は入力値のまま表示されるのに実際は丸められている**ので、`uiGetValue` で読み返して確かめる |
| 前の操作の設定が残る | UI の値は作ったあとも残る。**使わない設定は毎回明示的に戻す**。「前がどうだったか」に依存しない |

### 索引はコマンドで動く

スキンド変換のようにオブジェクトを増やすコマンドは、**既存の masterIndex をずらす。**
実測では、変換でボーンが先頭に挿し込まれ、描画オブジェクトが 0〜8 から 9〜17 へ移った。

索引を控えて使い回さない。**節目ごとに `queryModelStructure` や
`getRawData`（scope=Model）で取り直す。**

### 引数名は毎回確かめる

同じような意味でも名前が違う。推測で書くとエラーになる。

| コマンド | 正しい引数名 | 間違えやすい名前 |
|---|---|---|
| `selectMesh` | `indices` | `masterIndices` |
| `deleteFaces` | `faceIndices` | `faces` |
| `deleteMeshes` | `masterIndices` | — |

### `selectElements` の `masterIndices` は「選択を消す対象」

塗る・消す対象のメッシュだけを渡すと、**別メッシュの選択が残る。**

実際に、あるメッシュを塗り終えて次のメッシュの 1 段目を選んだとき、
6 頂点のはずが 12 頂点になった（前のメッシュの選択が残っていた）。

**関係するメッシュをすべて `masterIndices` に渡す。**
同じメッシュ内で進めるぶんには置き換わるので気づきにくい。

### Editor を固めた例

`InternalEditorUtility.RepaintAllViews()` を、コマンドを受け付けている最中から呼ぶと、
描き直しの中でまた受信処理へ入って絡み、**Editor が応答しなくなる。** 呼んではいけない。

影響範囲を確かめずに強い API を足さないこと。

---

## 4. UI 自動操作

### 4-1. 部品を足したら検査する

```
polyling_call queryUiAutomationAudit
```

6 つのカウンタが**すべて 0** であること。

| カウンタ | 0 でないときの直し方 |
|---|---|
| 未登録のセクション | `RegisterUiPanel` で登録。使わないなら `ExcludeSection` に理由を添える |
| 属性の付け忘れ | `UiControl` を付ける。外に出さないものは `Ignore = true` を明示 |
| 安全度未指定のボタン | `Safety` を付ける |
| 未対応の型 | `UiAutomationService` に読み書きを足す |
| 未登録の部品 | フィールドに上げるか、動的な置き場へ登録 |
| 登録失敗 | エラー文のとおりに直す |

**属性の付け忘れはコンパイルを通ってしまう。** 検査だけが気づく手段。
再生開始時にも自動で走り、問題があればログとコマンド一覧パネルに出る。

### 4-2. 安全度の選び方

| 値 | 使うとき |
|---|---|
| `ReadOnly` | 表示だけ |
| `SafeWrite` | 取り消せる、モデル内で完結する |
| `Destructive` | 消える・戻せない |
| `FileOperation` | ダイアログなしでファイルへ書く |
| `UserOnly` | ダイアログを開く（外から押しても人の操作が要る） |

---

## 5. シナリオを読んで遂行する

システムデバッグのシナリオは 14 本ある。どれも作りは同じ。

### 5-1. シナリオの位置づけ

シナリオは「六角柱ならこう数える」といった**固有の計算を埋め込んである**。

- **人間が学ぶとき**: 計算に当たる部分を目で見て決める
- **人工知能が行うとき**: シナリオと同じ計算を自分で行う

つまり「`HowTo` が目で見て選べと書いているから UI では無理」ではない。
**座標を取って索引を計算するのが、こちら側の仕事。**

この計算が汎用的なら、**新しいコマンドとして登録する候補**になる。
ただし登録は利用者からの要請があったときだけ。

### 5-2. どこを読むか

シナリオのパネルは `Runtime/Poly_Ling_Player/View/SubPanels/Pipeline/` にある。

| ファイル | 何が書いてあるか |
|---|---|
| `Player<名前>SubPanel.cs` | 段の定義。目的・操作手順（`HowTo`）・注意（`Note`） |
| 同 `.Stages.cs` / `.Bridge.cs` など | 段の実装。**実際に送っているコマンドと引数** |
| `<名前>Recipe.cs` | 諸元（あるシナリオのみ） |

`HowTo` は UI 操作の言葉で書いてある。そのとおりに `uiSetValue` / `uiClick` する。

### 5-3. 引数の値に迷ったら実装を読む

引数の値（`addMode` に何を渡すか等）を**推測で選ばない。**
選択肢が複数あって決め手が無いときは、実装を読んで実際に何を渡しているかを確かめる。

推測で試すと、当たっても理由が分からず、外れると切り分けに時間がかかる。
過去に推測で Editor の API を足して**応答しなくさせた**ことがある。

### 5-4. 判定基準も先に確かめる

**何をもって成功とするかを、先にコードか `Note` で確かめる。**

確かめずに思い込みで判定すると、**成功しているものを失敗と報告する**ことになる。
実際に 2 回やった。

| 場面 | 思い込み | 実際 |
|---|---|---|
| 穴つなぎ | 穴の数が減るはず | 減らないのが正常 |
| ウェイト塗り | 輪が等間隔に揃うはず | 形によっては散らばるのが正常 |

どちらも実装を最後まで読んでいれば防げた。

### 5-5. 計算の書き方

座標から索引を出すとき、**具体的な数値を直接書かない。**
性質で探すと、寸法や分割数が変わっても同じやり方が通る。

| 悪い | 良い |
|---|---|
| `x が ±0.0167 の頂点` | `x の符号が変わる隣接ペア` |
| `Y が 0.06〜0.12` | `側面を Y で 3 段に分けた中段` |

**必ず最後に `query*` で検算する。** `Note` に期待する結果が書いてあることが多い。

### 5-6. シナリオのパネルの「実行」は押さない

各段でファイルへ保存する作りになっている。保存が要らないときは押さない。
段を 1 つずつ自分で実行する。

---

## 6. 中断と再開

1 回の応答で終わらない作業は珍しくない。**「続けて」だけで再開できる**ようにしておく。

### 6-1. 作業ログ

`Packages/com.haglib.polyling/作業ログ.md` に、次の形で残す。

```
## いまの作業
（シナリオ名と系統）

## 完了した段
- 段名：何をして、検算で何を確かめたか

## 次にやること
（次の段と、そのときに要る値）

## 確認できなかったこと
（飛ばした検証があれば）

## 気づいた点
（手順書へ反映する候補）
```

- **段を 1 つ終えるたびに書き換える。** 応答の終わりにまとめて書くのではなく、終えた時点で書く
- 途中で上限に達しても、ここを読めば次の担当（次の応答の自分）が続けられる

### 6-2. 再開するとき

「続けて」と言われたら、**推測しない**。この順で確かめる。

1. `作業ログ.md` を読む
2. `unity_state` で再生中か確認
3. `queryModelStructure` で今あるものを確認
4. ログと実際が食い違っていたら、**実際を優先**してログを直す

再生し直しているとプロジェクトは空になっている。その場合は作り直しになるので、
ログに「作り直しが要る」と書いて、作業を再開する。

### 6-3. 1 応答の作業単位

**1 応答につき 1 段**を目安にする。

ただし段によっては 1 応答で収まらない。
図形を何個も作る段は、1 個あたり 10〜15 回の呼び出しが要る。

**着手時に分割を宣言する**こと。
「この段は 2 回に分ける。今回は前半」のように先に言う。
黙って途中で切れると、利用者からは進んでいないように見える。

### 6-4. 実行の定型

**下の順番を 1 つのまとまりとして扱う。** 途中を飛ばさない。

図形を 1 つ作るとき:

```
1. その図形で使わない設定を戻す（TrsReset / チェックを外す）
2. 諸元を入れる
3. 位置・名前を入れる
4. create を押す
5. unity_focus            ← 4 とセット。画面を更新させる
6. その形の 1 つ目なら getRawData で軸を検算
7. 作業ログを書き換える
```

段を 1 つ実行するとき:

```
1. 実行する
2. unity_focus            ← 1 とセット
3. query* で検算する
4. 作業ログを書き換える
```

---

## 7. 残件

- **撮影時の縮小。** `target=Window` は長辺 1,568px を超えるので、撮影側（`uiCapture`）で
  縮められれば無駄がない。見えなくなるわけではないので急がない
- PolyLing のコマンドが画像を直接返す経路（応答 JSON の `image` / `images` を
  MCP サーバが image content へ引き上げる）は、**サーバ側だけ実装済みで返すコマンドが無い。**
  足すときは、1 行 1 JSON の伝送路が数十万文字の行に耐えるかを別に確かめること

---

## 8. 着手前のチェック

- [ ] 「続けて」なら、まず `作業ログ.md` を読んだか（推測で再開していないか）
- [ ] 何をするか曖昧なら、確認したか（どのシナリオか、どの系統か）
- [ ] 対象シナリオの補足手順書があるなら、読んだか
- [ ] `polyling_tools` を取り直したか（コマンドが増えていないか）
- [ ] 利用者の指示を、自分の都合で言い換えていないか
- [ ] 消す操作を含むなら、断りを取ったか
- [ ] スクリプトを直したなら、`unity_refresh` を手順に入れたか
- [ ] **何をもって成功とするか**を、コードか `Note` で確かめたか（思い込みで判定しない）
- [ ] 引数の値に迷ったら、推測せず実装を読む用意があるか
- [ ] 画像で確かめるなら、`unity_capture` を使う用意があるか（`uiCapture` を手で 3 段に分けない）

## 9. 終わるときのチェック

- [ ] `作業ログ.md` を、実際に終えたところまで書き換えたか
- [ ] 「完了が返った」を「正しい」と読み替えていないか。確かめたか
- [ ] 見た目の確認を、`unity_capture` で自分で見ずに利用者へ回していないか
- [ ] 手順書に足すべき気づきがあれば、ログの「気づいた点」に書いたか
- [ ] シナリオ固有の気づきなら、汎用の手順書ではなく補足手順書へ書いたか
