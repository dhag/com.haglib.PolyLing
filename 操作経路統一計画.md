# PolyLing 操作経路の統一 計画書（検討段階）

| 項目 | 内容 |
|---|---|
| 状態 | 検討段階（未実装） |
| 作成 | 2026-09-18 |
| 関連 | `マルチディスプレー対応計画.md`（ペンディング。本計画を優先する） |

---

## 1. 目的

- 人（本体パネル・リモートクライアント）と AI（MCP）の操作を、同じ入口・同じ公開層で扱う。
- 左右ペインのパネルを「本体の状態を見てパラメータを設定し、コマンドや操作を送る操作盤」に作り直し、本体の外（別プロセスのクライアント）でも同じパネルが動くようにする。
- コマンドを作る前に必要な情報（概要・件数・現在値）を、パネルと MCP が同じ手段で取得できるようにする。

---

## 2. 決定事項

| 項目 | 決定 |
|---|---|
| パラメータの正本 | 本体のハンドラに残す。クライアント・MCP は写しを持つ |
| 操作者の扱い | 記録・表示のみ |
| Undo | 全員共通の 1 本のまま（操作者別 Undo は行わない。理由は 3.4） |
| 排他 | 既存の担当者ロック（`RemoteOwnership`）を全経路に適用する |
| 転送するデータ | 頂点位置・面構成の詳細以外すべて。ID・インデックス・件数などの概要は出す |

---

## 3. 現状（コードで確定した事実）

### 3.1 入口が 3 本あり、扱いが揃っていない

| 入口 | 経路 | 操作者の識別 | 選択 | 所有権判定 |
|---|---|---|---|---|
| 本体パネル | `DispatchPanelCommand` → ディスパッチャ | なし | ホストの選択 | 通らない |
| リモートクライアント | WebSocket → `RemoteServerCore` → `PanelCommandFactory.Create` → ディスパッチャ | `register` のユーザー名 | ユーザー別（`RemoteSelectionStore`） | 通る |
| MCP | TCP → `PolyLingEditorControlServer` → `PanelCommandFactory.Create` → `PolyLingCommandGateway` → ディスパッチャ | なし | ホストの選択 | 通らない |

- コマンドの組み立ては 3 本とも `PanelCommandFactory`（`PanelCommandFactory.cs`）。サーバ側の手書きは `selectMesh` と `applyBlend` の 2 件のみ（`RemoteServerCore.Messages.cs:497-554`）。
- 逆変換 `PanelCommandFactory.ToArgs(cmd)` がある（`PanelCommandFactory.cs:270`）。
- クライアント側の `PanelCommandRouter` は手書きの switch で一部のコマンドだけを送り、他は捨てる。
- `RemoteOwnership.cs` のコメントに「MCP の経路はこの判定を通らない」と明記されている。

### 3.2 パネルと本体の結合

パネルは本体の生オブジェクトを直接受け取っている。

| 受け取り方 | 内容 |
|---|---|
| `Func<具体ハンドラ型>`（`GetH` / `GetHandler`） | 35 か所（`PolyLingPlayerViewerCore.Layout*.cs`、`BillboardProfile.cs`） |
| `Func<ProjectContext>` / `Func<ModelContext>` / `Func<ToolContext>` | 約 60 パネル |
| `Action<PanelCommand> SendCommand` | 54 ファイル |
| Core のメソッドへのコールバック | `OnExecute`、`OnFinish`、`OnModeChanged`、`OnGenerate`、`OnSelectMesh`、`OnSelectMaterial`、`OnExportXxx` ほか |
| ビューポート設定 | Camera（`OrbitCameraController`）、GridAxis（`ViewportGridSettings`）、Underlay（`UnderlayConfig`） |

- `PanelContext` 経由（コマンド送信＋View 通知）で作られているのは MeshList、ModelList、VertexMove、SkinKind、MeshFilterToSkinned、BoneEditor（`GetModel` も併用）のみ。
- 左ペイン（`PlayerLayoutRoot.LeftPane*.cs`）は要素を公開するだけで、配線はすべて Core 側。

### 3.3 ハンドラのプロパティは単なる値ではない

例：`RotateTool.cs:65-73`

- `RotX` の setter はドラッグ中なら `UpdatePreview()` を呼ぶ。
- `UseOriginPivot` の setter は `UpdatePivot()` を呼ぶ。
- `SnapAngle`・`MagnetRadius` の setter は値をクランプする。
- `PivotPublic` の getter は `UpdateAffected()` と `UpdatePivot()` を実行する。

したがって、クライアント側の写しをハンドラのインスタンスで作ることはできない。クランプ後の値は本体から返す必要がある。

### 3.4 Undo

- `MeshUndoController` の `_mainGroup` 1 つに、頂点編集・エディタ状態・作業平面・メッシュリスト・プロジェクトの各スタックが属する。ポリシーは `OperationLog`（時系列で 1 本に統合）（`MeshUndoController.cs:183-231`）。
- 記録は「操作前の状態へ戻す」形。メッシュリストの追加・削除は索引で挿し戻す（`ModelUndoStack.cs:15-21`）。
- 操作者を表す情報は `MeshUndoStack`・`ModelUndoStack`・`MeshUndoController` にない。
- 共通の 1 本を新しい順に戻す限り整合する。特定の操作者の操作だけを戻すと、後続の他者の操作を飛び越えるため整合しない。

### 3.5 担当者ロック（`RemoteOwnership.cs`）

- 真値は `MeshContext.EditorName`（プロジェクト保存に含まれる）。
- `TryAuthorize` は状態を持たない純関数。対象解決（`ResolveTargets`）→ 安定 ID 照合（ズレ検出）→ 担当者判定。
- 対象を特定できないコマンドは「モデル内に他人の担当が 1 つでもあれば拒否」。
- 担当の解放は手動のみ。

### 3.6 MCP がコマンドを作る前に取れる情報

- `Query*` コマンド 20 種（`PanelCommand.Query.cs`、`PanelCommand.TopologyQuery.cs` ほか）。
- UI 自動操作（`uiDescribe` / `uiGetValue` / `uiSetValue` / `uiClick`）。パネルの部品単位。
- パネルがハンドラから読んでいる概要（`Inspect()`、各 Count、`LastResult` 等）に対応する `Query*` はない。

---

## 4. パネル → ハンドラの操作一覧

| パネル | 書くパラメータ | 呼ぶ操作 | 読み返す状態・結果 | 実行経路 |
|---|---|---|---|---|
| Rotate | RotX/Y/Z、AxisMode、AxisVecX/Y/Z、AxisAngle、UseSnap、SnapAngle、UseOriginPivot、UseMagnet、MagnetRadius、MagnetDistanceMode、MagnetFalloff | BeginSliderDrag、CommitViaCommand、Revert | GetTotalAffectedCount、PivotPublic | ハンドラが確定 |
| Scale | UniformScale、ScaleX/Y/Z、ScaleAxisX/Y/Z、UseOriginPivot、Magnet 系 4 項目 | BeginSliderDrag、CommitViaCommand、Revert | GetTotalAffectedCount | ハンドラが確定 |
| Deform | Phase、ShowShapePreview、Magnet 系 4 項目、`Deformer.Params`（直接書換え） | SelectDeformer、ApplyPreview、RequestGizmoRefresh、CommitViaCommand、Revert、ResetParams | DeformerName、Deformer.DisplayName、IsPreviewing、AffectedCount、PreviewContext | ハンドラが確定 |
| Lattice | Mode | SetCells、SetBounds、FitToSelection、BeginPlacement、BeginDeform、ResetDeformation、CommitViaCommand、Cancel | State、Grid.CellsX/Y/Z、Grid.BaseCenter/BaseSize、AffectedCount、ControlPointCount、SelectedPointCount | ハンドラが確定 |
| SurfaceSnap | CameraKind、SelectedVerticesOnly、SurfaceOffset、Backface | CancelIfActive、PruneReferences、TriggerCompute、SetSlider、TriggerCancel | IsReference、ReferenceIndices、TargetMeshCount、IsPreviewing、Slider、LastResult、`Model` | SendCommand |
| VertexMove | DragSelectMode、UseMagnet、MagnetRadius、Min/MaxMagnetRadius、MagnetDistanceMode、MagnetFalloff、GizmoScreenOffsetX/Y、IsRadiusDragMode | — | GetTotalAffectedCount | SendCommand |
| Sculpt | Mode、BrushRadius、Min/MaxBrushRadius、Strength、Min/MaxStrength、DistanceMode、Falloff、Invert、IsRadiusDragMode | — | 書いた値 | ビューポート操作 |
| AdvancedSelect | Mode、EdgeLoopThreshold、UvNormalCountThreshold、AxisKind、AxisDistanceThreshold、LimitToCurrentSelection、AddToSelection | ExecuteBoundaryEdgeInSelection、ClearAllSelection、InvertSelection、ClearShortestPathFirst | GetShortestPathFirstVertex | SendCommand＋ハンドラ直接 |
| AlignVertices | AlignX/Y/Z、Mode | TriggerAutoSelect | SelectedVertexCount、StatsCalculated、StdDevX/Y/Z、GetAlignTarget | SendCommand |
| AddFace | ModePublic、ContinuousLinePublic、ExtendLineGroupPublic、SnapToUnselectedObjects、SnapToBones、SnapToObjectOrigins | ClearPointsPublic | PlacedPointCount、RequiredPointsPublic、GetPointLabels | ビューポート操作 |
| BillboardProfile | Mode、ExtendExisting | FinishChain、SetSelectedConstraint、DeleteSelectedPoints、SmoothSelectedPoints | Status、ChainPoints.Count、FreeformPoints.Count、SelectedPoints.Count | Core の `OnFinish` |
| EdgeBridge | BoundaryEdgeOnly、AutoCorrespondence、FlipCorrespondence、FlipFaces、Subdivisions | ClearPicks | PickedEdgeCount、LastRejectReason、Inspect | Core の `OnExecute` |
| HoleRingCount | SplitTriangleIntoTriangles | ImportBase、ImportTarget、ClearSeeds | BaseSeed/TargetSeed（MeshIndex、Vertex、DirectionHint）、Inspect | Core の `OnExecute` |
| Knife | Mode、EqualDivide、Divisions、SimpleTriQuad | — | StageText | ビューポート操作 |
| EdgeBevel | Amount、DragSensitivity、Segments、Fillet | — | — | ビューポート操作 |
| EdgeExtrude | Mode、SnapToAxis、DragSensitivity | — | — | ビューポート操作 |
| FaceExtrude | Type、BevelScale、IndividualNormals、DragSensitivity | — | — | ビューポート操作 |
| EdgeTopology | ModePublic | — | — | Core の `OnModeChanged` |
| LineExtrude | Thickness、Scale、FlipY、SegmentsFront/Back、EdgeSizeFront/Back、EdgeInward | AnalyzeLoops | GetLoopSummaries、SelectedLineCount、DetectedLoopCount、Offset | SendCommand |
| Solidify | Thickness、AddToExisting、MeshName、AddTargetIndex、SegmentsFront/Back、EdgeSizeFront/Back、EdgeInward | — | SelectedFaceCount、LastMessage | SendCommand |
| SmoothEdges | Strength、Iterations、FixEndpoints、LockX/Y/Z | RefreshStats | StatsCalculated、SegmentCount、ChainVertexCount、EndpointCount、MovableVertexCount | SendCommand |
| MergeVertices | Threshold、ShowPreview、RemoveClosedFaces | — | PreviewInfo | SendCommand |
| PipeAlign | Mode、Direction、EdgeMode、RingVertexCount、CapStart、CapEnd、PairText、WeightText、TargetText | — | TargetMeshCount、LastResult | SendCommand |
| PlaceObjectReshape | Mode、Lambda、TargetText | — | CollectSelectedPartsIdText、TargetMeshCount、LastResult | SendCommand |
| PlanarizeAlongBones | BoneIndexA/B、PlaneMode、Blend | RebuildBoneList | BoneNames、SelectedVertexCount、GetBoneWorldPosition | SendCommand |
| VertexHole | Ratio | — | SelectedVertexCount、Inspect | SendCommand |
| VertexDissolve / Quad4To1 | — | — | SelectedVertexCount、Inspect | SendCommand |
| Tri4To1 | — | — | SelectedFaceCount、Inspect | SendCommand |
| FaceMerge | DeleteVertices | — | SelectedEdgeCount、Inspect | SendCommand |
| SplitVertices | — | — | SelectedVertexCount、GetSplittableCount | SendCommand |
| WorkAxis | SnapToVertex、SnapToBone、SnapToObject、RotateSnapDeg、Mode | — | 書いた値 | ビューポート操作 |
| Camera | ハンドラ：TargetKind、GizmoOp ／ OrbitCameraController：RotX/Y/Z、Distance、Fov、Target | — | Orthographic ほか | ビューポート直接 |
| FlipFace | （`GetH` は宣言のみで未使用） | — | — | SendCommand |

- 逆方向通知：Sculpt・VertexMove はハンドラに `OnRadiusChanged`、`OnRadiusDragModeExited` を登録している。
- 座標値を読み返すもの：`PivotPublic`、`GetAlignTarget`、`Grid.BaseCenter/BaseSize`、`GetBoneWorldPosition`、Camera の `Target`。いずれも単一の座標値で、頂点配列ではない。
- SurfaceSnap はハンドラの `Model`（ModelContext 参照）を読む。

---

## 5. 方針

| 記号 | 内容 |
|---|---|
| A | コマンド：クライアントの `PanelCommandRouter` を `ToArgs` による汎用送信に置き換える |
| B | パラメータ：ハンドラの公開プロパティに属性で「出す／出さない」を宣言（`PLParam` と同じく出さないものも明示）。本体がキー単位で get/set を受け、set は本体のハンドラで実行して適用後の実値を返す |
| C | 操作：ハンドラのメソッドに属性を付け、名前で呼べるようにする |
| D | 概要：読み取り専用の状態に属性を付け、本体が変化時に push する。逆方向通知（`OnRadiusChanged` 等）も含める |
| E | パネル：`Func<具体ハンドラ型>` と生 Context の受け取りを、B〜D へのアクセス用インターフェースに置き換える。本体用実装はハンドラを直接叩き、クライアント用実装は写しを持ってメッセージを送る |
| F | Core コールバック：コマンドか C の操作へ寄せる |
| G | 入口：本体パネル・リモート・MCP を 1 つの入口に集め、全員が名乗り、そこで `TryAuthorize` を通す |

B〜D の宣言から MCP のスキーマも生成する（既存の `PanelCommandSchema` と同じ仕組み）。

---

## 6. 進める順序

（2026-09-18 の調査結果を受けて 9 章で見直した。以下は当初案として残す）

1. 入口の一本化（G）
2. 本体側の公開層（B・C・D）
3. MCP・リモートを公開層へ接続（A を含む）
4. パネルを公開層経由へ置き換え（E・F）。パネル単位で順次移行

---

## 7. 未確認事項

| # | 内容 | 状態 |
|---|---|---|
| U1 | `ResolveTargets` が全コマンド型を網羅しているか | 調査済（8.1） |
| U2 | Core コールバックの中身 | 調査済（8.2） |
| U3 | 生 Context を直接読むパネル（約 60 件）が何を読んでいるか | 調査済（8.3） |
| U4 | 概要表示ラベルが `UiControl` 登録されているか | 調査済（8.4） |
| U5 | ライブプレビュー中（`BeginSliderDrag` 〜 確定）のメッシュ書き換えを、ロック判定のどこで止めるか | 調査済（8.5） |

---

## 8. 調査結果

### 8.1 U1：`ResolveTargets` の網羅性（2026-09-18）

**方法**：`Runtime/` 配下の `class *Command : PanelCommand`（および中間基底 `ApplyDeformCommand` / `CreatePrimitiveMeshCommand` / `CreateBeltPrimitiveCommand` の派生）を列挙し、`RemoteOwnership.cs` の `ResolveTargets`・`IsOwnershipExempt`・`IsUiAutomation`・`IsScenario`・`SetObjectEditorCommand` の分岐と手作業で照合した。

**結果**：どの分岐にも載っていない型は、`ResolveTargets` の `default` から `AuthorizeModelWide`（モデル内に他人の担当が 1 つでもあれば拒否）へ落ちる。該当は 187 件（手作業での照合）。

| ファイル | 分岐に載っていない型 |
|---|---|
| Blend | CreateBlendClone、ApplyModelBlend、PreviewModelBlend |
| Bone | SetBoneTransformValue、SetPoseDisplayMode、SetBonePoseValue、BeginBoneTransformSliderDrag、EndBoneTransformSliderDrag、FreezeCurrentPose、BuildSpringBoneTestRig、ApplyHumanoidMapping、ClearHumanoidMapping、SetAvatarRetarget、ClearAvatarRetarget |
| BridgeByName | BridgeHolesByName |
| Deform | ApplyShrink、ApplyThinPlateMorph、ApplyThinPlateMorphResult、MediaPipeFaceDeform |
| FileIO | ExportPmxFile、ImportMqoFile、ExportMqoFile、ImportObjFile、ExportObjFile、ImportStlFile、ExportStlFile、ExportVrmFile、ImportVrmFile、SaveProjectFile、LoadProjectFile、SaveProjectCsv、LoadProjectCsv |
| MeshAttribute | SetBatchLock、SetMirrorEnabled、SetBatchMirrorType、RenameMeshes、ApplyObjectOrigins、GenerateObjectPoseWedges、ApplyObjectPoseWedges、SetPreserveNormals、SetMeshBillboard、SetMirrorBranchRoot、SetFaceHidden |
| MeshList | ReorderMeshes、SetParentsByName、RenameModel、DeleteModel、MergeMeshes、RebuildObjectGroup、DeleteObjectGroup、MergeObjectGroup、SetObjectGroupAutoUpdate |
| Mirror | BakeMirror、UnbakeMirror |
| Morph | ConvertMeshToMorph、ConvertMorphToMesh、CreateMorphSet、StartMorphPreview、ApplyMorphPreview、EndMorphPreview、SelectAllMorphs、DeselectAllMorphs、CreateMorphFromDiff |
| Normal | SaveNormalExcludeSet、LoadNormalExcludeSet、DeleteNormalExcludeSet、RenameNormalExcludeSet、NormalEdit、RepairVertexIds、AssignPartsIds、AssignPartsIdsByBoneWeight、SplitObjectByPartsId、TransferVertexData、SaveMeshSelSetsCsv、LoadMeshSelSetsCsv、ApplyNormalTransplant |
| PartialImport / PmxPartialImport | ImportMqoVertexPositions、ImportPmxVertexAttributes |
| Primitive / PrimitiveBelt | `CreatePrimitiveMeshCommand` の派生 37 種、`CreateBeltPrimitiveCommand` の派生 3 種（Frill、Pipe、PlaceObject）、AddGeneratedMesh、CreateHoleBridge、CreateEdgeBridge、ResetProject |
| Query | QueryObjectGroups、QuerySkinWeightSummary、SetRawData |
| Selection | SavePartsSet、LoadPartsSet、AddPartsSet、SubtractPartsSet、DeletePartsSet、RenamePartsSet、ExportPartsSetsCsv、ImportPartsSetCsv、SaveSelectionDictionary、ApplySelectionDictionary、DeleteSelectionDictionary、RenameSelectionDictionary、CapturePartsSetVertexIds、ResolvePartsSetByVertexId |
| Skin | ConvertMeshFilterToSkinned、ConvertToMeshFilter、ConvertToSkinned、ResolveMirrorBoneIndex、FloodSkinWeight、NormalizeSkinWeight、PruneSkinWeight、SetSkinWeightNumeric、NormalizeAllSkinWeights |
| SpringBone | SetSpringBoneSettings、AddSpringBoneColliderGroup、RenameSpringBoneColliderGroup、DeleteSpringBoneColliderGroup、UpdateSpringBoneCollider、DeleteSpringBoneCollider、ApplyTPose、RestoreTPose、BakeTPose、ApplySpringBoneByPrefix |
| TempVerify | VerifyBindMove、VerifyBindRotate、VerifyBindSculpt |
| Topology | BooleanMesh、DiagnoseBoolean、ResolveTJunctions、QuadDecimate、ImportPmxFile |
| Transform | SetActiveWorkAxis、CreateWorkAxisObject、SetVertexPositions |
| UndoRedo | PerformUndo、PerformRedo、MatchHoleRingCount、DeleteFaces、CreateObjectArray |
| UvMaterial | ApplyUvUnwrap、UvToXyz、XyzToUv、ApplyUVChanges、ApplyLscmUnwrap、AddMaterialSlot、RemoveMaterialSlot、ApplyMaterialToFaces、SetMaterialColor |
| Vrm | SetVrmMeta、ClearVrmMeta、SetVrmLookAt、ClearVrmLookAt |

**意図して載せていないもの（`RemoteOwnership.cs` のコメントで確認）**
- VRM メタ・視線・Avatar リターゲット：モデル固有の値で対象ノードを持たないため。
- コライダーグループ・評価設定：参照索引の詰め直しで全ノードに波及しうるため。

**この結果から確定すること**
- `PanelCommandRouter`（リモートのリスト用クライアント）が送る SetBatchLock・SetMirrorEnabled・SetBatchMirrorType・RenameMeshes・RenameModel・DeleteModel は、対象が明示されているのにモデル全体判定になる。
- 照会の QueryObjectGroups・QuerySkinWeightSummary は `IsOwnershipExempt` に無く、モデル全体判定になる（他人の担当があるモデルでは読むだけでも拒否される）。
- 図形生成（40 種）はモデル全体判定になる。既存オブジェクトへの追加（`PLRebuildRole.TargetIndex` 付きの AddTargetIndex）を伴わない場合も同じ。
- PerformUndo・PerformRedo はモデル全体判定になる。一方、本体パネルと MCP はそもそも判定を通らないため、それらからの Undo は他人の担当オブジェクトへの変更も戻す（3.4 の共通 1 本の Undo と一致）。

**関連する既存の仕組み**
- `PLParam` には描画オブジェクトを索引で指すパラメータの印（`IsMeshRef`）があり、`PanelCommandFactory.MeshRefKeys(Type)` で型ごとに列挙できる（`PanelCommandMeshRefs.cs:61`）。印の付け忘れを数える `UnmarkedMeshIndexKeys(Type)` もある（同 :520）。
- ただし `MeshRefKey` が持つのは `Key`・`IsArray`・`ModelKey` だけで、「書き込み先」か「読むだけの参照元」かの区別を持たない（`PanelCommandMeshRefs.cs:34-55`）。同ファイル冒頭の注記は ApplyBlend の宛先（書き込み）とソース（読むだけ）を同列に挙げている。したがって `ResolveTargets` の手書き分岐を現在の `IsMeshRef` だけで置き換えることはできない。各コマンドの参照パラメータに `IsMeshRef` が付いているかは未確認。

### 8.2 U2：Core コールバックの中身（2026-09-18）

**方法**：`SubPanels/` 配下のパネルの公開デリゲート（`GetH`・`GetHandler`・`GetView`・`GetProject`・`GetModel`・`GetToolContext`・`SendCommand` 以外）を列挙し、Core 側の代入先を確認した。

**(a) 同じ機能の PanelCommand があるのに、パネルはディスパッチャを通らず Core の関数を直接呼ぶ**

| パネル | コールバック | Core 側 | 対応する PanelCommand（ディスパッチャに受け口あり） |
|---|---|---|---|
| Export | OnExportPmx/Mqo/Obj/Stl/Vrm | `OnExportPmx` 等が `PMXExporter.Export` 等を直接実行（`Import.cs:345-`） | Export*FileCommand（`PlayerCommandDispatcher.EditTools.cs:366` ほか） |
| Import | OnImportPmx/Mqo/Obj/Stl/Vrm | `ImportPmxCommand`（ICommand）を `CommandQueue` へ積む（`Import.cs:32-44`） | Import*FileCommand（同 :357 ほか） |
| ProjectFile | OnSave/OnLoad/OnSaveCsv/OnLoadCsv | `ProjectSerializer` を直接実行（`Import.cs:393-`） | Save/LoadProjectFileCommand、Save/LoadProjectCsvCommand（同 :454 ほか） |
| Pivot | OnPivotToVertexCentroid/BoneCentroid | `MovePivotToCentroid`（`Import.cs:525-`） | MovePivotCommand（同 :114）。両者の処理が同一かは未確認 |
| Capture | OnCapture | `StartCapture`（`ViewAids.cs:47-`）。uiCapture と共通 | UiCaptureCommand |
| EdgeBridge | OnExecute | `SendEdgeBridgeCommand`：ハンドラの拾った辺から組み立てる（`CreateCommands.Topology.cs:772-`） | CreateEdgeBridgeCommand |
| HoleRingCount | OnExecute | `SendHoleRingCountCommand`：ハンドラのシードから組み立てる（同 :932-） | MatchHoleRingCountCommand |
| AddFace | OnSelectMesh | `_commandDispatcher.Dispatch(new SelectMeshCommand(...))`（`Layout.EditTools.cs:180-`） | SelectMeshCommand |

**(b) コマンドになっていない本体状態の操作**

| パネル | コールバック | Core 側 |
|---|---|---|
| AddFace | OnSelectMaterial | `model.CurrentMaterialIndex = idx` を直接代入（`Layout.EditTools.cs:195-`） |
| BillboardProfile | OnFinish | `_billboardProfileHandler.FinishChain()`（`BillboardProfile.cs:46`） |
| EdgeTopology | OnModeChanged | 選択モードの上書き（`SelectMode.cs:234-`） |
| BoneEditor | OnFocusCamera | `orbit.SetTarget(pos)`（`Layout.Panels.cs:238-`） |
| BoneEditor | RequestBakeObjectScale | `BakeObjectScale`（`Layout.Panels.cs:248`） |
| UVZ | OnEnterUvEditMode / OnExitUvEditMode | `EnterUvEditMode` / `ExitUvEditMode`（`Layout.Panels.cs:291-292`） |
| MeshList | OnViewportOpModeChanged | `ApplyMeshListViewportOpMode`（`Layout.Panels.cs:46-51`） |
| Camera | SetTriFlip、SetMainOrthographic、FlipMainView、GetTri | ビューポートのカメラ設定 |

**(c) パネルの中で編集処理を実行している（本体の Undo・CommandQueue を直接受け取る）**

| パネル | 受け取るもの | 確認箇所 |
|---|---|---|
| Shrink | GetUndoController、GetCommandQueue、OnSyncMeshPositions、GetWorldPositions | パネル内で `ToolContext` を組み立ててプレビューを実行（`PlayerShrinkSubPanel.cs:700-722`） |
| Blend | GetUndoController、GetCommandQueue、OnSyncMeshPositions/Normals、GetModelContext | 同様に `ToolContext` を組み立て（`PlayerBlendSubPanel.cs:1039-1045`） |
| NormalTransplant | GetUndoController、GetCommandQueue、OnSyncMeshNormals、GetWorldPositions | 同様（`PlayerNormalTransplantSubPanel.cs:538, 621-622`） |
| UVEditor | GetUndoController、GetCommandQueue | `CommandQueue.Enqueue`（`PlayerUVEditorSubPanel.Helpers.cs:29-37`） |
| MorphCreate | GetUndoController | `PlayerMorphCreateSubPanel.cs:256` |
| BoneEditor | GetUndoController | `PlayerBoneEditorSubPanel.Helpers.cs:297` |
| MotionClipTest / VMDTest | GetUndoController | `PlayerMotionClipTestSubPanel.cs:465`、`PlayerVMDTestSubPanel.cs:500`（UnityClipTest も宣言はあるが、呼び出し箇所は検索で見つからなかった） |
| MqoToPmxTest / PmxToMqoTest | GetUndoController、ImportPmx/Mqo、ExportPmx/Mqo、EnqueueCommand（ICommand） | `PlayerMqoToPmxTestSubPanel.cs:487`、`PlayerPmxToMqoTestSubPanel.cs:518` |

- Shrink・NormalTransplant には PanelCommand（ApplyShrinkCommand、ApplyNormalTransplantCommand）も存在する。パネルの確定がそれを通るかは未確認。

**(d) 本体からの情報取得（`Func<...>`）**

| パネル | 取得するもの |
|---|---|
| AddFace | GetMeshEntries、GetActiveMeshIndex、GetMaterialNames、GetCurrentMaterialIndex |
| ObjectArray | GetDrawableList |
| PartsId / PlaceObjectReshape | GetDrawableMeshEntryList（`MeshObject` を含む）、GetLastResult ほか |
| Solidify | GetDrawableIndexList、GetFirstSelectedDrawableIndex |
| BillboardProfile | GetTargetInfo |
| WorkAxis | GetWorkAxis（WorkAxisContext）、GetSelectionCentroidWorld、GetLibrary |
| UVZ | GetCameraPosition、GetCameraForward |
| MeshList / BoneEditor | GetObjectMoveSettings |
| Sculpt | GetTempMirror |
| Blend / ModelBlend | GetProjectView（IProjectView） |
| RemoteServer | GetServer |
| Scenario | GetRun、RunCommand（`Func<PanelCommand, CommandResult>`。実行経路は未確認） |
| 多数 | GetModelIndex |

**(e) 本体表示の更新通知**：OnRepaint、OnGizmoRefresh、OnDisplayChanged、OnHighlightChanged、OnVisualizationTargetChanged、OnTargetBoneChanged、OnValueChanged、OnLibraryChanged、OnFrameApplied、OnRebuildModelList、OnNotifyTopologyChanged、OnRequestUpdateTransform、RefreshAfterReplace、RefreshAfterTopologyChange ほか。

**この結果から確定すること**
- 同じ機能に「パネル → Core 直接」と「PanelCommand → ディスパッチャ」の 2 経路があるものがある（(a)）。人の操作は前者、MCP は後者を通る。
- (c) のパネルは編集処理そのものをパネルが持っている。クライアント側へ出すには、処理を本体側（コマンドまたは公開層の操作）へ移す必要がある。
- (d) の中には `MeshObject` を丸ごと返すもの（GetDrawableMeshEntryList）がある。

### 8.3 U3：生 Context を読むパネルが何を読んでいるか（2026-09-18）

**方法**：`SubPanels/` 配下の `Player*SubPanel*.cs` で、メッシュ詳細への到達点となる `.MeshObject`・`.Vertices`・`.Faces` を検索し、読んでいる内容で分類した。

**(1) 頂点・面の詳細を読む／書く（Pipeline テスト以外）**

| パネル | 内容 | 確認箇所 |
|---|---|---|
| UVEditor | 頂点・面・UV を走査してキャンバスに描画、UV を編集 | `PlayerUVEditorSubPanel.Canvas.cs:101-460`、`.Edit.cs:33-112` |
| MaterialList | 面を走査。`_panelContext` があれば ApplyMaterialToFacesCommand を送り、無ければ `Faces[fi].MaterialIndex` を直接書き換えて Undo を記録する（パネル内に 2 経路） | `PlayerMaterialListSubPanel.cs:834, 850-879` |
| QuadDecimator | 面を走査して Quad/Tri 数をパネルで数える | `PlayerQuadDecimatorSubPanel.cs:149-167` |
| FaceHide | 面を走査 | `PlayerFaceHideSubPanel.cs:134-136` |
| MediaPipeFaceDeform | 頂点位置を読み、複製した MeshObject の頂点位置を書く | `PlayerMediaPipeFaceDeformSubPanel.cs:194-210` |
| MorphCreate | 頂点位置の差分を計算し、MeshObject を複製してモーフの MeshContext を作る | `PlayerMorphCreateSubPanel.cs:269-508` |
| Blend | `BlendVertexResolver.InspectVertexIds(MeshObject)`、`IsTriangulated` を読む | `PlayerBlendSubPanel.cs:709-738` |

**(2) MeshObject の付帯データを読む（頂点・面の詳細ではない）**

| パネル | 読む項目 |
|---|---|
| HumanLimit | `HumanBodyBone`、`HumanLimit` |
| SpringBoneCollider | `SpringBoneColliders` |
| Mirror | `MirrorBakeState`、`VertexCount` |
| NormalExcludeSet | `NormalRecalcExcludeList` |
| SpringBone、VrmSettings、PartsId | MeshObject を取得している（読む項目は未確認） |

**(3) 件数だけを読む**

| パネル | 読む項目 |
|---|---|
| SurfaceSnap、NormalTransplant、VertexTransfer、Blend（候補一覧） | `MeshObject.VertexCount` |
| VertexId | MeshObject の有無 |
| Mirror、TempMirrorController、FaceHide、MaterialList、NormalEdit | `Selection.Vertices/Faces.Count` |
| PartsSelectionSet | 選択セットの頂点数・面数 |

**(4) Pipeline テスト系**（Barnacle、BeltGroup、FrillSkirt、MqoToPmx、PmxToMqo、PipeHair、Profile2D、Revolution、RobotBuild、Skin、SpringBone、SpringSkinPipeScenario、SpringSkinScenario）：頂点位置・面・ボーンウェイトを直接走査している。

**この結果から確定すること**
- 上記以外のパネルは、この検索（`.MeshObject`・`.Vertices`・`.Faces`）では検出されなかった。読んでいる項目（一覧・名前・索引・選択など）の個別確認はしていない。
- 頂点・面の詳細が要るのは (1) と (4)。このうち UVEditor は UV・面構成の表示そのものが機能であり、「頂点位置・面構成の詳細は出さない」方針とそのままでは両立しない。
- QuadDecimator・FaceHide は面の走査結果を件数として表示しているだけで、件数を本体側で出せば詳細は不要になる（パネルが面を走査していることは確認済み。表示内容が件数だけかは QuadDecimator のみ確認）。
- MaterialList は同じ操作にパネル内で 2 経路（コマンド／直接書き換え）を持つ。

### 8.4 U4：概要表示ラベルの `UiControl` 登録（2026-09-18）

**方法**：(1) Play 中の本体へ MCP から `queryUiAutomationAudit` を実行。(2) `SubPanels/` 配下の `Label` フィールド宣言の直前行を検索。

**(1) 検査結果**

| 項目 | 値 |
|---|---|
| セクション / パネル / 項目 | 109 / 122 / 2436（未構築 169） |
| 未登録のセクション | 0 |
| 属性の付け忘れ | 14（すべて `PlayerPrimitiveMeshSubPanel._boltCircleBox` / `_boltRectBox` の 7 組） |
| 登録失敗 / 安全度未指定のボタン / 未対応の型 / 未登録の部品 | 0 / 0 / 0 / 0 |

**(2) 概要表示ラベル**
- 4 章の一覧で「読み返す状態・結果」を表示しているラベルは、確認した範囲ですべて `[UiControl("...", Safety = UiSafety.ReadOnly, Description = "...")]` 付きで登録されている（例：Rotate の `targetCount`・`pivot`、SmoothEdges の `stats.segments`・`stats.vertices`、VertexDissolve の `stats.target`・`stats.status`、QuadDecimator の `meshInfo`、MeshList の `detail.vertexCount`・`detail.faceCount` ほか）。
- 例外：`PlayerObjectArraySubPanel._srcCountLabel`（:84）と `_statusLabel`（:123）は直前行に属性が無い。検査（1）の付け忘れには出ていない。理由は未確認。
- PrimitiveMesh 系の一部ラベル（`_placeScaleLabel`、`_p2dBgSizeLabel`、`_revPtLabel` ほか）は `Ignore = true`。

**この結果から確定すること**
- MCP は概要を UI 自動操作（`uiGetValue`）で読める。ただし返るのはラベルの表示文字列（`UiGetValueCommand` の結果 `value` は Text）で、件数などは文字列の中に埋め込まれている（例：`$"選択中: {h.SelectedVertexCount} 頂点  /  除外: {info.SkippedCount} 頂点"`）。構造化した値としては取れない。
- `uiClick` は表示されていないボタンを押せない（`PanelCommand.UiAutomation.cs:119` の説明）。`uiGetValue` は「パネルが表示されていなくても読める」（同 :75 の説明）。

### 8.5 U5：ライブプレビュー中のメッシュ書き換えとロック判定（2026-09-18）

**方法**：Rotate の流れを読んだ（`RotateToolHandler.cs`、`RotateTool.cs`）。他ツールはプレビュー開始の共通フック `EnterTransformDragging` の呼び出し元だけを確認した。

**Rotate の流れ（確認済み）**

| 段階 | 処理 | 確認箇所 |
|---|---|---|
| 開始 | `BeginSliderDrag`：`_isSliderDragging = true`、`ctx.EnterTransformDragging()`（描画の更新モード切替） | `RotateTool.cs:84-91` |
| プレビュー | setter から `UpdatePreview()`。初回に全対象メッシュの開始位置を `_multiMeshStartPositions`（メッシュ索引 → 頂点索引 → 位置）へ記録し、以後モデルの頂点位置を直接書き換える。コマンドも Undo も通らない | `RotateTool.cs:65-67, 351-` |
| 確定 | `CommitViaCommand`：`TryTakeRotationFromDrag` が `RevertToStart()` で開始位置へ戻してから、`RotateSelectionCommand` を `SendCommand` で送る | `RotateToolHandler.cs:80-94`、`RotateTool.cs:133-145` |
| 戻し | `RevertToStart`：記録した (メッシュ索引, 頂点索引) へ開始位置を書き戻す | `RotateTool.cs:531-547` |

**ここから確定すること**
- 確定時に送られるのは通常のコマンドなので、入口で `TryAuthorize` を通せば確定は判定される。拒否された場合も、送信前に `RevertToStart` 済みのためメッシュは開始状態に戻っている。
- ただしプレビュー中の書き換えは判定を通らない。さらに開始位置は「メッシュ索引・頂点索引」で保持しているため、開始から確定までの間に別の操作者が同じメッシュの頂点を動かすと確定時の `RevertToStart` で上書きされ、メッシュの追加・削除・並べ替えで索引がずれると別のメッシュ・頂点へ書き戻される。
- したがってロックは確定時ではなく、プレビュー開始時点で対象メッシュについて取り、確定または取消まで他の操作者の書き込みを止める必要がある。

**プレビュー開始の共通フック `EnterTransformDragging` の呼び出し状況**

| 呼ぶ | 呼ばない（コメントで明記） | 未確認 |
|---|---|---|
| Move、ObjectMove（原点移動を含む）、Rotate、Scale、Sculpt、FaceExtrude、Deform、Lattice、PivotOffset、SkinWeightPaint | EdgeBevel、EdgeExtrude（`EdgeBevelTool.cs:383`、`EdgeExtrudeTool.cs:413`） | SurfaceSnap、Shrink・Blend・NormalTransplant のパネル内プレビュー |

- `EnterTransformDragging` は描画の更新モード切替のためのフックで、ロック用ではない。全ツールのプレビュー開始を網羅していない。

---

## 9. 見直し後の方針と順序（2026-09-18）

### 9.1 方針の追加・修正

| 記号 | 内容 | 根拠 |
|---|---|---|
| A〜G | 5 章のとおり | — |
| D（修正） | 概要は表示文字列ではなく構造化した値（件数・索引・結果コード等）で公開する | 8.4 |
| F（修正） | 同じ機能に「パネル → Core 直接」と「コマンド」の 2 経路があるもの（Export・Import・ProjectFile・Pivot・Capture ほか）は、パネル側をコマンド経路へ寄せて 1 本にする | 8.2 (a) |
| H（新規） | プレビュー中の書き込みロック：プレビュー開始時点で対象メッシュのロックを取り、確定または取消まで他の操作者の書き込みを止める。メッシュリストの構造変更（追加・削除・並べ替え）も止める | 8.5 |
| I（新規） | 対象解決の網羅：全コマンド型について「書き込む対象」を解決できるようにする。手書き switch に載っていない 187 型の扱いを決める | 8.1 |
| J（新規） | パネル内で編集処理を実行しているもの（Shrink・Blend・NormalTransplant・UVEditor・MorphCreate・MaterialList の直接書き換え経路ほか）の処理を本体側へ移す | 8.2 (c)、8.3 |

### 9.2 依存関係（事実）

- G で全経路に `TryAuthorize` を掛けると、I が未了のままでは 187 型がモデル全体判定になり、他人の担当が 1 つでもあるモデルでは本体パネル・MCP からもそれらのコマンドが通らなくなる（8.1）。したがって I は G と同時か先に要る。
- H はロックの判定・保持を G の入口に置く前提。
- E は B〜D と J が済んだパネルから順に行える。

### 9.3 見直し後の順序

1. G：入口の一本化（操作者の名乗り、判定位置の移動）
2. I：対象解決の網羅（G と同じ段で行う）
3. H：プレビュー中の書き込みロック
4. B・C・D：本体側の公開層
5. A・F・J：経路の一本化（クライアントの汎用送信、2 経路の解消、パネル内処理の移設）
6. E：パネルを公開層経由へ置き換え

---

## 10. G（入口の一本化）変更計画

### 10.1 現状（コードで確定）

- 3 経路はすべて `PlayerCommandDispatcher.Dispatch(PanelCommand)`（`PlayerCommandDispatcher.cs:656-698`）に集まる。
  - 本体：`_commandDispatcher?.Dispatch(...)` の直接呼び出しが Core に 76 か所（`DispatchPanelCommand`（`ModelSync.cs:148-151`）経由を含む）。
  - MCP：`PolyLingCommandGateway.Dispatch = cmd => _commandDispatcher.Dispatch(cmd)`（`Lifecycle.cs:362`）。
  - リモート：`RemoteServerCore.ProcessCommandViaPanelCommand` → `TryAuthorize` → `DispatchWithSelectionOf` → `DispatchCommand`（`RemoteServerCore.Messages.cs:304-356`）。`DispatchCommand` の実体は `_commandDispatcher.Dispatch`（`Lifecycle.cs:424-426`）。
- `Dispatch` は入れ子で呼ばれる（`_dispatchDepth`）。一番外側だけが構造署名の控え・手本の記録を行う。
- 所有権判定はリモート経路の `RemoteServerCore.Messages.cs:328` の 1 か所だけ。
- `RemoteOwnership` と `PlayerCommandDispatcher` は同じアセンブリ（`PolyLing.Runtime.asmdef`）にある。
- `SetObjectEditorCommand` の適用は「リモート発は TryAuthorize が弾く、ローカル発は無条件」という前提で force 適用している（`PlayerCommandDispatcher.MeshAttributes.cs:254-257`）。
- 選択の横取り（`selectMesh`）と選択スコープの差し替えはリモート経路だけにある（同 :318-323、:384-407）。

### 10.2 変更案

| # | 対象 | 内容 |
|---|---|---|
| G-1 | 新規 `CommandActor`（Runtime） | 操作者を表す型。名前、種別（本体／リモート／MCP）、封筒で届いた `objectIds` を持つ |
| G-2 | `PlayerCommandDispatcher.Dispatch` | 一番外側の呼び出しで操作者を必須にする。入れ子の呼び出しは外側の操作者を引き継ぐ。一番外側で操作者が無ければ失敗を返す |
| G-3 | 同上 | 一番外側で `RemoteOwnership.TryAuthorize` を呼ぶ。判定の位置を `RemoteServerCore` からここへ移す |
| G-4 | Core の 76 か所 | 本体の操作者（`HostUserName`）を名乗って呼ぶ |
| G-5 | `PolyLingCommandGateway` | MCP の操作者を名乗って呼ぶ |
| G-6 | `RemoteServerCore` | `TryAuthorize` の呼び出しを削除し、要求者と `objectIds` を操作者として渡す。選択の横取りと選択スコープの差し替え、`StaleView` 時の `refreshRequired` 送信は残す（判定結果を受けて行う） |
| G-7 | ログ | 一番外側の実行ごとに操作者名を記録する（表示のみ） |

### 10.3 決定・未決定

- 決定（2026-09-18）：本体（ホスト）の操作にも担当者判定を掛ける。
  - これに伴い、`SetObjectEditorCommand` の「ローカル発は無条件」前提（`PlayerCommandDispatcher.MeshAttributes.cs:254-257`）を見直す。強制解放（`Force`）は本体の操作者だけに許す（現行 `AuthorizeSetEditor` の「リモートからの強制上書きは認めない」を操作者の種別で判定する形にする）。
  - 本体からの PerformUndo / PerformRedo も判定対象になる。対象解決（I）で Undo をどう扱うかを決める必要がある（現状はモデル全体判定）。
- 決定（2026-09-18）：MCP の操作者名は `(mcp)`。選択は現状どおり本体の選択を共有する。

### 10.5 実装の段取り（案・未承認）

振る舞いを変えない段から順に行う。

| 段 | 内容 | 振る舞いの変化 |
|---|---|---|
| 1 | I-1・I-2 の属性定義を追加、I-4 の検査項目を追加 | なし（未宣言の数が検査に出るだけ） |
| 2 | I-5：全コマンド型へ属性を付ける（ファイル単位） | なし |
| 3 | I-3：`ResolveTargets`・`IsOwnershipExempt` を属性から引く処理へ置き換え | リモート経路の判定結果が変わる（187 型がモデル全体判定から外れる） |
| 4 | G-1〜G-7：操作者の導入と判定位置の移動 | 本体・MCP にも判定が掛かる |

**段 1 実施結果（2026-09-18）**
- 変更：`PLCommandAttribute.cs`（`PLWriteScope`、`Writes`）、`PLParamAttribute.cs`（`PLMeshRefAccess`、`MeshRefAccess`）、`PanelCommandMeshRefs.cs`（`MeshRefKey.Access`）、`PanelCommandFactoryAudit.cs`（`[参考]` 2 項目）。
- `PolyLing.Runtime.csproj` のビルド成功（エラー 0）。
- Play 中に `queryCommandAudit` を実行：コマンド 334、`Writes` 未宣言 334（全数と一致）、`MeshRefAccess` 未指定の `IsMeshRef` 引数 234。既存項目（PLParam 付け忘れ 0、action 衝突 0、説明が無い道具 0 ほか）は変化なし。

**段 2 進捗**

| ファイル | Writes | MeshRefAccess | 根拠 |
|---|---|---|---|
| ToolConfirm | 全 11 型 Targets | masterIndices → Write | `ResolveTargets` の既存分類 |
| TopologyTargeted | 全 10 型 Targets | masterIndices → Write、PlaceObjectReshape の原型・SurfaceSnap のリファレンス・頂点へ藤壺の配置元 → Read | 既存分類と `RemoteOwnership.cs` のコメント、コマンドの説明（「配置元オブジェクトを複製し」） |
| Primitive（`PrimitivePlacement`） | — | `AddTargetIndex` → Write | `PLRebuildRole.TargetIndex`（「出力先の masterIndex を書くパラメータ」） |
| Query | SetRawData 以外の 12 型 None、SetRawData は ModelWide | 読む対象 → Read、SetRawData の masterIndex → Write | 説明「モデルは変えない」、ディスパッチャの照会区画（`PlayerCommandDispatcher.Query.cs:35-`）。SetRawData は Scope が Object 以外のとき対象が引数に無い |
| TopologyQuery | 全 3 型 None | → Read | 説明「モデルは変えない」 |
| LineGroup | 全 7 型 Targets | masterIndex → Write | 既存分類 |
| UiAutomation | UiSetValue・UiClick は ModelWide、他 8 型 None | — | UiSetValue・UiClick は「利用者が操作したときと同じ処理を通る」。書き込み先はパネル側の処理で決まり、引数から特定できない |

**段 2 の途中で判明した設計上の事項**
- `Dispatch` の入れ子：UiClick・RunScenario・生成系の委譲などは内側で別のコマンドを `Dispatch` する。G-3 の「一番外側だけで判定」では内側のコマンドが判定を通らない。判定は各段で行い、操作者は外側から引き継ぐ形に改める必要がある。
- `PrimitivePlacement.AddTargetIndex` は「-1 で選択の先頭」（`PanelCommand.Primitive.cs:79`）。-1 のとき書き込み先は実行時の選択で決まり、引数だけでは解決できない。I-3 で扱いを決める必要がある。

**段 2 実施結果（2026-09-18）**
- 32 ファイルの全コマンド型へ `Writes`、`IsMeshRef` 引数へ `MeshRefAccess` を付けた。`PolyLing.Runtime.csproj` ビルド成功。
- Play 中に `queryCommandAudit` を実行：`Writes` 未宣言 9（334 から減少）、`MeshRefAccess` 未指定 4（234 から減少）。既存項目は変化なし。
- 宣言の根拠は、既存の `ResolveTargets`・`IsOwnershipExempt` の分類とコメント、コマンドと引数の説明文、必要な箇所はディスパッチャの処理。対象が引数に無いもの（選択依存・名前指定・モデル単位の設定）は ModelWide とした。

**未宣言だったものの確定（2026-09-18、処理とディスパッチャを読んで確定）**

| 型・引数 | 確定内容 | 根拠 |
|---|---|---|
| ImportPmx/Mqo/Obj/Stl/VrmFile | AddOnly | 読み込み結果は `PlayerLocalLoader.FinishLoad` で `_project.AddModel` により新しいモデルとして足される（`PlayerLocalLoader.cs:145-165`）。既存モデルは書き換えない |
| ApplyThinPlateMorphResult | AddOnly、targetMasterIndex は Read | `ThinPlateMorphOperation.ApplyAsNewObject` は対象を複製して新規追加し、対象は変えない（`ThinPlateMorphOperation.cs:476-521`）。引数の説明「変形結果を書き込む」は実際の処理と一致していない |
| CreateObjectArray | Targets、params.targetMasterIndex は Write | 出力先は `OutputMode` が AsChild なら子として足すだけ、Inside なら出力先の中身へ統合する（`ObjectArrayParams.cs:12-54`）。厳しい側で宣言 |
| SelectDrawablesByName | None | 内側で `SelectMeshCommand` を入れ子で Dispatch するだけ（`PlayerCommandDispatcher.MeshAttributes.cs:179-185`）。SelectMesh と同じ扱い |
| SetActiveWorkAxis | ModelWide、masterIndex は Read | 書くのは `model.ActiveWorkAxisObjectId`（モデル単位の値）だけで、作業軸オブジェクトは変えない（`PolyLingPlayerViewerCore.CreateCommands.Topology.cs:659-674`） |
| BooleanMesh.bMasterIndex | Write | `DeleteSourceB` が真なら B を削除する（`PlayerCommandDispatcher.TPoseMergeMorph.cs:418-422`）。厳しい側で宣言 |

- 確認の途中で、ApplyShrink の目標形状・衝突対象を Read から Write に直した。新規オブジェクト作成時にこれらの `IsVisible` を false にする（`ShrinkOperation.cs:246-252`）。
- 再実行した `queryCommandAudit`：`Writes` 未宣言 0、`MeshRefAccess` 未指定 0。

**段 2 全体の注意**
- Read の宣言の多くはコマンド・引数の説明文と既存分類を根拠にしており、ディスパッチャの処理まで読んで確かめたのは上表と ApplyShrink、MergeMeshes だけである。ApplyShrink のように説明に無い副作用（表示切替など）で参照先を書き換える例があったため、Read 宣言は段 3 の前に処理側で確かめる必要がある。

**Read 宣言 42 件の確認（2026-09-18）**

| 方法 | 対象 | 結果 |
|---|---|---|
| 処理を読んだ | ApplyBlend のソース | `BlendPreviewState.Start` で一時的に隠し、`BlendOperation.ApplyBlend` の末尾 `preview.RestoreVisibility` で戻す（`PlayerCommandDispatcher.Blend.cs:129-153`、`BlendOperation.cs:131-132`）。コマンド終了時点で変化なし。Read のまま |
| 処理を読んだ | DiagnoseBoolean の A・B | `BooleanDiagnostics.Run` へ渡して結果を報告するだけ（`PlayerCommandDispatcher.TPoseMergeMorph.cs:280-310`）。Read のまま |
| 処理を読んだ | 照会系（Query・TopologyQuery） | ディスパッチャの照会区画で書き込みは `SaveSelectionToDataStore` の DataStore への写しだけ（`PlayerCommandDispatcher.Query.cs:341-369`）。Read のまま |
| 処理を読んだ | ApplyThinPlateMorphResult の対象、SetActiveWorkAxis の作業軸 | 前表のとおり。Read のまま |
| 書き込みの型で全体を検索 | 残りの Read 宣言 | `Runtime/` 全体で `.IsVisible = false`、ディスパッチャで `model.RemoveAt(`・`.ClearSelection()` を検索。該当はすべて Write／ModelWide 宣言済みのコマンド（Shrink、Blend のプレビュー、モーフ変換、オブジェクトグループの作り直し、ミラー、MergeMeshes、BooleanMesh、DeleteMeshes）か、新規に作った複製だけで、Read 宣言の参照先へ当たるものは無かった |

- 未確認：残りの Read 宣言（生成・複製の元、法線移植・頂点データ転送・XyzToUv・UvToXyz・QuadDecimate・MediaPipe・Thin Plate の参照元、くさびコンテナ、パーツ ID の参照元、バインド先ボーン、揺れものの位置基準、梯子の取り込み元、穴の頂点数合わせの基準、ObjectArray の複製元）について、表示・削除・選択解除以外の書き込み（頂点属性・名前・親子関係など）が無いことは、処理関数を 1 件ずつ読んでは確かめていない。

**Read 宣言の 1 件ずつの確認（2026-09-18、続き）**

| 引数 | 結果 | 根拠 |
|---|---|---|
| DuplicateMeshes.masterIndices | Read 確定 | `MeshContextCloneOps.Clone` で複製して `model.Add` するだけ（`PlayerCommandDispatcher.MeshAttributes.cs:606-637`） |
| ApplyNormalTransplant の before・after | Read 確定 | `NormalTransplantOperation.ComputeSamples` はワールド座標を読むだけで、書き込みは対象（targetMasterIndices）のサンプルに限られる（`NormalTransplantOperation.cs:70-139`、`PlayerCommandDispatcher.DeformSkin.cs:36-71`） |
| ApplyThinPlateMorph の before・after・source | Read 確定 | `ComputeWarpedLocalPositions` で算出し `ApplyAsNewObject` で複製へ書く。フォルダ内の頂点書き込みは複製（`newCtx.MeshObject`）だけ（`ThinPlateMorphOperation.cs:498`） |
| MediaPipeFaceDeform.sourceMasterIndex | Read 確定 | 位置を配列へ写し、`srcMesh.Clone()` へ書いて新規追加（`PlayerCommandDispatcher.DeformSkin.cs:413-436`） |
| QuadDecimate.sourceMasterIndex | Read 確定 | `QuadPreservingDecimator.Decimate` は先頭で `source.Clone()` を作り結果を返す（`QuadPreservingDecimator.cs:25-28`）。結果は新規追加 |
| UvToXyz.masterIndex | Read 確定 | `UvUnwrapOps.BuildUvzMesh` で新しいメッシュを作り追加（`PolyLingCore_UvHandlers.cs:69-86`） |
| XyzToUv.sourceMasterIndex | Read 確定 | Undo のスナップショットと `UnityMesh` の作り直しは target だけ（`PolyLingCore_UvHandlers.cs:97-128`） |
| TransferVertexData.sourceMeshIndices | Read 確定 | Undo 記録と同期は転送先だけ（`PlayerCommandDispatcher.NormalEdit.cs:249-283`） |
| ConvertToSkinned.bindBoneMasterIndex | Read 確定 | ボーンは `Depth` を読むだけ。書き換えは対象側の `HierarchyParentIndex`・`Depth`・`BindPose`（`SkinKindConverter.cs:316-319`）。ただし変換で索引が動きうる（ディスパッチャのコメント `PlayerCommandDispatcher.DeformSkin.cs:327`）ため、構造変更として H の対象になる |

- 未確認（次回）：くさびコンテナ、パーツ ID の参照元、図形生成の取り込み元・配置元、梯子の取り込み元、藤壺の配置元、揺れものの位置基準、PlaceObjectReshape の原型、頂点へ藤壺の配置元、SurfaceSnap のリファレンス、穴の頂点数合わせの基準、ObjectArray の複製元。

| 引数 | 結果 | 根拠 |
|---|---|---|
| ApplyObjectPoseWedges.containerMasterIndex | Read 確定 | コンテナは `ObjectPoseWedgeReader.ResolveContainer`・`CollectSubtree`・`Read` で読むだけ。適用先の検索でもコンテナ配下を除外（`PlayerCommandDispatcher.ObjectOrigin.cs:556-622`） |
| AssignPartsIds.referenceMasterIndex | Read 確定 | 「1 パーツの頂点数を取るためだけに読む。書き換えない」とあり、`VertexCount` を読むだけ（`PlayerCommandDispatcher.NormalEdit.cs:95-115`） |
| MatchHoleRingCount.baseMeshIndex | Read 確定 | `HoleRingCountTool.Execute` の Undo 取得・書き込み対象は `_target.MeshIndex` とそのミラー側だけ（`HoleRingCountTool.cs:297-321`） |
| CreateObjectArray.sourceMasterIndices | Read 確定 | `ObjectArrayGenerator.BuildSources`・`Generate` で生成物を作るだけで、モデルへの書き込みは生成物の追加（`PolyLingPlayerViewerCore.ObjectArrayBridge.cs:50-86`） |

- 判明：MatchHoleRingCount は対象の**ミラー側オブジェクト**も書き換える（`MirrorBranchOps.CollectMirrorCaptureIndices`、`HoleRingCountTool.cs:311-317`）。ミラー側は引数に現れないため、I-3 の対象解決では「引数の対象 ＋ そのミラー側」を書き込み先に含める必要がある。他のコマンドでミラー側へ伝播するものは未調査。
- 未確認（次回）：図形生成の取り込み元・配置元、梯子の取り込み元、藤壺の配置元、揺れものの位置基準、PlaceObjectReshape の原型、頂点へ藤壺の配置元、SurfaceSnap のリファレンス（この 2 件は `RemoteOwnership.cs:294-295` のコメントに「読むだけ」とあるが処理は未読）、SetBoneParent の newParent。

| 引数 | 結果 | 根拠 |
|---|---|---|
| CreatePrimitiveMeshCommand.profileSourceIndex | Read 確定 | 名前で参照しているのは `ObjectGroupOps.cs:562-566` の引数の読み出しだけで、実行経路で対象オブジェクトに触れる箇所は検索で見つからない |
| CreateBoltPattern・CreatePlaceObject・CreateVertexBillboardPlace の配置元 | Read 確定 | 索引は `ResolvePlaceSourcesForCommand` → `MeshSourceMultiPick.Resolve` で `MeshObject` の一覧へ引くだけ（`PolyLingPlayerViewerCore.CreateCommands.cs:132, 240-243`、`VertexBillboardPlaceToolHandler.cs:185`） |
| 梯子系の取り込み元（BeltSourceIndex） | Read 確定 | `ResolveBeltSourceForCommand` で `MeshObject` を引き、ウェイト引き継ぎの突き合わせに使う（`PolyLingPlayerViewerCore.CreateCommands.cs:245-255`） |
| PlaceSpringBoneChains.originMasterIndex | Read 確定 | `SpringBoneChainPlacer.Place` は基準の `WorldMatrix` を読むだけで、「親にはしない」と明記（`SpringBoneChainPlacer.cs:77-126`） |

- 図形生成・梯子系の配置元・取り込み元は、生成関数へ `MeshObject` の参照として渡る。生成関数の中で参照先を書き換えていないことは、生成関数を 1 件ずつは読んでいない。
- 未確認（次回）：PlaceObjectReshape の原型、SurfaceSnap のリファレンス、SetBoneParent の newParent。

| 引数 | 結果 | 根拠 |
|---|---|---|
| PlaceObjectReshape.prototypeMasterIndices | Read 確定 | 原型は頂点位置の列（`IReadOnlyList<Vector3>`）として `PlaceObjectReshapeOps` へ渡り、書き込みは対象の `mo` だけ（`PlaceObjectReshapeOps.cs:123-276`） |
| SurfaceSnap.referenceMasterIndices | Read 確定 | リファレンスは書き込み対象の列挙から除外され（`SurfaceSnapTool.cs:214`）、`GetWorldPositions` で位置を読むだけ（同 :299-305） |
| SetBoneParent.parentMasterIndex | **Write → Read に修正** | 新しい親は `WorldMatrix` の位置を読むだけ。書き換えるのは子側の `HierarchyParentIndex`・`BoneTransform.Position`（`PlayerCommandDispatcher.SpringBone.cs:288-327`） |

- Read 宣言の 1 件ずつの確認はこれで全件終了。修正は ApplyShrink の 2 引数（Read→Write）と SetBoneParent の 1 引数（Write→Read）。`PolyLing.Runtime.csproj` ビルド成功。

**段 3-1 実施結果（2026-09-18）**
- `PLParamAttribute.WriteWhen`（"キー=値1|値2"、入れ子は入れ子の中のキー）、`MeshRefKey.WriteWhen`、`PanelCommandFactory.TryParseWriteWhen`・`IsWriteActive`・`InvalidWriteWhens` を追加。`queryCommandAudit` に「書き込み条件の不正」を完了条件として追加。
- `WriteWhen` を付けた箇所：MergeMeshes の元・基準（`createNewMesh=false`）、BooleanMesh の A（`createNewMesh=false`）・B（`deleteSourceB=true`）、ApplyBlend の宛先（`createNewObject=false`）、ObjectArray の出力先（`outputMode=Inside`）、梯子の元（`paintWeights=true`）、`PrimitivePlacement.AddTargetIndex`（`addMode=AddToExisting|ReplaceExisting`。`ReplaceExisting` はオブジェクトグループの作り直し用で、`PlayerPrimitiveMeshSubPanel.cs:37-51`）。
- 段 2 の誤りを修正（既存 `ResolveTargets` の分類 `RemoteOwnership.cs:350-422` との食い違い）：AddSpringBoneTailBone・PlaceSpringBoneChains → AddOnly（参照 Read）、梯子の付ける先 → Read、SelectBoneChain・SelectBonesByVertexWeight・SelectElements・AdvancedSelect・AdvancedSelectByAttribute → None（参照 Read）、ExportVrmAnimation・ExportVmdToVrma → None。梯子の `chainRootMasterIndices` は説明に「指定するとボーンを作らず位置だけ更新する」とあるため Write のまま。
- ビルド成功。`queryCommandAudit`：`Writes` 未宣言 0、`MeshRefAccess` 未指定 0、`WriteWhen` の不正 0。

**段 3-2 実施結果（2026-09-18、動作確認は未実施）**
- `RemoteOwnership.cs`：手書きの `ResolveTargets`・`IsOwnershipExempt`・`One` を削除。`WriteScopeOf`（`PLCommand.Writes` を引く）と `TryCollectWriteTargets`（Write 引数を `ToArgs` の値と `IsWriteActive` で集め、別モデル指定はモデルごとに分ける。負数を含めば false）を追加。
- `TryAuthorize` の流れ：UI 自動操作・手本の拒否 → `SetObjectEditor` 専用判定 → None は許可 → 名前未登録の拒否 → AddOnly は許可 → Targets 以外（ModelWide・未宣言）はモデル全体判定 → 対象が集められなければモデル全体判定 → `VerifyObjectIds`（主モデルの対象、ミラー追加前の並び）→ 各モデルで `MirrorBranchOps.CollectMirrorCaptureIndices` によりミラー側を加えて担当者判定。
- 旧実装からの振る舞いの差（コードから確定するもの）：
  - 名前未登録の拒否より前に許可するものが、旧「IsOwnershipExempt の照会系」から「Writes = None 全体」に広がった。選択系（SelectElements ほか）は旧実装では名前未登録なら拒否、新実装では許可。
  - 旧実装でモデル全体判定に落ちていた 187 型のうち Targets・AddOnly・None と宣言したものは、対象単位の判定か許可になる。
  - 書き込み先にミラー側オブジェクトを含める（旧実装は含めていなかった）。
  - SetWorkAxis・RecallWorkAxis は旧実装の「担当と無関係」から対象単位の判定になる（段 2 で意図して変更）。
- `PolyLing.Runtime.csproj` ビルド成功。リモート経路での判定結果の確認は未実施。

**判定の照会コマンドと確認（2026-09-18）**
- 追加：`QueryOwnershipVerdictCommand`（action `queryOwnershipVerdict`、Writes = None）。照会対象を `targetAction`・`targetArgKeys`・`targetArgValues`（手本の段と同じ形。値に配列を入れるときは `"0,1"` のように引用符で囲む。`string[]` は `SplitQuotedCsv` で読む：`PanelCommandFactory.cs:468`）から組み立て、実行せずに `RemoteOwnership.TryAuthorize` の結果と集めた対象を返す（`PlayerCommandDispatcher.Query.cs` の `DispatchQuery` 冒頭）。
- 修正：`ToArgs` は enum を整数の文字列で出す（`PanelCommandFactory.cs:628-631`）。`IsWriteActive(Type, MeshRefKey, args)` で条件の enum 名を整数へ直して比べるようにした。修正前は図形生成の「既存へ追加」が担当者判定を通っていた。
- MCP からの確認（立方体 index 0 の担当者 alice）

| 照会 | 要求者 | 結果 |
|---|---|---|
| toggleVisibility 0 | bob / alice | 拒否（担当: alice）／許可 |
| deleteMeshes 1（担当者なし） | bob | 許可 |
| deleteMeshes "0,1" | bob | 拒否。対象 [0,1] |
| duplicateMeshes 0 | bob | 許可（AddOnly） |
| createCube 既定 ／ addMode=NewObject | bob | 許可 |
| createCube addMode=AddToExisting・addTargetIndex=0 | bob | 拒否 |
| applyBlend 宛先 0・createNewObject=false ／ true | bob | 拒否 ／ 許可 |
| toggleVisibility 0・objectIds=123（食い違い） | alice | 拒否（staleView=true） |
| resolveTJunctions masterIndex=-1 | bob | モデル全体判定で拒否 |

- 未確認：ミラー側オブジェクトを書き込み先へ加える処理（ミラー構成のモデルで未照会）。リモートクライアント（別プロセス）経由の実送信。

**ミラー側の確認（2026-09-18）**
- 手順：立方体 index 0（担当 alice）に `setMirrorEnabled`（masterIndices=0, enabled=true）→ モデルの描画オブジェクトが 2 つになり、index 1 がミラー側（名前 `RoundedCube+`）。index 1 の担当を carol に設定。
- 照会：`toggleVisibility` masterIndex=0、要求者 alice → 拒否「RoundedCube+（担当: carol）」。`primaryTargets=[0]`、`checkedTargets=[0,1]`。ミラー側が書き込み先に加わることを確認。
- 判明した事項：ミラー側の追加は Targets の全コマンドに一律に掛けている。ミラー側への書き込み伝播をコードで確認したのは DeleteSelection・FaceMerge・HoleRingCount・Tri4To1・Quad4To1・VertexDissolve（`CollectMirrorCaptureIndices`）と SurfaceSnap・Shrink・Blend（`SyncMirrorSide`）だけで、表示切替などがミラー側を書き換えるかは確認していない。書き換えないコマンドでは、ミラー側の担当者の違いで実際より厳しく拒否される。
- 残り：リモートクライアント（別プロセス）経由の実送信。

**ミラー側への伝播の宣言（2026-09-18）**
- 共通経路の確認：頂点位置は描画同期でミラー側へ写る（`PlayerViewportManager.Renderer.cs:257-292`）、表示・ロックは `ExpandToMirrorPeers` でミラー側へ広げる（`PlayerCommandDispatcher.MeshAttributes.cs:912-982`）、位相変更はミラー側を作り直す、法線・原点は `RebakeDerivedMirror*` がある。上の照会例（表示切替でミラー側の担当者により拒否）は処理と一致していた。
- 方針：既定はミラー側を担当者判定に含め、処理を読んでミラー側へ書かないと確かめたコマンドだけ除外する。`PLCommandAttribute.WritesMirrorSide`（既定 true）と `RemoteOwnership.WritesMirrorSide` を追加し、`TryAuthorize`・照会コマンドで参照する。
- `WritesMirrorSide = false` にしたもの（処理で確認）：RenameMesh（`MeshAttributes.cs:481-512`）、RenameMeshes（同 :514-536）、SetMeshFolding（同 :547-570）、SetPreserveNormals（同 :375-384）、SetVrmFirstPerson（`VrmSettingsOps.SetFirstPerson`、`VrmSettingsOps.cs:129-143`）。いずれも対象の `MeshContext`／`MeshObject` だけを書く。
- 既定（含める）のまま残したもの：SetIgnorePose（`BoneTransform.Rotation` を書くため、姿勢の同期経路を未確認）、SetMeshBillboard（表示位置が変わるため同上）、線分群 7 型（ディスパッチャにミラーの記述は無いが、頂点を動かすものは描画同期で写るため個別に未確認）、ミラー設定系（CycleMirrorType・SetBatchMirrorType・SetMirrorEnabled・SetMirrorBranchRoot）。
- ビルド成功。実行時の照会による確認は未実施。
- 実行時の確認（同日）：立方体 index 0（担当 alice）＋ミラー側 index 1（担当 carol）の構成で、要求者 alice として照会。`renameMesh`（masterIndex=0）→ 許可、`checkedTargets=[0]`（ミラー側を含まない）。`toggleVisibility`（masterIndex=0）→ 拒否「RoundedCube+（担当: carol）」、`checkedTargets=[0,1]`。

### 10.6 段 4（G）変更計画（改訂版・未承認）

10.2 の案を、段 2〜3 で判明した事項（入れ子の Dispatch、UI 自動操作・手本の扱い、ホストにも判定を掛ける決定）で改めたもの。

| # | 対象 | 内容 |
|---|---|---|
| G-1 | 新規 `CommandActor`（Runtime） | 名前、種別（Host／Remote／Mcp）、封筒の `objectIds` を持つ。ホストは `HostUserName`、MCP は `(mcp)` |
| G-2 | `PlayerCommandDispatcher.Dispatch` | `Dispatch(PanelCommand, CommandActor)` を入口にする。一番外側で操作者を控え、入れ子の呼び出しでは渡された操作者を無視して外側の操作者を引き継ぐ（MCP の UiClick から呼ばれたパネルの Dispatch は MCP の操作として扱われる）。一番外側で操作者が無ければ失敗 |
| G-3 | 同上 | 各段で `RemoteOwnership.TryAuthorize` を呼ぶ。`objectIds` は一番外側のコマンドにだけ使い、入れ子では照合しない |
| G-4 | `RemoteOwnership.TryAuthorize` | 引数を操作者に変える。UI 自動操作・手本の拒否は種別 Remote のときだけ。`SetObjectEditor` の強制上書きは種別 Host のときだけ許す |
| G-5 | `CommandResult` | 担当者判定のズレ検出（StaleView）を載せる欄を足す。`RemoteServerCore` はこれを見て `refreshRequired` を送る |
| G-6 | Core の直接呼び出し 76 か所と `DispatchPanelCommand` | ホストの操作者で呼ぶ |
| G-7 | `PolyLingCommandGateway` | MCP の操作者で呼ぶ。選択はホストの選択を共有（決定済み） |
| G-8 | `RemoteServerCore` | `TryAuthorize` の呼び出しを削除し、要求者と `objectIds` を操作者として渡す。選択の横取り（`selectMesh`）と選択スコープの差し替えは残す |
| G-9 | ログ | 一番外側の実行ごとに操作者名と種別を記録する |

- 確認：ビルド → Play → MCP から担当者を設定し、MCP（`(mcp)`）の操作が他人の担当オブジェクトで拒否されること、担当者なし・自分の担当では通ること、UI 自動操作が MCP から従来どおり動くこと。

**段 4 実施結果（2026-09-18）**
- G-1〜G-9 を実装（`CommandActor.cs` 新設、`PlayerCommandDispatcher.Dispatch(cmd, actor)`、`RemoteOwnership.TryAuthorize(project, cmd, actor, verifyIds)`、`CommandResult.StaleView`／`FailStale`、Core の `DispatchHost`／`HostActor`、Gateway は `CommandActor.Mcp()`、`RemoteServerCore` は `CommandActor.Remote`）。一番外側から操作者なしで呼ばれた場合は `Debug.LogError` を出して失敗を返す。
- ビルド：PolyLing.Runtime・PolyLing.Editor・Assembly-CSharp とも成功（新規ファイルは Unity の再読込で csproj に入る）。
- Play での確認（MCP 経由）：`setObjectEditor` で他人の名前（alice）→ 拒否「他のユーザー名を設定することはできません」。自分の名前 `(mcp)` → 成功。`toggleVisibility`（担当 `(mcp)` の index 0）→ 成功。`uiDescribe` → 従来どおり応答。照会で要求者 `(host)`・種別 Host の `toggleVisibility` → 拒否「担当: (mcp)」（ホストにも判定が掛かる）。
- 未確認：他人の担当オブジェクトに対する MCP の実操作の拒否（MCP からは他人の名前を設定できないため、担当者の設定をホストの画面操作かリモートクライアントで行う必要がある）。本体パネル操作での拒否表示。リモートクライアント（別プロセス）経由の実送信。操作者なしの Dispatch が起動時・パネル操作で起きていないかのログ確認。
- 判明：`RemoteServerCore.cs:465`・`:500` にリモートから届いたメッシュを `MeshObject` へ直接書き込む経路があり、コマンドも担当者判定も通らない。

**一時ロック化（L-1〜L-5、2026-09-18）**
- 決定：担当者はロックの代わり。作業が終わったら外す。外れるきっかけは本人の解放・切断・一定時間（既定 10 秒）操作なし。目的は「MCP の作業中に人がうっかり触る」ことの防止。
- L-1：`EditorName` を保存・読込しない（`ModelSerializer_ModelContext.cs` の書き出し・読み込み 2 か所、`CsvMeshSerializer.Write.cs`・`.Read.cs`。旧形式の `editorName` 行は読み飛ばす）。`MeshListOps.SetObjectEditor` は Undo に積まず、プロジェクトを変更扱いにしない。
- L-2：`RemoteOwnership` に `LockTimeoutSeconds`（既定 10）、ObjectId → 最終操作時刻の実行時の控え、`TouchLock`・`ReleaseExpiredLocks`・`ReleaseLocksOf`・`RenewLocksAfter` を追加。ディスパッチャは一番外側の実行ごとに期限切れを外し、実行できたコマンドの書き込み先のうち操作者のロックを延ばす。
- L-3：MCP のコマンドが実行できたら、担当者なしの書き込み先（ミラー側を含む）を `(mcp)` でロックする。
- L-4：`RemoteServerCore` の切断時に、その利用者のロックを外して担当の変化を通知する。
- L-5：`PlayerCommandDispatcher.OnOwnershipDenied` を追加し、ホストの操作が止められたら Core の状態表示へ「編集できません（理由）」を出す。
- ビルド成功。Play での確認（MCP）：`createCube` → `toggleVisibility`（index 0）の直後、要求者 `(host)`・種別 Host の照会は拒否「担当: (mcp)」。12 秒待って同じ照会 → 許可（ロックが外れた）。
- 制約：期限切れの判定はコマンドの実行時にだけ行う。何も実行されない間は、リスト表示やリモートへの担当通知に期限切れのロックが残って見える。
- 未確認：本体パネル操作での拒否表示（状態表示）の見え方、リモート切断時の解放。
- 人手での確認（同日）：MCP の表示切替でロックを掛けた直後 10 秒以内に、画面のオブジェクトリストから同じ立方体の表示を切り替え → 止められた（ロックが効いた）ことを利用者が確認。状態表示の文言は画面が小さく目視で確認できなかったが、利用者の判断でこれでよいとした。

**リモートのバイナリ受信経路（R-1〜R-3、2026-09-18）**
- 事実：`RemoteServerCore.ProcessBinaryMessage` はクライアントから届いたバイナリを処理する（呼び出し元 `RemoteServerCore.Push.cs:293-297`）。`MeshData` は新規オブジェクトとして直接足し、`PositionsOnly` は独自の `IsEditableBy` と頂点数だけ見て `MeshObject` へ直接書いていた（コマンド・担当者判定の共通処理・Undo・記録を通らない）。`PositionsOnly` の送り手は PolyLing をクライアントとして動かしたときの頂点移動確定（`PolyLingPlayerViewerCore.VertexInteraction.cs:112-123`、v2 形式）。`MeshData` のクライアント側の送り手はパッケージ内に無い。
- R-1：`PositionsOnly` は届いた位置を写しへ読み、`SetVertexPositionsCommand`（全頂点）を要求者の操作（Remote、安定 ID 付き）として `DispatchWithSelectionOf` で流す。頂点数の食い違いによる拒否は残す。
- R-2：安定 ID の無い旧形式（v1）の「先頭描画メッシュへ当てる」代替動作を廃止し、拒否する（`ResolveBinaryTarget` も対象なしで null を返す）。
- R-3：`MeshData` は残し、`AddGeneratedMeshCommand`（新規オブジェクト、グループ保持なし、姿勢焼き込み済み）を要求者の操作として流す。あわせて `AddGeneratedMeshCommand` の `Writes` を AddOnly から Targets に直した（`PrimitivePlacement` を持ち、追加先モードが既存への追加なら既存を書き換えるため。条件は `AddTargetIndex` の `WriteWhen` で評価される）。
- ビルド成功。リモートクライアント（別プロセス）を使った実送信での確認は未実施。

**A：クライアントのコマンド送信の汎用化（2026-09-18）**
- `PanelCommandRouter`：手書きの switch（対応 25 型、他は捨てる）を廃止し、`PanelCommandFactory.ActionOf` と `ToArgs` で全コマンドを送る。`objectIds` は `RemoteOwnership.TryCollectWriteTargets` の主モデルの書き込み先（サーバの照合と同じ並び）から引く。`ListClientBase` が `GetProject` を渡す。
- `RemoteServerCore.BuildPanelCommand`：`applyBlend` の手書き組み立て（`srcModelIndices` ほか 3 列）を削除し、一般化した経路（`PanelCommandFactory.Create`）に任せる。`selectMesh` の手書きは HTML クライアントの単数 `index` 互換のため残す（`ToArgs` のキー `category`・`indices` と一致）。
- ビルド成功。実送信での確認は未実施。

**F：2 経路の 1 本化（書き出し・読み込み・プロジェクト保存読込、2026-09-18）**
- 事実：コマンド経路は `PLSandbox.TryResolveRead/Write/Folder` で作業フォルダの下に限る。パネル経路は Core の関数を直接呼び、ダイアログで選んだ任意のパスを使っていた。既存の `PLSandbox.AllowOnceFromDialog`（`PLSandbox.cs:150-166`、「利用者が選んだこと自体が許可の表明」、1 回だけ許可）を使うパネルが既にあった（モーション確認・Unity クリップ・VMD→VRMA）。
- 決定：サンドボックスを操作者で掛け分けない。パネルは画面で選んだパスを `AllowOnceFromDialog` で許可してからコマンドを送る。
- 変更：`PolyLingPlayerViewerCore.Layout.Panels.cs` の書き出し 5 形式・読み込み 5 形式・プロジェクト保存（mfproj・CSV）・読み込み（mfproj・CSV）を `DispatchFromPanel(new …FileCommand(…))` に置き換え。PMX 書き出しは材質名の置き換えと元 PMX パス、VRM 書き出しは作者、読み込みは後処理（Humanoid 自動割当・原点 CSV）もコマンドへ渡す。VRM 読み込みでテクスチャを書き出す場合は書き出し先フォルダも許可する。
- `ImportVrmFileCommand` に後処理の 4 引数を追加し、`ExecuteImportVrmFile` は空の `PostOptions` ではなくコマンドの値で組む（他の 4 形式と同じ `TryBuildImportPostOptions`）。
- `PolyLingPlayerViewerCore` に `DispatchFromPanel`・`AllowPanelPath`・`PanelModelIndex` を追加。
- ビルド成功。画面からの書き出し・読み込みの動作確認は未実施。
- 残り（F）：Pivot（`MovePivotToCentroid` と `MovePivotCommand` の処理の同一性が未確認）、Capture（`uiCapture` と `StartCapture` を共有）。
- Pivot（同日）：パネルの「原点を頂点／ボーン重心へ」（`MovePivotToCentroid`）は原点を動かさず全頂点を −Δ ずらしてカメラを逆に動かす処理で、原点そのものを動かす `MovePivotCommand`（`PivotOffset` ハンドラ）とは別の操作だった。寄せ先は `SetVertexPositionsCommand` とし、頂点の直接書き換えと独自の Undo 記録を、全頂点の位置を書くコマンドのホスト操作へ置き換えた。カメラの逆移動はコマンド成功後に行う。Undo の表示名は「頂点位置の書き込み」になる（従来は「Pivot→頂点重心／ボーン重心」）。
- Capture：画面の撮影だけでモデルを書き換えない。パネルと `uiCapture` は同じ `StartCapture` を使っており、判定を通す必要がないため変更しない。
- F はこれで完了。ビルド成功。

**J：パネル内の編集処理の移設（2026-09-18、途中）**
- 事実：`SetCommandContext`／`SetContext` で `PanelContext` を受けるパネルは、本体（`PolyLingPlayerViewerCore.Layout.Panels.cs`）でもリスト用クライアントでも必ず渡されている（MaterialList :276、Blend :123、NormalTransplant :211、UVEditor :258、UVUnwrap :266、Shrink :150、MeshFilterToSkinned :759）。したがって各パネルの「`_panelContext` が無いときにパネル内で直接書き換える」経路は到達しない。
- 削除した直接書き換えの経路：MaterialList（スロット追加・削除・面への適用。不要になった `RecordChange` も削除）、Shrink（確定）、NormalTransplant（確定）、Blend（確定）、MeshFilterToSkinned（変換）。確定はすべて既存のコマンドだけで行う。
- 残り：UVEditor（`PlayerUVEditorSubPanel.Edit.cs:128, 294, 451`）、UVUnwrap（`PlayerUVUnwrapSubPanel.cs:302`）の同種の経路。MaterialList の色・シェーダーの編集（`PlayerMaterialListSubPanel.cs:511-640` 付近）は Unity の Material を直接書き換えており、コマンド（`SetMaterialColorCommand`）を通っていない。スライダーでの連続編集をどう扱うかはプレビュー（H）と同じ問題。
- プレビュー（Shrink・Blend・NormalTransplant のスライダー操作）はパネル内で対象を直接動かしており、H の対象として残る。
- 続き（同日）：UVEditor の確定 3 か所（UV 移動・回転拡大縮小・数値変換）、UVUnwrap（LSCM 展開）、MorphCreate（差分からのモーフ作成）の直接書き換え経路も削除し、既存コマンド（`ApplyUVChangesCommand`・`ApplyLscmUnwrapCommand`・`CreateMorphFromDiffCommand`）だけで確定する形にした。本体はいずれも `SetCommandContext` または `SendCommand = DispatchHost` を渡している（`Layout.Panels.cs:258, 266, 383`）。
- 残った不要コード：MorphCreate の `CreateMorphMeshContext`・`CreateMirrorMorphMeshContext`・`HasDiff`・`GetUndoController`（削除した経路からだけ使われていた）。UVEditor の `RecordTopologyChange`・`ApplyUVTransform` の使用有無は未確認。
- 不要コードの削除（同日）：MorphCreate の上記 4 つと Core 側の `GetUndoController` 代入、UVEditor の `RecordTopologyChange`（Helpers）・`ApplyUVTransform`（Edit）・`GetUndoController`・`GetCommandQueue` と Core 側の代入を削除。ビルド成功。
- BoneEditor（同日）：「ポーズのリセット」はパネル内でポーズ層を直接消して Undo を積んでいた。対応する `ResetBonePoseLayersCommand` は Undo を積んでいなかった（`PlayerCommandDispatcher.BoneMorphUv.cs:151-164`）。コマンド側にパネルと同じ処理（ポーズ層が無ければ作る・消す・`MultiBonePoseChangeRecord` を積む）を移し、パネルはコマンドを送るだけにした。これで MCP からのリセットも Undo できる。BoneEditor の `GetUndoController` と Core 側の代入を削除。ビルド成功。
- モーション確認（`PlayerMotionClipTestSubPanel.cs:465`）・VMD 確認（`PlayerVMDTestSubPanel.cs:500`）：`GetUndoController` は `EditorState.PmxUnityRatio` など設定値を読むためだけに使っており、モデルの書き込みや Undo 記録はしていない。変更しない。
- 図形生成パネル（`PlayerPrimitiveMeshSubPanel.ProfileUndo.cs`・`.BeltProfile.Editor.cs`）：`GetUndoController` はパネル内のプロファイル編集（回転体・2D 押し出し・帯）用のサブウィンドウ Undo スタックに使っており、モデルは書き換えない。変更しない。ただしパネルをクライアントへ出す（E）ときは、このサブウィンドウ Undo を本体の Undo から切り離す必要がある。
- J の残り：MaterialList の色・シェーダー編集（Unity の Material を直接書き換え、`SetMaterialColorCommand` を通らない）。Blend・Shrink・NormalTransplant のプレビュー用 `ToolContext`（H と一緒に扱う）。Pipeline テスト系（`Layout.TestPanels.cs` の `GetUndoController` 代入）は未確認。

**H：プレビュー中の書き込みロック（2026-09-18、途中）**
- 計画：H-1 プレビューの開始・終了口、H-2 各プレビュー開始点での呼び出し（ツール 12 種・Shrink・Blend・法線移植・UVEditor）、H-3 MaterialList の色・シェーダーを操作を離したときにコマンドで確定。
- H-1 実施：`RemoteOwnership.TryBeginPreview(project, modelIndex, targets, actor, out reason)`（対象とミラー側を担当者判定し、通れば担当者なしの対象を操作者のロックにしてプレビュー中は期限で外さない）と `EndPreview(project)`（プレビューのために掛けたロックを外し、元からのロックは期限を数え直す）を追加。`ReleaseExpiredLocks` はプレビュー中の対象を外さない。Core に `TryBeginHostPreview`・`TryBeginHostPreviewOfSelection`（止められたら状態表示へ理由）・`EndHostPreview` を追加。ビルド成功。
- 残り：H-2・H-3。
- H-2 実施（ビューポートの左ボタン操作）：ツールごとにプレビュー開始を止める口は無く、`EnterTransformDragging` の呼び出しはツール内部に 24 か所ある。ビューポートの左ボタンを各ツールへ振り分ける `PlayerVertexInteractor`（押下・押下時追従・クリック・ドラッグ開始／中／終了）に共通の関門を置いた。押下時に `TryBeginPreview`（Core の `TryBeginHostPreviewOfSelection`）を呼び、通らなければその押下の押下時追従とドラッグを止め、クリック（選択）は通す。クリックまたはドラッグ終了で `EndPreview`。ビルド成功。
- H-2 の残り：`PlayerVertexInteractor` を通らない開始経路。オブジェクトモードのドラッグ（`PolyLingPlayerViewerCore.VertexInteraction.cs:466-473`）、要素クリックからの EdgeBevel・EdgeExtrude・FaceExtrude の開始（`PolyLingPlayerViewerCore.InteractionMode.cs:315-352`）、パネルのスライダーによるプレビュー（Rotate・Scale・Deform・Lattice・SurfaceSnap・Shrink・Blend・法線移植）、UVEditor のドラッグ。
- H-2 続き（同日）：ボーン編集パネル表示中のオブジェクトドラッグ（`ConnectBoneEditorObjectMove`）にドラッグ開始時の判定と終了時の解放を入れた。`TryBeginHostPreviewOfSelection` の対象に選択中のボーンも加えた。EdgeBevel・EdgeExtrude・FaceExtrude の開始（`OnDragStartExtra`）で、ヒットした要素のオブジェクトを判定し、止められたら開始しない（押下時の関門は選択だけを見るため、ヒット先が選択外でも判定するよう追加）。止められた後に呼ばれる `OnToolDragExtra`・`OnToolDragEndExtra` を各ハンドラが開始前の状態で無視するかは未確認。ビルド成功。
- H-2 の残り：パネルのスライダーによるプレビュー（Rotate・Scale・Deform・Lattice・SurfaceSnap・Shrink・Blend・法線移植）、UVEditor のドラッグ。
- 確認（同日）：`EdgeBevelTool.OnMouseDrag`・`OnMouseUp` は `_state` で分岐しており（`EdgeBevelTool.cs:140-190`）、開始していない状態でドラッグ・終了が来ても分岐に入らない。EdgeExtrude・FaceExtrude は未確認。
- Rotate（同日）：`RotateToolHandler` に `TryBeginPreview`・`EndPreview` と `BeginSliderDragFromPanel`（止められたらプレビューに入らない。以後の値の代入は保持されるだけでメッシュは動かない）を追加し、確定（`CommitViaCommand`）・終了（`EndSliderDrag`）・取消（`Revert`）でロックを外す。パネル（`PlayerRotateSubPanel.cs` の 9 か所）は `BeginSliderDragFromPanel` を呼ぶ。コマンド実行（`ExecuteFromCommand`）は判定済みなので従来の `BeginSliderDrag` のまま。ビルド成功。
- H-2 の残り：Scale・Deform・Lattice・SurfaceSnap・Shrink・Blend・法線移植・UVEditor。
- Scale（同日）：Rotate と同じ形で実装（`ScaleToolHandler.BeginSliderDragFromPanel`、パネル 14 か所、Core で配線）。
- Deform（同日）：プレビュー開始の共通点 `DeformToolHandler.BeginPreview` に関門を入れた（パネルからの `ApplyPreview` もここを通る）。コマンド実行中（`ExecuteFromCommand`）は `_inCommand` で関門を通さない。開始失敗時と `ExitPreview`（確定・取消の共通の終わり）でロックを外す。Core で配線。
- ビルド成功。
- H-2 の残り：Lattice・SurfaceSnap・Shrink・Blend・法線移植・UVEditor。
- Lattice（同日）：頂点を動かし始める `BeginDeform` に関門を入れた（配置段階 `BeginPlacement` は頂点を動かさないので対象外）。コマンド実行（`ExecuteFromCommand`）の間は `_inCommand` で通さない。セッション終了（`EndSession`：確定・取消の共通の終わり）と開始失敗でロックを外す。Core で配線。ビルド成功。
- H-2 の残り：SurfaceSnap・Shrink・Blend・法線移植・UVEditor。
- SurfaceSnap（同日）：`SurfaceSnapToolHandler.TriggerCompute`（パネルの「計算」＝プレビュー開始）に関門を入れ、プレビューに入れなかったとき・取消（`TriggerCancel`・`CancelIfActive`）でロックを外す。コマンド実行（`ExecuteFromCommand`）の中は関門を通さないよう `_tool.TriggerCompute()` を直接呼ぶように変更（コマンド実行は冒頭と finally で `CancelIfActive` を呼ぶので、そこでパネルのロックも外れる）。Core で配線。ビルド成功。
- H-2 の残り：Shrink・Blend・法線移植・UVEditor。
- Shrink（同日）：パネル（`PlayerShrinkSubPanel`、頂点版と面版の 2 インスタンス）に `TryLockForPreview`（`Func<IList<int>, bool>`）・`UnlockAfterPreview` を追加。プレビュー開始（`_preview.Start`）の前に変形元・目標形状・衝突対象を判定してロックし、止められたら状態欄に「他の操作者が作業中のため開始できません」。開始失敗と `EndPreview`（確定前・取消の共通の終わり）でロックを外す。Core で両インスタンスに `TryBeginHostPreview`・`EndHostPreview` を配線。ビルド成功。
- H-2 の残り：Blend・法線移植・UVEditor。
- 法線移植・Blend（同日）：Shrink と同じ形（`TryLockForPreview`・`UnlockAfterPreview`・`_previewLocked`）。法線移植は対象（`_targetIndices`）、Blend は宛先と隠すソース（`BuildHideIndices`）を、プレビュー開始（`_preview.Start`／`_blendPreview.Start`）の前に判定してロックし、開始失敗と `EndPreview` で外す。Core で配線。ビルド成功。
- H-2 の残り：UVEditor。
- UVEditor（同日）：UV 移動の開始（`BeginUVMove`）とハンドル操作の開始（`BeginUVHandle`）で、編集中のオブジェクトを判定してロックする。止められたら記録を空のままにして、以後のドラッグで UV を動かさない（状態欄に「他の操作者が作業中のため編集できません」）。終了（`EndUVMove`・`EndUVHandle`）は本体を `…Core` に分け、`finally` でロックを外す。Core で配線。ビルド成功。
- **H-2 はこれで一通り完了。**
- H-3 色（同日）：MaterialList のメインカラーのスライダー（`MakeColorRGBARow`）は、操作中は画面上の Material と見本だけを変え、離したとき（`PointerCaptureOutEvent`）・フォーカスを外したとき（`FocusOutEvent`）に `SetMaterialColorCommand` を送って確定する。パネルでの `MaterialData.SetBaseColor` と `MarkDirty` の直接呼び出しは削除（コマンド側が永続データと変更扱いを行う）。送信後に永続データの色と食い違っていれば（担当者判定で止められた場合）、画面の色と見本を永続データへ戻す。スライダーの位置は戻していない。リスト用クライアントでは永続データの更新がサーバからの通知まで遅れるため、確定直後に一度元の色へ戻って見える（未確認）。ビルド成功。
- H-3 の残り：シェーダー変更（対応するコマンドが無い）。
- H-3a・H-3b（同日）：`Poly_Ling.Materials.MaterialEditOps`（`Runtime/Poly_Ling_Main/Core/Misc/MaterialEditOps.cs`）を新設し、パネル内にあった材質の補助処理（色・メインテクスチャの取得と設定、半透明判定、ブレンド設定の書き込み、`MaterialData` への反映）と確定操作（`ApplyColor`・`ApplySurface`・`ApplyShader`・`ApplyTextureFile`）を移した。コマンド `SetMaterialShaderCommand`・`SetMaterialSurfaceCommand`・`SetMaterialTextureCommand`（いずれも Writes = ModelWide。テクスチャはパスを `PLSandbox.TryResolveRead` で解決）を追加し、ディスパッチャに受け口を追加。`SetMaterialColorCommand` の受け口も `ApplyColor` を使う形にした。ビルド成功。
- H-3c の残り：パネル（`PlayerMaterialListSubPanel`）のシェーダー変更・不透明／半透明・テクスチャ参照をコマンド送信へ置き換え、パネル内の同じ補助関数を削除する。
- H-3c（同日）：パネルのシェーダー変更（`ApplyShaderType`）・不透明／半透明（`OnSurfaceOpaque`・`OnSurfaceTransparent`）・テクスチャ参照（`OnBrowseTexture`。選んだパスは `PLSandbox.AllowOnceFromDialog` で許可）をコマンド送信へ置き換え、パネル内の補助関数（`GetMaterialColor`・`GetMainTexture`・`SetMainTexture`・`EnsureData`・`WriteSurface*`・`SyncSurfaceToData` ほか）を削除。見た目の更新に使う `IsTransparent`・`SetMaterialColor` は `MaterialEditOps` へ委ねる。テクスチャは送信後に材質の実物を見て設定できたかを表示する。ビルド成功。
- 判明：Metallic・Smoothness のスライダー（`PlayerMaterialListSubPanel.cs:352-389`）も Material と `MaterialData` を直接書き換え、コマンドを通らない（対応コマンドなし）。色と同じ形（操作中は見た目だけ、離したらコマンド）にする必要がある。
- Metallic・Smoothness（同日）：`MaterialScalarKind`・`MaterialEditOps.SetScalar`／`ApplyScalar`・`SetMaterialScalarCommand`（Writes = ModelWide）と受け口を追加。パネルのスライダーは操作中は見た目だけ変え、離したとき・フォーカスを外したときに送信（`RegisterScalarCommit`）。止められて永続データが変わらなければ、材質とスライダーを永続データへ戻す。ビルド成功。
- Play での確認（MCP）：`createCube` の後、`setMaterialScalar`（slot 0, Metallic, 0.8）→ 成功、`setMaterialSurface`（slot 0, transparent）→ 成功、`setMaterialShader`（Custom, "NoSuchShader"）→ 拒否「シェーダーが見つかりません: NoSuchShader」。材質の表示と永続データの値そのものは照会していない。画面からのパネル操作は未確認。
- **H はこれで完了（MaterialList の画面操作の実確認を除く）。**

**B・C・D：ツールの公開層（2026-09-18、試験導入）**
- 追加：属性 `PLTool`（クラス、ツール名）・`PLToolParam`（読み書きプロパティ）・`PLToolAction`（引数なしメソッド）・`PLToolState`（読み取り専用の概要）（`PLToolAttributes.cs`）。名前での読み書き・呼び出し `PLToolSurface`（値の文字列変換は `PanelCommandFactory.TryParseValue`／`TryFormatValue` として公開した既存規則）。コマンド `QueryToolStateCommand`・`SetToolParamCommand`・`InvokeToolActionCommand`（いずれも Writes = None）と受け口。ディスパッチャの `ResolveTool`・`ListTools` を Core の登録簿（`ToolRegistry`：型に `[PLTool]` の付いたハンドラのフィールドを列挙）へ配線。
- 試験：`RotateToolHandler` に `[PLTool("rotate")]`、パラメータ 15、概要 2（`pivotPublic`・新設の `affectedCount`）、操作 2（`revert`・`commitViaCommand`）を付けた。
- Play での確認（MCP）：`queryToolState`（toolId 省略）→ `["rotate"]`。`setToolParam`（rotate, rotX, 30）→ `"30"`。`queryToolState`（rotate）→ パラメータ名・型・現在値（rotX=30）、概要、操作の一覧を返した。
- 判明：enum の値は `TryFormatValue` が整数で出す（例 magnetFalloff=4）。名前で読めるかは未確認。
- 残り：BCD-4（検査項目）、Scale・SmoothEdges・AdvancedSelect への試験導入、その後の全ハンドラへの展開。
- 続き（同日）：BCD-4 として `PanelCommandFactoryAudit.RunAll` に「ツール（PLTool）数・名前の重複・扱えない型」を追加。Scale（パラメータ 12、概要 `affectedCount`、操作 `revert`・`commitViaCommand`）、SmoothEdges（パラメータ 6、概要 5、操作 `refreshStats`）に属性を付けた。説明文はコードで意味を確かめていない項目は元のメンバー名を書いた（例 "ScaleTool.ScaleAxisX（パネルの軸欄 X）"）。ビルド成功。
- 残り：AdvancedSelect、Play での確認、その後の全ハンドラへの展開。
- AdvancedSelect（同日）：パラメータ 7（Mode ほか。説明は既存の doc コメント）、概要 2（`isAttributeMode`・新設の `shortestPathFirstVertex`）、操作 1（`clearShortestPathFirst`）。`ClearAllSelection`・`InvertSelection`・`ExecuteBoundaryEdgeInSelection` は選択をコマンドを通さずに書き換えるかを確認していないため、操作として公開していない。
- Play での確認（MCP）：`queryToolState` → `["advancedSelect","smoothEdges","rotate","scale"]`。`setToolParam`（advancedSelect, mode, "EdgeLoop"）→ `"2"`（enum は名前で受け付け、整数で返す）。`queryCommandAudit` → 「ツール（PLTool） 4、名前の重複 0、扱えない型 0」。コマンド数 342、既存項目はすべて 0。
- 残り：残りのハンドラ（計 35 のうち 31）への展開。
- 展開（同日、途中）：faceMerge（パラメータ 1・概要 1）、quad4To1・tri4To1・vertexDissolve（概要 1）、vertexHole（パラメータ 1・概要 1）、mergeVertices（パラメータ 3。`PreviewInfo` は構造体のため対象外）、alignVertices（パラメータ 4・概要 4）。説明文は元のメンバー名、または既存 doc コメントの文言。ビルド成功。これで 11 ツール。
- 残り：EdgeBevel・EdgeExtrude・FaceExtrude・Knife・PipeAlign・PlaceObjectReshape・PlanarizeAlongBones・EdgeTopology・EdgeBridge・Solidify・Lattice・Deform・AddFace・Camera・WorkAxis・PointDefined・Move・Sculpt・SkinWeightPaint・ObjectMove・PivotOffset・SurfaceSnap・HoleRingCount・EdgeRibbonFace・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・PrimitivePlace・VertexBillboardPlace。
- 展開の続き（同日）：edgeBevel（パラメータ 4）、edgeExtrude（3）、faceExtrude（4）、knife（パラメータ 4・概要 4。`Segment` は `VertexPair` のため対象外）、pipeAlign（パラメータ 9・概要 1）、placeObjectReshape（パラメータ 3・概要 2）、planarizeAlongBones（パラメータ 4・概要 2。`BoneNames` は `string[]`）。ビルド成功。これで 18 ツール。
- 残り：EdgeTopology・EdgeBridge・Solidify・Lattice・Deform・AddFace・Camera・WorkAxis・PointDefined・Move・Sculpt・SkinWeightPaint・ObjectMove・PivotOffset・SurfaceSnap・HoleRingCount・EdgeRibbonFace・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・PrimitivePlace・VertexBillboardPlace。
- 展開の続き（同日）：edgeTopology（パラメータ 1）、edgeBridge（パラメータ 4・概要 3）、solidify（パラメータ 9・概要 2）、lattice（パラメータ 1・概要 4。`Grid` は対象外）。ビルド成功。これで 22 ツール。
- 残り：Deform・AddFace・Camera・WorkAxis・PointDefined・Move・Sculpt・SkinWeightPaint・ObjectMove・PivotOffset・SurfaceSnap・HoleRingCount・EdgeRibbonFace・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・PrimitivePlace・VertexBillboardPlace。
- 展開の続き（同日）：deform（パラメータ 5・概要 3。`PreviewContext` は対象外）、addFace（パラメータ 6）、camera（パラメータ 3）、workAxis（パラメータ 4）。ビルド成功。これで 26 ツール。
- 残り：PointDefined・Move・Sculpt・SkinWeightPaint・ObjectMove・PivotOffset・SurfaceSnap・HoleRingCount・EdgeRibbonFace・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・PrimitivePlace・VertexBillboardPlace。
- 展開の続き（同日）：pointDefined（パラメータ 3・概要 4。`Picks`・`HoverPick` は対象外）、move（パラメータ 7）、sculpt（パラメータ 6。Min／Max の範囲設定は対象外）。ビルド成功。これで 29 ツール。
- 残り：SkinWeightPaint・ObjectMove・PivotOffset・SurfaceSnap・HoleRingCount・EdgeRibbonFace・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・PrimitivePlace・VertexBillboardPlace。
- 展開の続き（同日）：surfaceSnap（パラメータ 4・概要 3）、edgeBridge に取りこぼしの `BoundaryEdgeOnly` を追加。ビルド成功。これで 30 ツール。
- 判明：属性を付けられる公開プロパティを持たないハンドラがある。SkinWeightPaint は公開プロパティが無い。ObjectMove は設定を `GetSettings()` が返す設定オブジェクト（`ObjectMoveSettings`）と public フィールド `DragSelectMode` で持つ。EdgeRibbonFace（`Settings`）・PrimitivePlace（`Settings`）も設定オブジェクト型。これらは `PLToolSurface` がプロパティしか扱わないため、そのままでは公開できない。
- 残り：PivotOffset・HoleRingCount・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・VertexBillboardPlace（未調査）と、上の設定オブジェクト型の扱い。
- 設定オブジェクト型への拡張（同日）：`PLToolParam` をフィールドにも付けられるようにし、`PLToolSettings`（プロパティまたは引数なしメソッドに付ける。返した参照型の public フィールドと読み書きできるプロパティを "設定名.メンバー名" で公開）を追加。`PLToolSurface` を「持ち主とメンバー」の組で読み書きする形に書き直し、検査（`Unformattable`）も設定オブジェクトの型の中まで見る。適用：objectMove（`GetSettings()` を settings、フィールド `DragSelectMode`）、edgeRibbonFace（`Settings`）、primitivePlace（`Settings`）。ビルド成功。これで 33 ツール。Play での確認は未実施。
- 残り：PivotOffset・HoleRingCount・LineExtrude・DeleteSelection・FlipFace・SplitVertices・BillboardProfile・VertexBillboardPlace（未調査）、SkinWeightPaint（公開プロパティなし）。
- 展開の続き（同日）：billboardProfile（public フィールドのパラメータ 6・概要 3）、holeRingCount（パラメータ 1。`BaseSeed`・`TargetSeed` は構造体のため対象外）、lineExtrude（public フィールドのパラメータ 9）。ビルド成功。これで 36 ツール。
- 残り：PivotOffset・DeleteSelection・FlipFace・SplitVertices・VertexBillboardPlace（未調査）、SkinWeightPaint（公開プロパティなし）、Play での確認。
- 展開の完了（同日）：deleteSelection（概要 1、新設 `DeletableCount`）、vertexBillboardPlace（概要 1、新設 `SelectedVertexCount`）。PivotOffset・FlipFace・SplitVertices・SkinWeightPaint は公開プロパティ・フィールドを持たず、公開するものが無い（設定はパネル側にあるかは未調査）。
- Play での確認（MCP）：`queryToolState` → 38 ツール。`queryToolState`（objectMove）→ `dragSelectMode`（フィールド）と `settings.*` 9 項目を返した。`setToolParam`（objectMove, settings.pickBones, true）→ `"true"`、（lineExtrude, thickness, 0.25）→ `"0.25"`。`queryCommandAudit` → 「ツール（PLTool） 38、名前の重複 0、扱えない型 0」。既存項目はすべて 0。
- **B・C・D の展開はこれで一通り完了。** 残る課題：概要・パラメータの変化時の通知（push）は E で必要になった時点で追加。enum 値は整数で返る。

### 10.7 E（パネルを公開層経由へ）

**E-1 洗い出し（2026-09-18、途中：34 パネルのうち 21）**

分類：A＝公開層で足りる／B＝属性を足せば足りる（公開していない読み取り・引数なし操作、構造体の中の数値）／C＝仕組みの追加が要る（引数あり操作、プレビュー、多態の設定、構造体・リストをそのまま使う）。

| パネル | A 以外で使っているもの | 分類 |
|---|---|---|
| AddFace | `ClearPointsPublic()`、`PlacedPointCount`、`RequiredPointsPublic`、`GetPointLabels()`（List<string>） | B |
| AdvancedSelect | なし（`GetShortestPathFirstVertex()` は概要 `shortestPathFirstVertex` で代替可） | A |
| AlignVertices | `TriggerAutoSelect()`、`SelectedVertexCount`、`GetAlignTarget()`（Vector3） | B |
| BillboardProfile | `FinishChain()`、`SetSelectedConstraint(bool, HandleConstraint)`、`ChainPoints.Count`・`FreeformPoints.Count`・`SelectedPoints.Count` | C（引数あり操作） |
| Deform | `Phase`、`RequestGizmoRefresh()`、`Deformer.Params`（デフォーマごとに型が違う）、`ApplyPreview()`・`Revert()`、`PreviewContext`（構造体） | C（多態の設定・プレビュー） |
| EdgeBevel・EdgeExtrude・EdgeTopology・FaceExtrude | なし | A |
| EdgeBridge | `Inspect()`（構造体） | B |
| FaceMerge・Quad4To1 | `Inspect()`（構造体。除外数など） | B |
| HoleRingCount | `BaseSeed`・`TargetSeed`（構造体）、`Inspect()` | B |
| Knife | `Segment`（VertexPair）、`StageText()` | B |
| Lattice | `SetCells(x,y,z)`・`SetBounds(...)`、`Mode`、`Grid.CellsX` ほか | C（引数あり操作） |
| LineExtrude | `AnalyzeLoops()`、`SelectedLineCount`、`DetectedLoopCount`、`GetLoopSummaries()`（リスト） | B（リストは C） |
| MergeVertices | `PreviewInfo`（構造体） | B |
| PipeAlign | `TargetMeshCount` | B |
| PlaceObjectReshape | `CollectSelectedPartsIdText()`（文字列を返す） | B |
| PlanarizeAlongBones | `RebuildBoneList()`、`GetBoneWorldPosition(int)` | C（引数あり） |
| Rotate | `BeginSliderDragFromPanel()`（プレビュー）、`GetTotalAffectedCount()` | C（プレビュー） |

- 共通して判明：AlignVertices・MergeVertices・PipeAlign・LineExtrude・PlaceObjectReshape・PlanarizeAlongBones・AdvancedSelect の各パネルは、ハンドラの値を読んで確定コマンドを自分で組み立てている。値を名前で読めれば組み立ては変えずに済む。
- 残り 13 パネル（Scale・Sculpt・SmoothEdges・Solidify・SplitVertices・SurfaceSnap・Tri4To1・VertexDissolve・VertexHole・VertexMove・WorkAxis・Camera ほか）は未洗い出し。

**E-1 洗い出しの続き（同日、完了）**

| パネル | A 以外で使っているもの | 分類 |
|---|---|---|
| Scale | `BeginSliderDragFromPanel()`（プレビュー）、`GetTotalAffectedCount()` | C（プレビュー） |
| Sculpt | `Min/MaxBrushRadius`・`Min/MaxStrength`、`IsRadiusDragMode` と `OnRadiusChanged`・`OnRadiusDragModeExited`（ビューポートで半径を決める操作のコールバック） | C（ビューポート操作） |
| SmoothEdges・Solidify・Camera | なし | A |
| FlipFace | ハンドラを使っていない | A |
| SplitVertices | `SelectedVertexCount`・`GetSplittableCount()`（ハンドラに `[PLTool]` が無い） | B |
| SurfaceSnap | `TriggerCompute()`・`SetSlider()`・`TriggerCancel()`・`CancelIfActive()`（プレビュー）、`IsReference(idx)`・`PruneReferences(...)`・`ReferenceIndices`（引数あり・リスト）、`Model`（候補一覧をモデルから直接読む）、`TargetMeshCount` | C（プレビュー・引数あり・モデル直読み） |
| Tri4To1・VertexDissolve・VertexHole | `Inspect()`（構造体の除外数） | B |
| VertexMove | `GizmoScreenOffsetX/Y`、`Min/MaxMagnetRadius`（公開済み）、`IsRadiusDragMode` とコールバック、`GetTotalAffectedCount()` | C（ビューポート操作） |
| WorkAxis | `SnapToBone`・`SnapToObject` | B |

**E-1 のまとめ（34 パネル）**
- A（9）：AdvancedSelect・EdgeBevel・EdgeExtrude・EdgeTopology・FaceExtrude・SmoothEdges・Solidify・Camera・FlipFace
- B（16）：AddFace・AlignVertices・EdgeBridge・FaceMerge・Quad4To1・Tri4To1・VertexDissolve・VertexHole・HoleRingCount・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude（ループ一覧のみ C）・SplitVertices・WorkAxis
- C（9）：Rotate・Scale・Deform・SurfaceSnap（プレビュー）／BillboardProfile・Lattice・PlanarizeAlongBones・SurfaceSnap（引数あり操作）／Sculpt・VertexMove（ビューポートで半径を決める操作のコールバック）／Deform（デフォーマごとに型が違う設定）／SurfaceSnap（モデルの直読み）
- C の仕組みの種類は 5 つ：プレビュー、引数あり操作、ビューポート操作のコールバック、多態の設定、モデルの直読み。

**E-2・E-4 実施（同日、途中）**
- E-2：`Poly_Ling.Data.IToolSurface`（`TryGet`・`TrySet`・`Invoke`）と型付きの補助 `ToolSurfaceExtensions`（`Get<T>`・`GetFloat`・`GetInt`・`GetBool`・`GetString`・`Set<T>`）を追加（`IToolSurface.cs`）。`PLToolSurface.TryGetValue`（1 項目だけ読む）を追加。本体側の実装 `HostToolSurface`（Player/View/Core）：読み取りは登録簿のハンドラから直接、設定・操作は `SetToolParamCommand`／`InvokeToolActionCommand` をホストの操作として送る。Core の `ToolSurface` プロパティで生成。
- E-4：EdgeBevel パネルを置き換え（`GetH` を削除し `IToolSurface Surface` とツール名 "edgeBevel" で読み書き）。Core の配線を `Surface = ToolSurface` に変更。ビルド成功。画面での動作確認は未実施。
- 残り：SmoothEdges の置き換え、2 パネルでの動作確認（重さを含む）、その後 A の残り 7 パネル。
- SmoothEdges（同日）：同じ形で置き換え（ツール名 "smoothEdges"、統計の再計算は操作 `refreshStats` を呼ぶ）。これに合わせて `InvokeToolActionCommand` の受け口から、パネルへの通知を外し Undo に記録しないようにした（パネルの再描画から `refreshStats` を呼ぶため、通知すると再描画と操作が回り続ける）。ビルド成功。
- Play での確認（MCP の UI 自動操作）：`uiSetValue`（smoothEdges.strength = 0.7）→ `queryToolState`（smoothEdges）の strength = 0.7。`uiSetValue`（edgeBevel.amount = 0.3）→ `queryToolState`（edgeBevel）の amount = 0.3。パネルの操作が窓口経由でハンドラへ届くことを確認。スライダーのドラッグ中の重さは MCP から確かめられず未確認。
- 残り：A の残り 7 パネル（Solidify・EdgeExtrude・EdgeTopology・FaceExtrude・AdvancedSelect・Camera・FlipFace）。
- EdgeExtrude・EdgeTopology・FaceExtrude（同日）：同じ形で置き換え（ツール名 "edgeExtrude"・"edgeTopology"・"faceExtrude"。enum の設定は `Surface.Set(Tool, "mode", 値)` で整数へ変換して送る）。Core の配線を変更。ビルド成功。画面での確認は未実施。
- 残り：Solidify・AdvancedSelect・Camera・FlipFace。
- Camera・FlipFace（同日）：Camera は対象の種類とギズモの操作を窓口経由に（ツール名 "camera"）。FlipFace はハンドラを使っていなかったので `GetH` と Core の代入を削除。ビルド成功。これで 7 パネル。
- 残り：Solidify・AdvancedSelect。
- Solidify（同日）：置き換え（ツール名 "solidify"。入力欄は `Surface.Set` が返す設定後の値で表示を戻す）。追加先ドロップダウンの既定値の設定は Refresh から呼ばれるため、値が変わるときだけ送る形にした（毎回送ると設定 → 通知 → Refresh の繰り返しになる）。ビルド成功。これで 8 パネル。
- 残り：AdvancedSelect。
- AdvancedSelect（同日）：置き換え（ツール名 "advancedSelect"）。E-1 の洗い出しで拾えていなかった操作（`GetHandler?.Invoke()?.X()` の形）が 4 つあった：`ExecuteBoundaryEdgeInSelection`・`ClearAllSelection`・`InvertSelection`・`ClearShortestPathFirst`。前 3 つはハンドラ内で選択状態を直接書き換える（全解除は Undo なし、他は選択の Undo を積む：`AdvancedSelectToolHandler.cs:157-163, 262-275, 414-427`）。選択はモデルの書き込みではなく、`InvokeToolActionCommand` の中で動けば選択スコープの差し替えも効くため、`[PLToolAction]` を付けて窓口の操作として呼ぶ形にした。ビルド成功。**A の 9 パネルはこれで完了。**
- Play での確認（MCP の UI 自動操作）：`uiSetValue`（solidify.thickness = 0.35）→ ハンドラの thickness = 0.35。`uiSetValue`（faceExtrude.individualNormals = true）→ individualNormals = true。
- 判明：E-1 の洗い出しは `GetH()?.X`・`h.X` の形だけを探しており、`GetHandler?.Invoke()?.X` の形を取りこぼしていた。B・C のパネルを置き換えるときは、各パネルの `GetH`／`GetHandler` の使用箇所をすべて読み直す。

**B のパネル（同日、途中）**
- 公開層の追加：`PLToolStateGroup`（構造体・オブジェクトで返す概要を "名前.メンバー名" の概要として公開。プロパティまたは引数なしメソッドに付ける）。`PLToolSurface` に概要の読み取り対象をまとめる `StateSlots`、まとまりを 1 回の取得で読む `ReadGroup` を追加し、検査も中身の型まで見る。`IToolSurface.GetGroup(toolId, group)` と取り出しの補助 `Item<T>` を追加（下調べは取得のたびに計算が走るので、項目ごとに読み直さないため）。
- `Inspect()` に `[PLToolStateGroup(Name = "inspect")]`：FaceMerge・Quad4To1・Tri4To1・VertexDissolve・VertexHole・HoleRingCount・EdgeBridge。
- Quad4To1 パネルを置き換え（下調べは `Surface.GetGroup("quad4To1", "inspect")` で 1 回だけ読む）。ビルド成功。
- 残り（B）：Tri4To1・VertexDissolve・VertexHole・FaceMerge・EdgeBridge・HoleRingCount・AddFace・AlignVertices・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude・SplitVertices・WorkAxis。
- Tri4To1・VertexDissolve・VertexHole（同日）：Quad4To1 と同じ形で置き換え（下調べは `GetGroup(..., "inspect")` で 1 回だけ読む。VertexHole の比率は "ratio"）。ビルド成功。B は 4 パネル完了。
- 残り（B）：FaceMerge・EdgeBridge・HoleRingCount・AddFace・AlignVertices・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude・SplitVertices・WorkAxis。
- FaceMerge・EdgeBridge（同日）：置き換え。EdgeBridge は `ClearPicks` に `[PLToolAction]` を付け、下調べ（`inspect.ok`・`inspect.message`）と拾った辺の数・拒否理由を窓口から読む。ビルド成功。B は 6 パネル完了。
- 残り（B）：HoleRingCount・AddFace・AlignVertices・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude・SplitVertices・WorkAxis。
- HoleRingCount（同日）：`BaseSeed`・`TargetSeed` に `[PLToolStateGroup]`（"baseSeed"・"targetSeed"）、`ImportBase`・`ImportTarget`・`ClearSeeds` に `[PLToolAction]` を付けて置き換え。ビューポートの起点マーカー用にパネルが実装している `IHoleSeedSource` の値（メッシュ・頂点・向き）も窓口のまとまりから読む形にした（本体のビューポートがパネル経由で起点を読む結合は残っている）。ビルド成功。B は 7 パネル完了。
- 未確認：起点（`HoleRingCountTool.Seed`）の中に文字列にできない型のメンバーがあるか（検査の「扱えない型」で確かめる）。
- 残り（B）：AddFace・AlignVertices・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude・SplitVertices・WorkAxis。
- SplitVertices・WorkAxis（同日）：SplitVertices は `[PLTool("splitVertices")]` と概要 2（`selectedVertexCount`・新設の `splittableCount`）を付けて置き換え。WorkAxis は `SnapToBone`・`SnapToObject` に `[PLToolParam]` を付け、吸着・回転スナップ・サブモードを窓口経由に（WorkAxis パネルは本体に 2 つあり、両方の配線を変更）。ビルド成功。B は 9 パネル完了。
- 残り（B）：AddFace・AlignVertices・Knife・MergeVertices・PipeAlign・PlaceObjectReshape・LineExtrude。
- MergeVertices（同日）：結合候補（`MergePreviewInfo`、グループごとの頂点番号の一覧を持つ）はそのまま公開せず、ハンドラに概要 3 つを新設：`previewGroupCount`・`previewVerticesToMerge`・`previewGroupLines`（パネルの詳細表示と同じ文言の行：先頭 5 グループ、各グループの頂点数と先頭 8 個の頂点番号、残り件数）。パネルはこれを読む。設定 3 つと確定コマンドの組み立ても窓口経由に。ビルド成功。B は 10 パネル完了。
- 未確認：`string[]` の概要を `Surface.Get<string[]>` で読み戻したとき、カンマを含む行（"1, 2, 3"）が 1 行のまま戻るか（PanelCommandFactory の配列の書き出し・読み取りの規則に依存）。
- 確認（同日、コード）：`string[]` の書き出しは各要素をカンマ・引用符・改行を含むとき二重引用符で囲み（`PanelCommandFactory.EscName`、:680-686）、読み取りは二重引用符に対応した分割（同 :688-）。カンマを含む行も 1 行のまま戻る。
- 残り（B）：AddFace・AlignVertices・Knife・PipeAlign・PlaceObjectReshape・LineExtrude。
- PipeAlign（同日）：`TargetMeshCount` に `[PLToolState]` を付けて置き換え（モード・方向・端の扱いは enum を窓口で読み書き、確定コマンドの組み立ても窓口から）。ビルド成功。B は 11 パネル完了。
- 残り（B）：AddFace・AlignVertices・Knife・PlaceObjectReshape・LineExtrude。
- PlaceObjectReshape（同日）：パネルの「選択頂点から取得」はハンドラの文字列を受け取って対象テキストへ入れていたため、ハンドラに操作 `CollectSelectedPartsIdIntoTarget`（集めて `TargetText` へ入れる）を新設し、パネルは操作を呼んでから `targetText` を読む形にした。モード・λ・対象テキスト・確定コマンドの組み立て・実行可否も窓口経由。原型（材料）の一覧はパネル内の `_srcPick` がモデルから作っており、ハンドラを使っていない。ビルド成功。B は 12 パネル完了。
- 残り（B）：AddFace・AlignVertices・Knife・LineExtrude。
- Knife（同日）：通過線分（`VertexPair`）の代わりに概要 `segmentV1`・`segmentV2`、状態説明テキストの概要 `stage` をハンドラに新設して置き換え（モード・等分割・分割数・三角＋四角も窓口経由）。ビルド成功。B は 13 パネル完了。
- 残り（B）：AddFace・AlignVertices・LineExtrude。
- AlignVertices（同日）：ハンドラに概要 `selectedVertexCount`・新設の `alignTarget`（Vector3）、操作 `triggerAutoSelect` を付けて置き換え（揃える軸・基準・標準偏差・プレビュー表示・確定コマンドの組み立ても窓口経由）。ビルド成功。B は 14 パネル完了。
- 残り（B）：AddFace・LineExtrude。
- AddFace（同日）：ハンドラに概要 `placedPointCount`・`requiredPointsPublic`・新設の `pointLabels`（string[]）、操作 `clearPointsPublic` を付けて置き換え（モード・連続線・線分群の延長・吸着 3 種も窓口経由）。追加先・マテリアルのドロップダウンは元からハンドラを使わず Core の関数（`GetMeshEntries` ほか）で作っている。ビルド成功。B は 15 パネル完了。
- 残り（B）：LineExtrude。
- LineExtrude（同日）：ハンドラに概要 `selectedLineCount`・`detectedLoopCount`・新設の `loopLines`（パネルの一覧と同じ文言の行）、操作 `analyzeLoops` を付けて置き換え（厚み・倍率・オフセット・Y 反転・前後の分割数とエッジサイズ・内向きエッジ・確定コマンドの組み立ても窓口経由）。ビルド成功。**B の 16 パネルはこれで完了。**
- Play での確認（MCP）：`queryCommandAudit` →「ツール（PLTool） 39、名前の重複 0、扱えない型 0」（起点 `HoleRingCountTool.Seed` の中身を含む）。既存の検査項目はすべて 0。
- E の残り：C の 9 パネル（Rotate・Scale・Deform・SurfaceSnap・BillboardProfile・Lattice・PlanarizeAlongBones・Sculpt・VertexMove）。

**C のパネル（同日、途中）**
- C-1 の仕組み：`PlayerCommandDispatcher.CurrentActor`（実行中コマンドの一番外側の操作者）を公開し、Core の `TryBeginHostPreview` はコマンド実行中ならその操作者の名前でプレビューのロックを掛ける（窓口の操作として MCP・リモートから呼ばれた場合）。
- `SetToolParamCommand` の受け口からパネルへの通知を外した。スライダーのドラッグ中は 1 目盛りごとに設定が送られ、通知でパネルの Refresh が毎回走ると、読み取りに計算を伴う概要（Rotate の `pivotPublic` は読み取り時に回転中心を計算し直す）をプレビュー中に読み直すため。この結果、MCP から `setToolParam` した値は、別の通知が来るまで表示中のパネルに反映されない（変化の通知 push で扱う）。
- Rotate・Scale：`BeginSliderDragFromPanel` に `[PLToolAction]` を付けて置き換え（スライダーは「プレビュー開始の操作 → 値の設定」、確定は `commitViaCommand`、取消は `revert`）。ビルド成功。C は 2 パネル完了。画面でのドラッグ動作は未確認。
- 残り（C）：Deform・SurfaceSnap・BillboardProfile・Lattice・PlanarizeAlongBones・Sculpt・VertexMove。
- C-2 の仕組み（同日）：`InvokeToolActionCommand` に `ArgKeys`・`ArgValues`（既定は空）を追加し、`PLToolSurface.TryInvoke` は引数ありのメソッドも呼べるようにした（引数はメソッドの引数名で対応づけ、値は PanelCommandFactory の規則で変換。渡されなかった引数は既定値、無ければ失敗）。`queryToolState` の操作一覧は引数ありなら "名前(引数名,...)" と出す。`IToolSurface.InvokeWith(toolId, action, keys, values)` と型付きの補助 `Invoke(toolId, action, params (key, value)[])` を追加。ビルド成功。
- Lattice のハンドラ（同日）：`Mode`（パラメータ）、`SetCells(x,y,z)`・`SetBounds(center,size)`（引数ありの操作）、`BeginPlacement`・`FitToSelection`・`BeginDeform`・`ResetDeformation`・`Cancel`・`CommitViaCommand`（操作）、格子の分割数・中心・大きさ（概要 `gridCellsX/Y/Z`・`gridBaseCenter`・`gridBaseSize`）を公開。ビルド成功。パネルの置き換えは未実施。
- Lattice・PlanarizeAlongBones・BillboardProfile のパネル置き換え（同日）：Lattice は上の公開分で置き換え。PlanarizeAlongBones はハンドラに概要 `boneWorldPositionA/B`・操作 `rebuildBoneList` を追加。BillboardProfile はハンドラに操作 `finishChain`・`deleteSelectedPoints`・`smoothSelectedPoints`・新設の `setSelectedConstraintValues(isOut, direction, length, ratio, lengthGroupId)`（拘束の構造体を値ごとに受け取る）、概要 `chainPointCount`・`freeformPointCount`・`selectedPointCount` を追加。ビルド成功。C は 5 パネル完了。
- 残り（C）：Deform・SurfaceSnap・Sculpt・VertexMove。
- Sculpt・VertexMove・SurfaceSnap・Deform（同日）：
  - Sculpt・VertexMove：ハンドラに `IsRadiusDragMode`・半径／強度の上下限・ギズモのオフセット（パラメータ）、影響頂点数（概要）を追加。パネルは型付きの薄い包み（`SubPanels/Edit/ToolSurfaceViews.cs` の `SculptSurfaceView`・`MoveSurfaceView`）で窓口を読み書きする。半径ドラッグの通知（`OnRadiusChanged`・`OnRadiusDragModeExited`）はパネルが登録するのをやめ、Core がハンドラ生成箇所（`PolyLingPlayerViewerCore.VertexInteraction.cs`）でパネルの Refresh へ配線。
  - SurfaceSnap：操作 `compute`・`cancel`・`cancelPreviewIfActive`・`setReferenceOn(meshIndex,on)`・`pruneReferencesToCandidates`、パラメータ `sliderValue`（プレビュー中だけ反映）、概要 `referenceIndexArray`・`candidateIndices`・`candidateNames`・`candidateVertexCounts` を追加。候補一覧は計画（C-5）の「本体側の関数から受け取る」ではなく、ハンドラ内で従来と同じ条件（頂点を持つ Mesh・BakedMirror・MirrorSide）で作って概要として出す形にした。
  - Deform：`Phase`（パラメータ）、`[PLToolSettings(Name="params")] DeformerParams`（今のデフォーマの設定。型はデフォーマごとに違う）、`deformerDisplayName`・`previewContext`（概要のまとまり）、操作 `selectDeformer(name)`・`applyPreview`・`revert`・`commitViaCommand`・`requestGizmoRefresh`・`resetParams` を追加。パネルは `DeformSurfaceView` で、今のデフォーマ名から `DeformerRegistry.Create` で同じ型の入れ物を作って `params.*` を詰めた写しを読み、書くときは写しを変えて変わった項目だけ送る。
  - ビルド成功。`queryToolState`（deform）で `phase`・`params.angleX/Y/Z/euler`・`previewContext.*`・操作一覧を確認。
- **E（34 パネル）はこれで完了。** `View/` 配下に `Func<XxxToolHandler>` は残っていない。
- E の残る課題：
  - 変化の通知（push）が無い。`setToolParam`・`invokeToolAction` はパネルへ通知しないため、MCP・リモートから変えた値は別の通知が来るまで表示中のパネルに反映されない。
  - パネルが本体側の関数でモデルを直接読んでいる箇所：AddFace の追加先・マテリアル一覧、PlaceObjectReshape の原型一覧（`_srcPick`）、Solidify の追加先一覧、HoleRingCount の起点マーカー（ビューポートがパネル経由で起点を読む `IHoleSeedSource`）。
  - 画面での実操作（スライダーのドラッグ、半径ドラッグ）は未確認。
- P（変化の通知、同日）：`PlayerCommandDispatcher.OnToolChanged`（ツール名）を追加。`setToolParam`・`invokeToolAction` が成功し、操作者がホスト以外（MCP・リモート）のときだけ呼ぶ。Core は受けたら表示中のセクション（`_sectionRefreshPairs` のうち display が Flex のもの）を Refresh する。ホストの画面操作では呼ばない（ドラッグ中の再描画の多発を避けるため）。ビルド成功。
- 確認（MCP）：EdgeBevel パネルが非表示の状態で `setToolParam`（edgeBevel.amount = 0.4）→ `uiGetValue` は 0.1 のまま（非表示のセクションは対象外のため）。表示中のパネルでの反映は未確認。
- E 残り（生 Context を読むパネル）第 1 弾：`IModelView.ActiveMeshIndex` を追加（Live・Player・Summary の 3 実装、Summary は SummaryBuilder で設定）。Core に `ActiveProjectView`（`PlayerProjectView` を都度作る）を追加。Edit 系 16 パネル（SmoothEdges・AlignVertices・FlipFace・LineExtrude・MergeVertices・PlanarizeAlongBones・Solidify・SurfaceSnap・FaceMerge・PipeAlign・Quad4To1・Tri4To1・VertexDissolve・VertexHole・AdvancedSelect・SplitVertices）の `GetView` を `Func<IProjectView>` に変更（読んでいたのは模型索引・選択中の描画オブジェクト・編集対象メッシュの索引のみ）。ビルド成功。
- 第 2 弾：Scenario（模型索引のみ）。第 3 弾：`IModelView.ActiveMesh`（本体は `LiveMeshView` で現物を読む。Summary は各リストから引く）、`IMeshView.HiddenFaceCount`・`NormalExcludeSets` を追加（`MeshSummary` は選択件数と同じく 0／空を返す＝スナップショットに載せていない）。Core に `LoadedProjectView`（ローカル読み込み、無ければ受信側）を追加。FaceHide・NormalEdit・NormalExcludeSet を窓口経由に。ビルド成功。
- 判明：`PlayerModelView`（本体の `IProjectView`）の各メッシュのビューは `MeshSummary` で、選択件数は常に 0 を返す（`MeshSummary.cs` の明示実装）。選択件数が要るパネルは `ActiveMesh`（現物）を使う。
- 第 4 弾：`IModelView.GetMesh(masterIndex)`（本体は `LiveMeshView`、Summary は各リストから引く）、`IPartsSetView.VertexIdCount`・`HasResolvableVertexIds` を追加。PartsSelectionSet・MeshSelectionSet を窓口経由に（選択辞書は `MeshSelectionSetNames`）。ビルド成功。累計 22 パネル。
- 第 5・6 弾：QuadDecimator・MediaPipeFaceDeform・Mirror を窓口経由に。QuadDecimator・MediaPipeFaceDeform にあったパネルからの直接実行の予備経路（コマンドが送れないとき用）を削除（J の残り）。`IMeshView` に一時ミラーの状態（`IsMirrorBakedState`・実体化前の頂点数／面数・境界の説明）を追加（`MeshSummary` は未実体化として返す）。Mirror の未使用ヘルパー `FindMeshContextByName` を削除。ビルド成功。累計 25 パネル。
- 第 7 弾：TempMirror（コントローラ）・VertexId・PartsId・Boolean・MergeMeshes を窓口経由に。`IMeshView.InspectVertexIds()`・`InspectPartsIds()`（本体でだけ診断を計算、`MeshSummary` は null）を追加。Boolean・MergeMeshes は、選択中の索引（MeshContextList 索引）を DrawableList の位置として引いていたのを `GetMesh(索引)` に改めた（旧コードは描画オブジェクト以外が前にあると別のメッシュを引いていた）。ビルド成功。累計 30 パネル。
- 第 8 弾：VertexTransfer を窓口経由に。`IProjectView.PreviewVertexTransfer(...)`（本体でだけ計算、`ProjectSummary` は null）と結果の写し `VertexTransferPreviewView` を追加。ビルド成功。累計 31 パネル。
- 第 9 弾：ObjectGroup を窓口経由に。`IModelView.ObjectGroups`（`ObjectGroupView`：名前・action・ステップ数・自動更新・要更新・出力先なし・詳細文）を追加。判定と文面はパネルから `LiveProjectView.BuildObjectGroupViews` へ移した（`PlayerModelView` はプロジェクトを受け取って作る。`LiveModelView`・`ModelSummary` は空）。パネルが直接呼んでいた `ObjectGroupOps.PurgeMissing` をコマンド `PurgeObjectGroupsCommand`（ModelWide、Undo 記録なしは従来どおり）へ移した（J の残り）。ビルド成功。累計 32 パネル。
- 第 10 弾：WorkAxis を窓口経由に。作業軸の値は写し `WorkAxisView`（Core の `CurrentWorkAxisView()`、持ち主の作業軸オブジェクトの索引つき）、軸一覧は `IModelView.GetMesh` の `Type == MeshType.WorkAxis`（旧コードは `IsWorkAxis` = 型が WorkAxis かつ WorkAxis 参照あり。参照なしの作業軸型オブジェクトも一覧に出るようになった）、辞書の名前は `IProjectView.WorkAxisLibraryNames`。パネルが直接書き換えていた作業軸ライブラリ（登録・削除・CSV 保存・読込）をコマンド 4 本（`RegisterWorkAxisEntryCommand`・`RemoveWorkAxisEntryCommand`・`SaveWorkAxisLibraryCsvCommand`・`LoadWorkAxisLibraryCsvCommand`、いずれも Writes=None）へ移した（J の残り）。CSV 読込の表示は件数の増減から出す形に変えた（読み飛ばし行数は出さなくなった）。ビルド成功。累計 33 パネル。
- 第 11 弾：PlaceObjectReshape を窓口経由に（累計 34）。MaterialList（M-1〜M-3）：
  - M-1：`IModelView.MaterialCount`・`CurrentMaterialIndex`・`GetMaterialSlot(slot)`（`MaterialSlotView`：名前・シェーダー名・検出したシェーダー種別・色・メインテクスチャ（プロパティ名・名前・本体でだけ Texture2D）・Metallic・Smoothness・表面種別と、保存データ側の色・Metallic・Smoothness）。組み立ては `LiveProjectView.BuildMaterialSlotView`。`ModelSummary` は 0／-1／null。
  - M-2：ツール `materialEdit`（`MaterialEditHandler`、`Runtime/Poly_Ling_Player/View/ToolHandlers/`）を追加。操作 `previewColor(slot,r,g,b,a)`・`previewScalar(slot,kind,value)`（見た目だけ）、`commitColor`・`commitScalar`（`SetMaterialColorCommand`・`SetMaterialScalarCommand` を送り、見た目を保存データへ揃える）、`revertPreview(slot)`。パネルは `Material` を直接触らない。プレビューはロックを取らない（見た目だけで、確定のコマンドに担当者判定が掛かる）。
  - M-3：`SetCurrentMaterialSlotCommand`（ModelWide。現在スロットの切り替えと `AutoSetDefaultMaterials` 時の既定マテリアルの控え）を追加。パネルの `IsDirty`・`OnListChanged`・`SyncMesh` 呼び出しを削除（各受け口が行っている）。
  - `ApplyMaterialToFacesCommand`：面の索引が空なら受け口が対象メッシュの今の選択面を使う（PLParam の Required を外した）。
  - クライアント：`ClientToolSurface`（`SetToolParamCommand`・`InvokeToolActionCommand` を送る。読み取りは未対応で TryGet は false）を追加し、`MaterialListClient` を `GetView`（`PlayerProjectView` でクライアントの写しを包む）と `Surface` に配線。
  - Play：`queryCommandAudit` で ツール 40・扱えない型 0・各検査 0。MCP から `invokeToolAction`（materialEdit.commitScalar）が成功することを確認。画面操作は未確認。累計 35 パネル。
- 第 12 弾：Morph（モーフエクスプレッション）を窓口経由に。パネルが直接書き換えていた属性変更・削除・CSV 読込・エントリのウェイト変更をコマンド（`SetMorphExpressionAttributesCommand`・`DeleteMorphExpressionCommand`・`SetMorphEntryWeightsCommand`・`ImportMorphCsvCommand`・`ExportMorphCsvCommand`）へ移した（Undo 記録も受け口へ。`PlayerCommandDispatcher.RecordMeshListUndo` を追加）。`MorphCsvIO` をダイアログ付き（従来）とパス指定（`ImportFromPath`・`ExportToPath`）に分け、ダイアログだけの `AskImportPath`・`AskExportPath` を追加。試し表示とウェイト調整中のプレビューはツール `morphExpression`（`MorphExpressionHandler`。`startPreview`・`applyPreview`・`endPreview`・`previewEntryWeight`・`commitEntryWeights`・`revertEntryWeights`）で行う（M-2 と同じ方式）。ウェイト調整は従来「操作中もデータを直接書き、離したときに Undo を記録」だったが、「操作中は表示だけ、離したときにコマンドで確定」に変わった。`IModelView.MorphExpressions`（`MorphExpressionView`・`MorphEntryView`）を追加（`ModelSummary` は空）。ビルド成功。累計 36 パネル。
- 第 13 弾：MorphCreate を窓口経由に。パネルが直接行っていた「モーフ → モデル展開」（新規モデルの追加）を `ExpandMorphExpressionToModelCommand`（Writes=None。今のモデルは変えない）へ移した（J の残り）。累計 37 パネル。
- 第 14 弾：Blend を窓口経由に。プレビュー（`BlendPreviewState`）・担当者判定とロック・ソースの一時非表示・対応率の集計・対応方式の注意書きをツール `blend`（`BlendToolHandler`。`preview`・`endPreview`・`inspectMatchMode`、概要 `isPreviewing`・`statsLines`・`statsWarn`・`matchModeHint`）へ移した。GPU 同期・可視の書き戻し・ロックの配線もパネルからハンドラへ移した。パネルは宛先・ソース・重みの選択だけを持ち、候補は `IModelView.GetMesh`（Type・VertexCount）から作る。`SetModel(ModelContext)` は `SetModel()` に変更。対応率の文の色は「対象頂点なし」も未対応ありと同じ橙になった（従来は赤）。Play：MCP から `blend.preview`（宛先 0・ソース 0・重み 0.5）→ `isPreviewing=true`・`statsLines=対応 168 / 168（100.0%）`、`endPreview` 成功を確認。累計 38 パネル。
- MCP の制約（事実）：`invokeToolAction` の `argValues` はカンマ区切りの 1 文字列なので、配列引数は要素 1 個のときしか渡せない（パネルからの呼び出しは string[] で渡すので影響なし）。
- 第 15 弾：BoneEditor を窓口経由に（`GetModel` を `Func<IModelView>` に）。書き込みは元からコマンド（TRS スライダーは `BeginBoneTransformSliderDrag`・`SetBoneTransformValue`・`EndBoneTransformSliderDrag`）なので、M-2 の方式への変更は不要と判断した（パネルは値を直接書いていない）。読み取りのため `IMeshView` にポーズ層 "Manual" の有無・移動量・回転、`WorldPosition` を追加（`MeshSummary` は 0）。TRS 欄は `LocalPosition`・`LocalRotationEuler`・`LocalScale`（= BoneTransform）から読む。パネルがモデルを読んで書き出していた原点 CSV を `ExportObjectOriginsCsvCommand`（Writes=None）へ移した。動作差：BoneTransform が null のメッシュは従来 TRS 欄 0／相対操作を飛ばしていたが、窓口は 0・0・1 を返すので相対操作の基準値として 0 を使う。ビルド成功。累計 39 パネル。
- 第 16 弾：TPose・HumanoidMapping を窓口経由に。`IModelView` に `HumanoidMappingCount`・`HasAnySkinWeight`・`HasTPoseBackup`・`DiagnoseTPose()`・`HumanoidMissingRequiredCount`・`AvatarRetarget`（`AvatarRetargetView`）を追加（`ModelSummary` は 0／false／空／null）。両パネルにあった「コマンドが送れないとき用の直接実行の予備経路」（Tポーズ適用・復元・破棄、マッピング適用・クリア）を削除（J の残り）。TPose の原点 CSV 書き出しは `ExportObjectOriginsCsvCommand` に `IncludeBones`・`BakeRotationToPosition` を足して使う（件数・除外数の表示は出さなくなった）。HumanoidMapping のリターゲット欄の読み直し判定は「同じ ModelContext か」から「モデル索引と名前が同じか」に変わった（窓口の写しは毎回作るため）。ビルド成功。累計 41 パネル。
- 第 17 弾：HumanLimit・SpringBoneCollider・SpringBone を窓口経由に。`IMeshView` に可動域（`HumanBodyBone`・`IsHumanLimitCarrier`・`HasCustomHumanLimit`・`HasHumanLimit`・`HumanLimitMin/Max/Center`・`HumanLimitAxisLength`）、揺れもの（`IsSpringBoneCarrier`・`SpringBoneColliders`（`SpringBoneColliderView`）・`HasSpringBoneChainRoot`・`SpringBoneChainName`・`SpringBoneChainCenterBone`・`SpringBoneChainGroupIndices`）を追加。`IModelView.SpringBoneColliderGroupNames` を追加。パネルがモデルに直接書いていた 3D 表示の状態（当たり判定の表示・強調、鎖の強調）をツール `springBoneColliderDisplay`（`show`・`clear`）・`springBone`（`refreshSummary`・`updateHighlight`・`clearHighlight`、概要 `chainRows`・`chainMasters`・`issueRows`・`issueMasters`・`fixedDeltaTime`・`warmupFrames`・`placeText`）へ移した。鎖の一覧・検査結果はハンドラが本体側で組み立てる（`SpringBoneOps`・`MeshHierarchyOps` を読むため）。Play：MCP から `springBone.refreshSummary`・`queryToolState`、`springBoneColliderDisplay.show`（`isDisplayed=true`）・`clear` を確認。累計 44 パネル。
- 第 18 弾：SkinWeightNumeric・VrmSettings を窓口経由に。SkinWeightNumeric は読み取り（選択頂点の共通ウェイトの取り込み、ウェイト合計の検査、対象オブジェクトと選択頂点数）をツール `skinWeightNumeric`（`SkinWeightNumericHandler`。`gather`・`checkSums`、概要 `gatheredBones`・`gatheredWeights`・`gatherError`・`sum*`・`targetNames`・`targetSelectedVertexCounts`）へ移した。VrmSettings は `IModelView` に `HasVrmMeta`・`VrmMetaCopy`・`HasVrmLookAt`・`VrmLookAtCopy`（`VrmSettingsOps.GetMetaOrNew`・`GetLookAtOrNew` の写し）、`IMeshView` に `VrmFirstPerson`・`IsVrmFirstPersonCarrier` を追加。欄の読み直し判定は「モデル索引と名前が同じか」に変わった。ビルド成功。累計 46 パネル。
- 第 19 弾：MotionClipTest を窓口経由に。クリップ（`MotionClipDTO`）・`MotionClipApplier`・読み込み検査結果をパネルからツール `motionClip`（`MotionClipHandler`。`load(path,sourceKind,time)`・`clear`・`resetPose`・`setTime`・`setPositionScale`・`loadBind`、概要 `clipLoaded`・`error`・`clipName`・`clipLength`・`frameRate`・`trackCounts`・`matchedTrackCount`・`positionScale`・`reportText`・`trackLines`・`trackMatched`・`trackTotal`・`bindBoneCount`）へ移した。VMD の位置倍率（`PmxUnityRatio`）・表示同期・再描画の配線もハンドラへ。パネルは時刻・再生位置の UI だけを持つ。ビルド成功。累計 47 パネル。
- 第 20 弾：ReferenceSymmetry を窓口経由に。パネルが直接呼んでいた `ReferenceSymmetryOperation.ApplyAsNewObject` を `ApplyReferenceSymmetryCommand`（Writes=AddOnly。REF・対象は IsMeshRef/Read）へ移した。受け口は `BuildMinimalToolCtx` を渡す。パネルは `GetModel`（IModelView）・`SendCommand`（失敗理由を返す `DispatchFromPanel`）・`GetModelIndex`。成功時の表示は「完了しました」になった（従来は作成名と移植頂点数。結果はコマンドの ReportData に `newMasterIndex`・`transferred`・`message` として出す）。ビルド成功。累計 48 パネル。E の対象パネルはすべて窓口経由になった。
- 確認（Unity 再起動後、Play）：`applyReferenceSymmetry`（REF=0・対象=1、許容誤差 0.001、キューブ 2 個）→ 成功、`newMasterIndex=2`・移植 84 頂点。`motionClip.load`（存在しないパス）→ `error=ファイルが見つかりません`・`clipLoaded=false`、`queryToolState` で概要 13 項目と操作 6 個が出ることを確認。パネル画面での操作は未確認。
- 未確認：BoneEditor（`PlayerBoneEditorSubPanel.Helpers.cs:297`）、MotionClipTest（:465）、VMDTest（:500）の Undo 直接操作。
- ビルド成功。
- 付記：SetBoneParent の newParent は Write と宣言したが、親側を書き換えるかは処理を読んでいない。

| 型・引数 | 理由 |
|---|---|
| ImportPmx/Mqo/Obj/Stl/VrmFile | 読み込み先が既存モデルか新規かを未確認 |
| ApplyThinPlateMorphResult（型・targetMasterIndex） | 説明は「新規オブジェクトとして追加」、引数の説明は「変形結果を書き込む描画オブジェクト」で食い違う |
| CreateObjectArray（型・params.targetMasterIndex） | 入れ子の `targetMasterIndex` の役割が未確認 |
| SelectDrawablesByName | 既存判定は名前から対象を引いており、属性では表せない |
| SetActiveWorkAxis（型・masterIndex） | 処理が Core 側に委譲されており、何を書くか未確認 |
| BooleanMesh.bMasterIndex | 削る側を消す・隠すか未確認 |

**条件によって書き込み範囲が変わるもの（厳しい側で宣言した。I-3 で扱いを決める必要あり）**
- MergeMeshes：`CreateNewMesh` が真なら元を読むだけで新規追加、偽なら基準を書き換え他を削除（`PlayerCommandDispatcher.TPoseMergeMorph.cs:157-248`）
- Primitive 系・Frill・Pipe・PlaceObject：配置モードと `AddTargetIndex = -1`（選択の先頭）
- ApplyBlend：新規オブジェクト作成時は宛先を書かない（既存 `ResolveTargets` と同じ条件分岐）
- PlaceSpringBoneLadderChains：元はウェイトを塗るときだけ書く
- SetWorkAxis・RecallWorkAxis・ResolveTJunctions：-1 は実行時の編集対象・アクティブな作業軸

**既存の判定から結果が変わる宣言**
- SetWorkAxis・RecallWorkAxis：既存は「担当と無関係」。説明に「書き込み先の作業軸オブジェクト」とあるため Targets／Write とした。
- SetBatchLock・SetMirrorEnabled・SetBatchMirrorType・RenameMeshes ほか、既存でモデル全体判定に落ちていた型のうち引数で対象を持つものは Targets とした。

### 10.4 I（対象解決の網羅）変更計画（案・未承認）

**方針**：手書きの switch を増やさず、コマンド定義の属性から対象を引く。既存の `PanelCommandMeshRefs.cs` 冒頭注記「コマンド種別ごとの手書き表を作らない。印は属性側に持たせる」と同じ考え方に揃える。

| # | 対象 | 内容 |
|---|---|---|
| I-1 | `PLCommandAttribute` | コマンドの書き込み範囲を宣言する項目を追加：書き込まない（照会・書き出し等）／パラメータで示す対象だけ／新規追加のみ／モデル全体。未宣言は検査で数える |
| I-2 | `PLParamAttribute` | `IsMeshRef` の付いたパラメータに「書き込み先／読むだけ」の区別を追加 |
| I-3 | `RemoteOwnership.ResolveTargets` | 手書き switch を、I-1・I-2 の属性から対象を引く処理に置き換える。`IsOwnershipExempt` も I-1 の「書き込まない」に統合する |
| I-4 | 検査 | `PanelCommandFactoryAudit` に、I-1 未宣言のコマンド型と、I-2 未指定の `IsMeshRef` を数える項目を追加。`queryCommandAudit` で確認できるようにする |
| I-5 | 全コマンド型 | 属性を付ける。手書き switch の現在の分類（`RemoteOwnership.cs` のコメント付き）をそのまま移し、未分類の 187 型は定義を読んで個別に宣言する |

- `IsUiAutomation`・`IsScenario`（リモートから受けない）は対象解決とは別の規則なので、I-3 の置き換え対象に含めない。

---

## 残件（E 完了時点。パネルをクライアントへ移すために必要なもの）

E で全パネルが窓口（`IProjectView`・`IModelView`・`IMeshView`）・コマンド・ツールの窓口（`IToolSurface`）経由になった。ただしクライアント側で動かすには以下が足りない（コードで確認した事実）。着手順は X-1 → X-2 → X-3（X-1 が無いと X-2・X-3 を進めても値が出ない）。

| 番号 | 内容 | 現状（事実） |
|---|---|---|
| X-1 | ツールの窓口の読み取りをクライアントで使えるようにする（ホストへ問い合わせて結果を受け取る経路） | `ClientToolSurface.TryGet` は常に false、`GetGroup` は空。ツールの概要を読むパネル（blend・morphExpression・springBone・skinWeightNumeric・motionClip など、`Surface.Get*` を使う全パネル）はクライアントでは値が空になる |
| X-2 | スナップショット（`ModelSummary`・`MeshSummary`）に載せていない項目を載せる。本体でだけ計算する項目は X-1 と同じ問い合わせ経路で取る | 下の一覧の項目は Summary 側で 0・空・null・false を返す |
| X-3 | パネルのクライアント側配線 | クライアントへ移したのは MaterialList（`MaterialListClient`）だけ |

### X-2 の対象（Summary 側が既定値を返す項目）

- `IModelView`：`MaterialCount`・`CurrentMaterialIndex`・`GetMaterialSlot`、`MorphExpressions`、`HumanoidMappingCount`・`HumanoidMissingRequiredCount`・`HasAnySkinWeight`・`HasTPoseBackup`・`AvatarRetarget`、`SpringBoneColliderGroupNames`、`HasVrmMeta`・`VrmMetaCopy`・`HasVrmLookAt`・`VrmLookAtCopy`
- `IMeshView`：選択件数、`HiddenFaceCount`、`NormalExcludeSets`、ミラー焼き込みの状態（`IsMirrorBakedState` ほか）、ポーズ層 "Manual"（`HasManualPoseLayer`・`ManualPoseDelta*`）、`WorldPosition`、可動域（`HumanBodyBone`・`IsHumanLimitCarrier`・`HasCustomHumanLimit`・`HasHumanLimit`・`HumanLimit*`）、揺れもの（`IsSpringBoneCarrier`・`SpringBoneColliders`・`HasSpringBoneChainRoot`・`SpringBoneChain*`）、`VrmFirstPerson`・`IsVrmFirstPersonCarrier`
- 本体でだけ計算する項目（問い合わせ経路で取る）：`IModelView.DiagnoseTPose()`、`IMeshView.InspectVertexIds()`・`InspectPartsIds()`、`IProjectView.PreviewVertexTransfer(...)`、`WorkAxisLibraryNames`
- `LiveModelView.ObjectGroups` は空を返す（本体の `LiveProjectView` 経由でだけ写しを作る）
