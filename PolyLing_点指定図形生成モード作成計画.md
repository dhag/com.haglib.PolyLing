# PolyLing 点指定図形生成モード 仕様

## 1. 概要

PolyLing に、新しい「点指定図形生成モード」を追加する。

本機能は、既存の「面追加」と同様に 3D プレビュー画面上で頂点を順番に指定し、その指定点を基準として以下の図形を生成する。

- 線分：円筒または角柱
- 三角：三角形の板
- 四角：四角形の板

本機能は「高度な図形」として登録する。

基本的な操作思想は以下である。

1. 3D プレビュー上で必要な点を順番に指定する。
2. 右ペインで分割数、奥行き、断面画数などを調整する。
3. 調整結果は即時に 3D プレビューへ反映する。
4. 必要に応じて既存メッシュとの共有可能箇所をハイライトする。
5. `[生成]` ボタンで確定する。

---

## 2. UI 上の位置付け

### 2.1 登録カテゴリ

「高度な図形」に 1 項目として登録する。

推奨名称：

- 点指定図形
- Point Defined Primitive

### 2.2 図形モード

右ペイン上部で以下を切り替える。

```text
[ 線分 ] [ 三角 ] [ 四角 ]
```

内部的には以下のような列挙を想定する。

```csharp
public enum PointPrimitiveMode
{
    Line,
    Triangle,
    Quad
}
```

---

## 3. 共通操作

### 3.1 点の指定

3D プレビュー画面上をクリックして点を指定する。

- 線分：2 点
- 三角：3 点
- 四角：4 点

既存頂点へスナップできるようにする。

各指定点は単なるワールド座標ではなく、可能なら以下の情報を保持する。

```csharp
public struct PointPick
{
    public Vector3 WorldPosition;
    public int MeshIndex;      // 既存メッシュに属さない場合は -1
    public int VertexIndex;    // 既存頂点でない場合は -1
}
```

これにより、既存頂点へスナップした点は、生成時にその既存頂点を直接再利用できる。

### 3.2 点の順序

点はユーザーがクリックした順番を保持する。

四角形は以下の周回順で指定する。

```text
P0 -> P1 -> P2 -> P3 -> P0
```

三角形も同様に、

```text
P0 -> P1 -> P2 -> P0
```

の順番を保持する。

### 3.3 生成前プレビュー

必要点数が揃った時点で仮メッシュを表示する。

右ペインの各スライダ・数値入力を変更すると、仮メッシュを即時再生成する。

プレビューと実生成では、可能な限り同一の MeshBuilder を使用する。

推奨構成：

```text
PointDefinedMeshBuilder
    ├─ BuildLine(...)
    ├─ BuildTriangle(...)
    └─ BuildQuad(...)
```

プレビュー用と生成用で別実装を持たないこと。

---

## 4. ビルボードによる表裏と奥行き方向の決定

本機能では、三角形・四角形の奥行き方向を、頂点列から求めた面法線だけでは決めない。

3D プレビュー画面のビルボード方向、すなわち現在のカメラ視線方向を基準として決定する。

### 4.1 奥行き方向

ユーザーが見ている画面に対して奥行きを付ける。

概念的には以下である。

```text
ユーザーから見える側
        ↓
      表面
        ↓  Depth
      裏面
```

奥行き量 `Depth` は右ペインであらかじめ指定する。

### 4.2 表裏

頂点の周回順は形状構築に使うが、表裏の最終判定はビルボード基準で決める。

つまり、

- カメラ側に向いている側を表面
- 反対側を裏面

とする。

ユーザーが P0 -> P1 -> P2 -> P3 の順序を逆向きに指定した場合でも、最終的な表面の向きはビルボード基準で補正する。

### 4.3 裏面頂点

表面頂点 `Pi` に対して、ビルボード由来の奥行き単位ベクトルを `D`、奥行き量を `d` とすると、裏面頂点 `Qi` は概念的に以下とする。

```text
Qi = Pi + D * d
```

符号は「表面から裏面へ向かう方向」に統一する。

---

## 5. 線分モード

### 5.1 入力

2 点を指定する。

```text
P0 ---------------- P1
```

この線分を円筒または角柱の中心軸とする。

### 5.2 右ペイン項目

最低限、以下を持つ。

```text
半径
断面画数
縦方向分割数
キャップ ON/OFF
```

例：

```text
半径            0.05
断面画数           8
縦方向分割         4
キャップ          ON
```

### 5.3 断面画数

断面画数により形状を連続的に変える。

- 3：三角柱
- 4：四角柱
- 6：六角柱
- 8：八角柱
- 16 以上：円筒に近い形

将来拡張として「断面回転角」を追加してもよい。

---

## 6. 四角モード

### 6.1 入力

4 点を周回順に指定する。

```text
P3 -------- P2
 |          |
 |          |
P0 -------- P1
```

四角形は完全な長方形である必要はない。

### 6.2 分割

右ペインで以下を指定する。

```text
横方向分割数
縦方向分割数
奥行き量
奥行き方向分割数
```

表面格子は 4 辺を使った双線形補間を基本とする。

内部の各セルは、隣接セルと頂点を共有する。

### 6.3 厚み

表面格子をビルボード由来の奥行き方向へ押し出し、裏面格子を作る。

表面・裏面・側面は可能な限り同一頂点を共有する。

同じ位置に別頂点を生成して後からマージする方式を基本としない。

---

## 7. 三角モード

### 7.1 入力

3 点を指定する。

```text
        P0
       /  \
      /    \
    P1------P2
```

デフォルトでは開始頂点 `P0` を三角形の頂点とする。

右ペインで、どの指定点を「頂点」とみなすか変更できるようにする。

```text
頂点:
[ P0 ] [ P1 ] [ P2 ]
```

### 7.2 分割

右ペインで以下を指定する。

```text
底辺分割数
高さ方向分割数
奥行き量
奥行き方向分割数
```

三角形は、底辺側では指定分割数を持ち、頂点側へ向かうにつれてメッシュ列が集中する構造とする。

概念図：

```text
             A
            /|\
           / | \
          o--o--o
         /|  |  |\
        o--o--o--o
       /|  |  |  |\
      B--o--o--o--C
```

頂点 A では複数の重複頂点を作らず、1 頂点へ集中させる。

最上段は三角形面、その下は可能な範囲で四角形面として構成する。

---

## 8. 共有頂点・共有辺の基本方針

### 8.1 新規形状内部

新規に作るメッシュ内部では、同じ幾何学的位置にある頂点を別々に持たせず、原則として 1 頂点を共有する。

対象：

- 隣接セル間
- 表面と側面の境界
- 裏面と側面の境界
- 三角形の頂点集中部

### 8.2 既存メッシュとの共有

指定点が既存頂点へスナップしている場合、その頂点を可能な限り直接再利用する。

特に、新規図形の辺と既存メッシュ上の辺列が一致可能な場合は、既存頂点列をそのまま共有する。

---

# 9. 既存辺との接続仕様

この部分は本機能の重要仕様である。

単純に「P0 と P1 が既存頂点にスナップしていれば辺を共有する」という実装にしてはいけない。

新規図形側の分割数と、既存メッシュ側の頂点列数が一致する場合だけ、完全な頂点共有を行う。

ユーザーは右ペインの分割数スライダを操作し、既存メッシュ側と分割数が一致した瞬間に表示されるハイライトを見て、適切な分割数で止める。

---

## 9.1 用語

以下のような新規辺を考える。

```text
P0 ---------------- P1
```

この辺の分割数を `N` とする。

例えば `N = 4` の場合、新規メッシュ側の辺は次のようになる。

```text
P0 -- v1 -- v2 -- v3 -- P1
```

この場合：

```text
分割数      = 4
エッジ数    = 4
頂点数      = 5
```

したがって内部判定では、混乱を避けるため「分割数 = エッジ数」と定義する。

既存経路の頂点数を使って比較する場合は、

```text
既存経路頂点数 == 分割数 + 1
```

である。

---

## 9.2 接続判定の開始条件

新規図形のある 1 辺について、両端点が既存メッシュの頂点へスナップしている場合のみ、既存経路探索を行う。

例えば：

```text
P0 = 既存頂点
P1 = 既存頂点
```

であることが必要。

どちらか一方でも新規点なら、その辺については既存頂点列との完全共有判定を行わない。

---

## 9.3 最短経路探索

P0 と P1 が同一既存メッシュ上の頂点である場合、その既存メッシュのエッジ接続グラフ上で、P0 から P1 までの最短経路を求める。

ここでいう最短経路とは、基本的には通過エッジ数が最小の経路である。

例：

```text
P0 -- A -- B -- C -- P1
```

この場合、

```text
既存経路エッジ数 = 4
既存経路頂点数   = 5
```

である。

---

## 9.4 同じ長さの最短経路が複数存在する場合

格子状メッシュなどでは、P0 から P1 まで同じエッジ数の経路が複数存在する場合がある。

この場合、単純に BFS で最初に見つかった経路を採用してはいけない。

新しく作ろうとしている直線辺 `P0-P1` に、幾何学的に最も近い既存経路を採用する。

各候補経路について、中間頂点から線分 `P0-P1` までの距離を評価する。

概念的には以下の評価値を用いる。

```text
E = Σ distance(Vi, segment(P0, P1))^2
```

`E` が最も小さい経路を採用する。

これにより、ユーザーが画面上で「この既存辺列につなぎたい」と見ている経路が優先される。

---

## 9.5 分割数一致判定

既存最短経路が見つかった後、次を比較する。

```text
既存経路エッジ数 == 新規辺の分割数
```

一致した場合のみ、「完全共有可能」とする。

例：

```text
既存側
P0 -- A -- B -- C -- P1

既存経路エッジ数 = 4
```

新規側スライダが：

```text
横分割 = 3  -> 不一致
横分割 = 4  -> 一致
横分割 = 5  -> 不一致
```

となる。

ユーザーが `4` にした瞬間に共有可能状態となる。

---

## 9.6 一致時のハイライト

分割数が一致した場合、3D プレビュー上で該当する既存経路を明確にハイライトする。

概念例：

```text
P0 ===== A ===== B ===== C ===== P1
```

表示方法は以下のいずれかでよい。

- 緑色の太線
- 発光色
- 頂点と辺の強調表示

重要なのは、ユーザーがスライダを操作しながら、

```text
「今の分割数なら既存辺と完全共有できる」
```

ことを視覚的に即座に理解できることである。

右ペインにも補助表示を出してよい。

例：

```text
共有可能: 4 分割 / 既存経路 4 エッジ
```

---

## 9.7 一致しない場合

分割数が一致しない場合でも、生成そのものは禁止しない。

つまり以下の挙動とする。

```text
一致しない
    -> 通常の新規頂点として生成

一致する
    -> 既存頂点を共有して生成
```

これにより、ユーザーが既存メッシュとの接続を意図しない場合も自由に生成できる。

---

## 9.8 一致時の実生成

分割数が一致した場合、新規辺上の頂点を新たに作らない。

例：

新規側の論理頂点：

```text
P0 -- v1 -- v2 -- v3 -- P1
```

既存側：

```text
P0 -- A -- B -- C -- P1
```

一致時には、

```text
v1 = A
v2 = B
v3 = C
```

として、既存頂点番号そのものを新規面から参照する。

したがって、生成後に頂点マージするのではなく、最初から同一頂点インデックスを共有する。

これは非常に重要である。

NG：

```text
既存 A と同じ位置に新規 v1 を作る
    -> 後で MergeVertices
```

OK：

```text
新規面が最初から既存 A を参照する
```

---

## 9.9 四角形での共有判定対象

四角形では 4 辺すべてについて独立に共有判定する。

```text
P3 -------- P2
 |          |
 |          |
P0 -------- P1
```

対象辺：

```text
P0-P1
P1-P2
P2-P3
P3-P0
```

それぞれについて、

1. 両端が既存頂点か
2. 同一既存メッシュ上か
3. 最短経路が存在するか
4. 最短経路エッジ数と分割数が一致するか

を判定する。

横方向・縦方向の分割数は、それぞれ対応する 2 辺に適用する。

例：

```text
P0-P1, P3-P2 -> 横分割数
P0-P3, P1-P2 -> 縦分割数
```

---

## 9.10 三角形での共有判定対象

三角形では 3 辺すべてについて共有判定可能とする。

特に底辺はユーザーが分割数を明示するため、共有判定の対象として重要である。

```text
        A
       / \
      /   \
     B-----C
```

底辺 B-C の既存経路エッジ数と底辺分割数が一致した場合、底辺頂点列を完全共有する。

斜辺については高さ方向分割とのトポロジ構造上、単純な一定分割とは一致しない場合があるため、V1 では底辺共有を優先してもよい。

ただし、実装可能なら斜辺についても、生成される境界頂点数と既存経路頂点数が一致するとき共有してよい。

---

## 9.11 線分モードでの共有

線分モードは中心軸を指定する機能であり、P0-P1 自体が表面境界ではない。

したがって、線分モードでは P0-P1 経路を既存辺と共有する必要はない。

ただし P0、P1 が既存頂点へスナップしている場合、それらを円筒端面中心の基準点として使用する。

---

## 9.12 分割スライダ操作時のリアルタイム判定

分割数スライダを動かすたびに、共有可能判定を再実行する。

処理の流れ：

```text
ユーザーが分割数を変更
        ↓
新規プレビューメッシュを再生成
        ↓
各対象辺について既存最短経路を確認
        ↓
経路エッジ数 == 分割数 ?
        ↓
Yes -> 共有可能ハイライト
No  -> 通常表示
```

ただし、最短経路探索そのものは P0/P1 が変わらない限りキャッシュしてよい。

つまり、スライダ変更時には毎回 BFS をやり直す必要はない。

推奨：

```text
点指定変更時
    -> 既存最短経路探索
    -> 経路をキャッシュ

分割数変更時
    -> キャッシュ済み経路のエッジ数と比較するだけ
```

---

## 10. 右ペイン UI

### 10.1 共通

```text
モード
[線分] [三角] [四角]

点をクリア
[Clear]

[生成]
```

### 10.2 線分

```text
半径             Slider + 数値入力
断面画数         SliderInt + IntegerField
縦方向分割数     SliderInt + IntegerField
キャップ         Toggle
```

### 10.3 三角

```text
頂点指定         [P0] [P1] [P2]
底辺分割数       SliderInt + IntegerField
高さ方向分割数   SliderInt + IntegerField
奥行き           Slider + FloatField
奥行き分割数     SliderInt + IntegerField
```

### 10.4 四角

```text
横分割数         SliderInt + IntegerField
縦分割数         SliderInt + IntegerField
奥行き           Slider + FloatField
奥行き分割数     SliderInt + IntegerField
```

分割数を変更した際、共有可能な既存辺が見つかった場合は右ペインにも状態表示する。

例：

```text
共有可能
横辺 P0-P1 : 4 / 4
横辺 P3-P2 : 共有なし
縦辺 P0-P3 : 6 / 6
縦辺 P1-P2 : 共有なし
```

---

## 11. 生成ボタンの有効条件

### 線分

- 2 点が指定済み

### 三角

- 3 点が指定済み
- 3 点が完全同一点ではない
- 面積がほぼ 0 でない

### 四角

- 4 点が指定済み
- 周回順で図形を構成可能
- 明らかな自己交差四角形でないことを推奨

自己交差時は警告表示し、生成不可としてよい。

---

## 12. 実装構成案

```text
Runtime/Poly_Ling_Player/View/PrimitiveMesh/
    PlayerPrimitiveMeshSubPanel.PointDefined.cs

Runtime/Poly_Ling_Player/View/ToolHandlers/
    PointDefinedToolHandler.cs

Runtime/Poly_Ling_Main/Tools/PrimitiveMesh/
    PointDefinedMeshBuilder.cs
    PointDefinedParams.cs
```

役割：

### PlayerPrimitiveMeshSubPanel.PointDefined.cs

- 右ペイン UI
- モード切替
- スライダ
- 生成ボタン
- 状態表示
- ハイライト状態表示

### PointDefinedToolHandler.cs

- 3D クリック入力
- 既存頂点スナップ
- PointPick 管理
- 点の追加・削除・クリア
- プレビュー更新要求

### PointDefinedMeshBuilder.cs

- Line / Triangle / Quad の純粋なメッシュ生成
- UI には依存しない
- プレビューと実生成で共用

### 共有辺探索ヘルパー

別クラスに分離してよい。

```text
ExistingEdgePathMatcher.cs
```

責務：

- 既存頂点間の最短経路探索
- 同長経路の幾何学的比較
- 新規分割数との一致判定
- 共有に使用する頂点インデックス列の返却

---

## 13. 共有辺判定 API の推奨形

例：

```csharp
public sealed class EdgeShareMatch
{
    public bool CanShare;
    public int MeshIndex;
    public List<int> VertexPath;
    public int EdgeCount;
    public float PathError;
}
```

判定：

```csharp
EdgeShareMatch TryMatch(
    PointPick start,
    PointPick end,
    int subdivisionCount);
```

結果：

- `CanShare == false`
  - 通常の新規頂点列を生成する

- `CanShare == true`
  - `VertexPath` に含まれる既存頂点をそのまま利用する
  - プレビューでは該当経路をハイライトする

---

## 14. 重要な設計原則

本機能では以下を守る。

1. プレビューと実生成で同じ MeshBuilder を使用する。
2. 新規メッシュ内部では可能な限り頂点を共有する。
3. 既存メッシュと共有可能な場合は後処理マージではなく、既存頂点番号を直接再利用する。
4. 分割数が一致しない場合でも生成は禁止しない。
5. 分割数が一致したときだけ、既存経路との完全共有を行う。
6. 共有可能状態は必ず 3D プレビュー上でハイライトする。
7. ユーザーは分割数スライダを操作し、ハイライトを見ながら共有可能な分割数を選択できるようにする。
8. 表裏および奥行き方向はビルボード基準で決定する。
9. 四角形の入力点は周回順を守る。
10. 三角形ではどの点を頂点とするか右ペインから変更可能にする。

---

## 15. ユーザー操作例：既存辺との完全共有

既存メッシュに以下の辺列があるとする。

```text
A0 -- A1 -- A2 -- A3 -- A4
```

ユーザーが A0 と A4 を新規四角形の P0, P1 として指定する。

このとき既存最短経路は、

```text
A0 -> A1 -> A2 -> A3 -> A4
```

であり、

```text
エッジ数 = 4
頂点数   = 5
```

である。

ユーザーが横分割スライダを操作する。

```text
横分割 = 2   -> ハイライトなし
横分割 = 3   -> ハイライトなし
横分割 = 4   -> A0-A1-A2-A3-A4 をハイライト
横分割 = 5   -> ハイライト解除
```

ユーザーは `横分割 = 4` で止めて `[生成]` を押す。

生成時、新規四角形の下辺について、

```text
新規頂点を 5 個作る
```

のではなく、

```text
A0, A1, A2, A3, A4
```

をそのまま利用する。

結果として、新規面と既存面は完全に同じ頂点列を共有する。

この挙動を本機能の標準とする。
